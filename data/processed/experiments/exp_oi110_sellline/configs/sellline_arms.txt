# OI-110 删除估值减持线（§12.146，2026-08-31，用户指令）。**轨道 A（机制修复）**，跑数前登记。
# 依据：§12.1「两条线必须重解到同一在册合格面」的对齐总体是全面板在册观测，而该线的应用总体
# 是「只买 P/V ≤ 买入线、平均持有 124 天」的持仓子集，两者不同（登记于 OI-110）。
# 采纳依据为机制正确性，回测只作护栏：闸门与否决不得触发、主读数损失不超过 1pp。
# BASE 已删该线；对照臂用 `--sell-line 2.4257` 把删除前的行为原样装回，
# 故 Δ(SL24257 − BASE) = 删除的反号效应，且该臂的绝对读数应复现 §12.1 在册 BASE 读数。
BASE|
SL24257|--sell-line 2.4257
