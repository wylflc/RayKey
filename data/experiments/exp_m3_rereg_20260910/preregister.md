# m3 首批同批重算（记账修复后）预登记

日期 2026-09-10。依据回测日志 §12.222（主读数冻结为同窗口配对差、计量版本 m3）与 OI-169/170（新仓首日按成交日收盘盯市；买入含费税守卫；同日对冲退款先在授信内融回）。

固定口径：
- 引擎与扫描器为修复后的现行版本（v4.174）；`BASE` 参数 = `sweep_backtest_configs.BASE`，生产状态文件、14 个标准起点、m3。
- 臂只有三条：`BASE`、`EBDS02`（利差 <2pp 去融资，零响应对照）、`EBDS03`（利差 <3pp 去融资，OI-171 在评候选）。不新增候选、不改阈值、不改双表判定。
- 两次扫描：① `BASE` 单臂走正式扫描器（summary 落台账，作在册重登）；② 三臂走 `high_base_engine.py` 包装（与 §12.220 同一候选实现），全样本／剔除集 A 自动两遍，U = A ∪ EBDS03 2011-11-01 起点前五赢家，U ≠ A 时固定剔除集单遍。两次扫描的 `BASE` 行须逐字段相同。
- 产出：在册重登读数（对旧在册的逐起点配对差只描述记账修复的影响）、EBDS03/EBDS02 的判定翻转表（旧记账 m2 主读数、旧记账同窗读数、新记账两种读数、四读数判定）、`BASE` 负现金日数核对（须为 0）。
- 判定按冻结的 m3 规则；EBDS03 是促成换尺的候选，即使通过也只取得 §12.1 第 4 款待考察资格，采纳前仍须补边际、K 剂量、成本与信号层三表。
