# OI-198：负少数股东账面扣减的历史同步

北京时间 2026-09-21，修复前提交 `85177907`，跑数前登记。

轨道 A：§6.5.1 股权桥中少数股东权益是扣减，负账面不构成母公司现金请求权。
成文式显式补齐零下界，与 `7bd97bef` 引入的现行生产桥一致；保留账面原符号。
旧实现在 m=0 或 EV−fin_nd≤0 时跳过盈利份额比较，可能把负账面加给母公司。
本批不改变 §9.3.1、BASE 参数、买入线、换仓边际或回测价格末端。

先流式普查全部存量候选／B2报告带，包括负零和拒绝行，不能把 OI-197 的
19家156键当作全市场范围。冻结全部七份历史输入、小型生产输入、行情与财务哈希。
全BASE面板代码按原字节提取，校验完整覆盖；保留旧全量输入供复现。

对可能受影响的代码按§6.7重建。NEW只使用现行生产源码，不加载任何旧桥兼容层。
历史桥仅在独立对照重建中使用：报告逐字段须等同存量，差异须能由旧／新桥唯一归因；
两侧分别识别旧键，再验证混合OLD逐日状态完全复现。未解释漂移立即停止发布。
非目标行保留原字节，目标中不变字段保留原值；不以通用误差阈值掩盖估值变化。

按§12.1使用 `sweep_backtest_configs.BASE`、标准14起点、m3、0bp，跑OLD/NEW
全样本／固定去赢家A／并集U（U=A时明确复用）。旧BASE锚点须逐字段复现登记指标。
轨道A护栏、全/A配对读数、资金风险、非重叠窗口及信号层诊断在发布前落盘；
本批为既有机制与存量同步，沿用现行参数，不进行收益择优或未请求的滑点压力。

通过后按文件哈希检查并原子替换历史状态，按§6.7同步当前带、档案、核心池与阅读版，
同键重登BASE并留旧输入。核对每日执行凭据、账户、持仓及影子原始观测档；
若当前估值没有变化，用内容哈希证明无需更改执行快照，不能仅假定下游未受影响。
旧实验的历史证据不改写，OI-198完成后的现行生产及重建不再依赖OI-197旧桥清单。
