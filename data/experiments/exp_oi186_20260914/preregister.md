# OI-186 成交确认后启动冷却：预登记

2026-09-14，北京时间，跑数前冻结。轨道 A：用户确认「实际成交后才启动；已启动后按合格机会递减」，对应工作流 §9.3.3；修复未执行计划、资金不足、净额为零却启动冷却的实盘差异。不是收益参数优化，不增加可调阈值。

两臂共用 `sweep_backtest_configs.BASE` 正式输入和全部 14 起点。BASE（对照）追加 `--lot-cooldown-start plan`；CONF（修复）使用 `confirmed`。只改变确认启动、实际净成交比例、已有冷却逐合格机会消费。比例的档位冻结在原信号日净资产；委托数量的 OI-180 保持独立待办。测试覆盖无成交、部分成交、对冲零净额、重复回执、同日重扫与历史隔离。

复用 OI-179 已验证的面板状态子集前，重新核验生产文件与子集 SHA256 均与其等价性证据一致；任一变化则停止，不能跳过等价性验证。检查所有原始行情末日，并冻结实际输入、引擎、共享冷却实现及当前基准命令的哈希。当前历史末端不因本次修复刷新。

顺序：先一个锚点验证旧模式复现当前在册 BASE；再跑全样本与旧 BASE 前五赢家 A；U 取旧、新赢家并集，同 A 时复用。同起点同窗配对，主窗口固定滚动 5 年，m3 标准指标及尾部按 §12.1 全报。轨道 A 护栏：全样本主读数与全期 CAGR 损失各不超过 1pp，滚 5 回撤中位变深不超过 3pp，无过半起点负窗 0→正。另两臂按每边 10/20/30bp、同样固定 A/U 重跑并报告翻判。无需数值邻档：本次确认的是记账时点，未调整倍率或次数公式。

不因看到收益再变更语义或窗口。候选资格与估值排序公式未改变，信号层输入及 P/V 分档沿用同一已验证面板；本次专查执行路径差异。结果、限制及生效日回写日志；用户已确认该执行语义，历史结果不作为撤销未成交不得记成交这一约束的理由。
