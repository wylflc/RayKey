# OI-172／OI-173 历史判定核对（2026-09-13）

`recheck.py` 对仓库内全部 m3 扫描文件的候选臂各判两次：旧实现（起点／窗口取交集配对、任意负窗占比增大计入否决）与现行 `sweep_backtest_configs.adoption_verdict`（判定前校验起点集合＝标准起点集、同窗窗口集合相同、决策字段有限；否决只数负窗占比由 0 转正的起点）。`recheck.txt` 逐臂列出旧判定、新判定、旧计数、新计数与新判定理由。

结果：22 份文件 190 条候选臂（含本日 OI-167／OI-174 批次的扫描文件），36 条的负窗计数变小，判定无一翻转；无一臂因起点或窗口不全转为不可判。规则变化见 changelog v4.181，回测日志 §12.232。
