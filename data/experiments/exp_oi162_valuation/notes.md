# OI-162 金融股估值入口统一

已修复。扫描、跟踪、核心池阅读版和成交估值命令共同调用 `resolve_live_band`；银行/保险继续使用既有 `bank_dividend_intrinsic` 的分红与除权算法。国债利率按信号日过滤，缺少利率或完整财年分红时返回无值，不退回旧静态带。非金融股原估值方法保留。

2026-09-07 已落盘行情复算：招商银行收盘 41.07，V=53.7736547，P/V=0.7637569，区间48.3963–59.1510；中国平安收盘56.36，V=71.6115911，P/V=0.7870248，区间64.4504–78.7728。按各界面原展示精度取整；7条金融股阅读行与2条持仓跟踪行已更正，原日志保留并追加关联更正。

复现：`python3 scripts/experimental/oi162_valuation_audit.py --as-of 2026-09-07`（默认只核对；`--apply` 写回）。审计使用当日既存收盘/均线，无联网取价。`verification.json` 保存实际数值、旧行、新行、阅读行及受保护文件哈希。持仓、账户、买入/卖出清单逐字不变。

回归：`test_live_financial_valuation.py` 覆盖银行与保险、静态带缺失、分红缺失、利率按日期而非文件顺序取值、除权日边界；同跑故障语义、每日执行清单、分红、参数同步和信号日期检查。成交回写先运行 `resolve_trade_valuation.py --as-of <日期> --code <代码> --price <成交价>` 取得两侧同源P/V；命令只输出估值，不变更持仓。
