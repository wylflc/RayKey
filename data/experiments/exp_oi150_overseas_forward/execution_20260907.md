# OI-150 恢复执行登记

用户于 2026-09-07 要求完成 OI-150。本登记写于本次正式估值与回报计算之前；此前已见 OI-159 补充分档结果和 OI-150 四家公司冒烟结果，不能声称从未看过美股结果。

原预登记的 653 个 CIK、4,400 个公司年、2010-04 至 2021-03 月末、八个 P/V 档、两个期限及通过/剔除阈值全部保留。原 universe 两表不改；按 CIK 连接数据，不以标普500成分替换样本。运行输入及脚本哈希写入 manifest。

执行修复与数据接续：

1. 复用 OI-159 **原始完整** Yahoo/Tiingo 响应，经已有取价器还原未复权收盘；不用按指数成员期裁剪过的 ohlcv_us 序列。继续使用原腾讯缓存；剩余有明确证券代码的对象按既有 Yahoo/腾讯接口尝试补取。逐 CIK 记录身份依据、来源、覆盖、失败原因；同名代码有多个 CIK 且无明确映射时不强行连接。不刷新原 SEC 财报快照。
2. 估值逐观测日重算，事实（含 DEI 股数）须有非空 `filed <= t`；不复用提前一交易日生效的 OI-159 状态。所有对象以 CIK 调用统一 L2/美元模型，避免同名 ticker 意外命中海外个股配置。财报币种或 ADR/普通股换算无证据时记数据缺口，不以美元股价除非美元/未知股本口径估值。
3. 原回报口径仍为 SEC 拆股事实及季度每股股息；源价格自带的公司行动只校验拆股日期/股数口径，不用来挑选收益。缺乏可靠拆股日期或股息股本口径的记录显式降级。序列截断只在有退市申报/证券终止证据时使用末价现金终值，其他截断记缺失。
4. 公司月缺失按集合去重计数，保留无法分档的记录；分档剔除率未知部分给上下界，不把无法计算 P/V 的记录默认为零缺失。缺少比较档/观测年组或任一数据前提无法确认时为「不可判」，不能输出正式「不支持」。原两项收益判据可单独列为描述性读数。
5. 审计原 universe 的营收 frame 来源 accession 是否在入池前已经申报；后申报/无法定位者显式计数。原样本冻结便于复核，但不把当前 frames 的历史字段自动认定为历史时点可得。若该前提失败，完整运行后的结论仍是「不可判」。
6. Spearman 对相同值用平均秩；完整输出 11 个观测年组、3/5年分档、缺失原因、公司行动与输入/产物完整性。原数据门槛之外发现的身份/时点问题作为未满足的原数据前提说明，不修改收益门槛。

实现入口 `scripts/experimental/oi150_complete.py`，SLURM `scripts/slurm/oi150_complete.sbatch`。主报告 `report.md`；原冒烟文件留作历史，不覆盖。结论不改变生产模型或交易规则。
