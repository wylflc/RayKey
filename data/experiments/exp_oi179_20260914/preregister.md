# OI-179 完整公司行动日历修复（2026-09-14，结果前登记）

轨道A，依据工作流§8.3当日基准均线、§11.4除权折算及用户明确“解决OI-179”。仅纠正事件覆盖，不引入双正斜率等新生产择时条件。

- 冻结前版本c3e90b96的`exright_affine`、`daily_returns`和`moat_param_lab.total_return_index`于`legacy.py`，只供旧版本复现。旧研究文件及旧台账行保留。
- 修复仿射映射为完整事件日历，支持连续停牌期间现金、送转、配股及多事件按时序复合；空序列/首日/末日/范围外事件有边界测试，无事件路径不变，未来事件不影响历史均线。共享映射的均线、复权价、量比一起核验。
- 同源收益路径：相关性日收益与常设前向回报工具纳入两次报价间全部事件；现金仅在下一报价再投，配股按自融资总回报计扣认购款。审计其他按日取事件路径，区分股票报价日历与完整市场日历，不能机械替换正确的日历调用。必要的其他独立缺陷另列证据，不将本修复外推为完整交易引擎认证。
- 正式BASE参数、14标准起点、m3计量和历史面板均冻结。重验当前生产面板子集与沿用OI-167子集逐行digest；逐票价格末端和所有运行输入哈希登记，不刷新行情缓存。
- 机制护栏：LEGACY与修复后BASE在OI-167在册冻结输入上各跑全样本/A共28条路径；LEGACY复现当前登记1316数值和同窗；A取旧BASE2011锚点前五，U取两臂前五并集，必要时补齐。判定按轨道A：全样本同窗和全期年化配对中位均不低于−1pp，滚5回撤闸门和负窗否决不触发；全/A完整标准指标、配对差、长跑、尾部并报。
- 当前登记：若生产参考表与在册冻结副本有变化，修复后BASE另用当前参考表冻结副本跑14起点全/A，区分机制变化与输入更新；通过护栏后刷新28份标准BASE摘要、以新机制版本标记保留旧台账。
- 在修复后的当前BASE上复核上一批5个候选（BOTH1、BOTH2、BOTH3、MA20_1、MA60_1），沿用原预登记斜率定义及实现，不增参数、不择峰；完整全/A/U，U=A时复用。对照旧资格判定，若出现新U优秀资格按工作流登记，不能自动改生产择时参数。
- 信号复核分别量出：冻结旧rank1仅改收益事件/仅改MA60周期/两者同时改的影响，以及按修复后原趋势重新过滤旧rank1（被挡不补位）；当前BASE与BOTH1候选重排结果另报。每组250/750日、每日/MA60周期/周期加不重叠/锚止损周期，含亏损、缺窗、逐股逐年及样本数。旧表只作历史复现，修复后读数单独输出，不覆盖原结果。
- 审计逐票停牌事件、受影响MA/收益点、股票与信号变化；独立逐窗口重算及现金/股份账验证，核对长跑买入条件、信号时点与非重叠区间。正确的前向回报必须与独立完整事件现金账一致，不能只复现旧数。
- 烟测16CPU，正式与并行验证提交SLURM64CPU、workers≤48。每阶段输入运行前后哈希一致；复用缓存须有相同批次输入签名。通过目标测试及相关交易/复权回归、文档审计，再写报告、日志、版本与结案并本地提交。
