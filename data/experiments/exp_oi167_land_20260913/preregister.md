# OI-167 落地核验（2026-09-13，轨道 A）

§9.3.1 单票机械上限行与 §9.3.1.1 成文「本档金额 = min(一档, 可用资金, 单票上限余量)，按一手向下取整，不足一手跳过」；生产扫描器已按此执行（`room < lot_amount` 挡下），回测引擎 `run` 在一档买不起一手时经高价股一手兜底成交，未复核上限余量（`data/experiments/exp_position_cap_25_20260908/` 合成路径：净资产 18,000、25% 上限 4,500，原实现累计买到 5,000）。本批把守卫写进正式引擎（余量不足一手即跳过，不触发卖出），策略名加 `s` 标记以与原实现台账行区分。

轨道 A 依据：实现与成文标准不符，采纳依据为机制正确性；回测只作护栏（§12.1）：对在册 BASE（`exp_c030r35_land_20260910`，v4.178）同起点配对，闸门与否决不得触发，主读数与复利读数两表各自损失不超过 1pp。

核验范围：

1. 数据输入（价格缓存、逐日状态子集、面板、参考表）与在册批次哈希相同；只允许本批脚本改动，价格末端不变。
2. 14 标准起点全样本 14 条 + 去赢家 14 条：剔除集先取在册 BASE 的 2011-11-01 前五赢家做同剔除集配对；新 BASE 自身的前五赢家若不同则另跑一遍作登记用的 A 表。
3. 逐路径记录与在册 BASE 的全字段差异、守卫触发次数（`单股上限·余量不足一手·跳过`）、负现金日为 0、起点与滚五窗口集合完整。
4. 护栏用 `sweep_backtest_configs.adoption_verdict`（OI-172/173 修复后的实现）以在册 BASE 作对照；通过后 28 条摘要经 `clean_derived_artifacts.write_ledger` 登记，旧 BASE 行按策略名保留，`data/backtest/summary_BASE*.csv` 刷新为新引擎产物。
5. 新在册读数写回测日志；未通过护栏则不登记、回退引擎改动并报用户。
