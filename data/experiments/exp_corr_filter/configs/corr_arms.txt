# 相关性过滤上限剂量臂（§12.145，2026-08-31，用户指令）。轨道 B（收益动机），跑数前登记。
# 假设：§12.144 表 C 的买腿超额在 2012-2016 为负、2017-2025 为正；相关性过滤（BASE 0.70）
# 在早期把买入推离正在复利的消费／制造簇、推向不相关的周期股，可能是该残差的来源。
# 剂量而非单点：§12.1 第 4 款要求扫相邻区间、取宽平台。
# --max-corr 1.0 不关闭相关性计算（引擎判 `value > max_corr` 才跳过，1.0 恒不触发），
# 故与 BASE 走同一条代码路径，只放宽阈值；给 0 才会整块关掉 corr 对象、连带改换仓路径。
BASE|
C060|--max-corr 0.60
C085|--max-corr 0.85
C100|--max-corr 1.0
