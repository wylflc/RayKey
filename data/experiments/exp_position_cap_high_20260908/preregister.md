# 单股买入上限 70%／80%／90%／100% 预登记

登记日期：2026-09-08（北京时间）；轨道 B，用户指定收益敏感度实验。登记先于提交作业及读取新臂结果。

问题：当前 60% 单股买入上限放宽到 70%／80%／90% 后的收益如何变化？另设 100% 邻档，观察 90% 是否仍在上升区间。分母为净资产，只限制新增买入，被动超限不强制卖出。其他卖出规则照常，融资、费税、执行时点、股票池、估值及全部其他参数保持 BASE 不变。

OI-167 已知整手兜底可绕过上限，本次主候选均复用前次实验 `exp_position_cap_25_20260908/strict_cap_engine.py` 的守卫，不修改正式引擎。保留原实现 BASE 与严格 60%（PC60SSEP08）两个对照，分别报告相对当前生产实现的总变化与相对严格 60% 的单参数变化。新增候选四个：PC70SSEP08／PC80SSEP08／PC90SSEP08／PC100SSEP08；本日两轮上限实验累计 11 个候选名，不将多个起点视为独立样本。

沿用前次实验完全相同的正式输入。提交后先校验前次登记的源文件哈希和 BASE 参数；记录本次源代码、配置及参考摘要的哈希，结束时复核未变。行情末端以文件哈希一致及前次实查为依据：面板 296 只中 155 份止于 2026-08-07，140 份止于 2026-08-28，1 份退市股止于 2018-12-20；不刷新缓存，不能称为截至今日的完整行情。

标准起点读取 DEFAULT_STARTS，14 个半年起点，计量 m2。六臂同批跑全样本和 A（BASE 在 2011-11-01 起点前五赢家），共 168 路径。每个新候选都按 `ex_winner_symmetry.top5` 生成自身赢家 B 与 U=A∪B。相同 U 合并一次扫描，每组含 BASE、严格 60% 和该组全部候选；U=A 时复用 A，并记录等价性。固定 U 通过正式扫描器的 `--exclude-codes` 单遍执行；不同 U 不直接比较候选水平。每次扫描立即保存独立摘要，结束恢复普通 A 缓存，防止 U 覆盖语义。

主窗口沿用五年。主要回答用全期 CAGR 逐起点配对差中位，同时报告滚五中位／P25、非重叠五年块、逐年收益、2009／2011 两个长跑锚点、标准指标集与跨起点尾部。水平中位之差不能冒充配对差，历史差值不作为未来收益预测。收益判断按用户前文口径，不以职业经理人的回撤、集中度约束否决本次试验；正式扫描器的制度判定仍保留供核对。

本次只测试，不采纳生产参数，不重扫换仓边际。若出现按 §12.1 第 4 款定义的待考察候选，登记证据但不自动采纳。

复现：`scripts/slurm/position_cap_high_20260908.sbatch`。完整报告 `report_full_A.txt`／`report_U*.txt`，精确摘要 `summary_rows.csv`，配对差及解释 `readout.md`，运行与输入校验 `manifest.json`／`verification.json`。
