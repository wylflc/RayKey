# OI-174 剩余候选对新 BASE 的资格复核（2026-09-13，轨道 B，生产不改）

对象：`exp_equity_bond_opt_20260910` 在旧 E2 基准下取得 U 全面优秀资格的 7 个未采纳候选——C100R35、C100R40、CONFIRM2、CONFIRM3、TREND10、TREND12、C030R45。规则定义与实现沿用该目录冻结的 `policy.py`（不改一字），对照改为现行生产 BASE（v4.178 C030R35，且经 OI-167 整手守卫落地后的引擎，`exp_oi167_land_20260913`）。

预登记：

1. 8 臂（BASE + 7 候选）× 14 标准起点 × 全样本／剔除集 A／各候选 U；A = 新 BASE 2011-11-01 前五赢家（取 OI-167 落地登记值），U = A 与候选同起点前五赢家并集，U 与 A 相同时复用 A 表。
2. BASE 的 28 条全/A 路径须与 OI-167 落地扫描逐字段相同，否则不得解释候选读数。
3. 判定用修复后的 `sweep_backtest_configs.adoption_verdict`：先校验完整起点与相同窗口集合（OI-172），负窗否决按「由 0 转正」（OI-173）。U 全面优秀按 §12.1 第 4 款 `CLAUSE4` 各项配对差判。
4. 读数只回答「在新 BASE 下是否仍有资格」：仍具资格者保留为待考察候选并须另行补齐第 4 款三项与第 7 款成本压力才能进入裁定；无资格者按不采纳结案。不因本批结果改生产参数。
5. 数据输入与 53 臂批次哈希相同、价格末端不变；负现金日 0。
