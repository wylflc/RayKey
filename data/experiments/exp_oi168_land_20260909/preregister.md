# OI-168 落地预登记（2026-09-09）

用户裁定（2026-09-09）：①按原则采纳营运资金口径 `operating`（去重＋经营性应收款项融资），走 OI-128 先例——读数只登记、不作采纳依据；重解买入线等参数并重登在册读数。②给 §12.1 买入线对齐规则加容差。

## 固定在跑数之前

- 标准先改：§6.3 第 6 条生产口径 = `operating`（`RoicYear.working_capital` 与建带缺省同此）；§6.7 两条建带命令显式 `--wc-aggregation operating`；§12.1 对齐容差 0.2pp（原线在新口径上的下侧合格面与在册合格面之差 < 0.2pp 保留原线、在册合格面不改写）。
- 买入线：冻结输入上 `align_buy_line.py --registered-share 17.771` 读原线 1.0454 合格面 17.928%，差 +0.157pp，容差内保留（`alignment.txt`）。
- 生产重建：§6.7 第 2／2b／3 步在**冻结输入**（与 `exp_oi168_wc_20260909` 同一输入纪元，不刷新第 1 步）上全市场重建，令本次变化只归因于口径；重建前记录旧生产文件哈希。
- 换仓边际：新生产状态上 0.12～0.18 七档（`configs/margin_arms.txt`），14 标准起点、m2、全样本＋剔除集 A（A 由新 BASE 的 2011-11-01 前五赢家自动重选）。取法沿用用户惯例：优先宽平台、现行 0.15 在平台内则维持，在悬崖肩则取平台严格端并同报水平中位；读数不用于判定口径。
- 在册重登：新 BASE 的全样本／去赢家标准指标集、跨起点尾部、两长跑锚点，写回测日志 §12.217。
- 信号层不变量：`panel_tier_forward.py` 在新生产状态与冻结 legacy 子集各一遍，只描述。
- §6.7 第 4～7 步展示链按 `--signal-date 2026-09-09` 重出；第 7 步先 `--baseline` 报现值再校验，V 标度漂移超容差则重定基准。
- 不做：滑点档位、K 剂量、U 对称集——本次不是候选采纳，是口径切换后的在册重登（OI-128 先例）。
