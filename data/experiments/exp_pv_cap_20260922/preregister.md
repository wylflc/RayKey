# 低 P/V 取消单票上限：预登记

登记日：2026-09-22。用户要求研究，轨道 B；生产规则不变。

## 唯一差异

按信号日候选侧 P/V 判断：低于门槛时关闭单票上限；等于或高于门槛时沿用 BASE 的单票市值／净资产 60% 买入上限（不足时只补到上限、余量不足一手跳过）。不强制卖出超限仓位。每天独立判定，不锁存豁免状态。持仓侧 P/V 仍仅服务原有持仓估值和换仓卖出判断。

预先固定四臂：BASE（无豁免）、PV055、PV060（用户方案）、PV065。主研究对象为 PV060，相邻两臂只检查平台，不根据结果扩大网格。一档、授信、股债总仓位上限、止损、名单、换仓、费税、整手和 T+1 执行均继承 `sweep_backtest_configs.BASE`。默认 0bp 滑点，不额外安排滑点压力测试。

## 输入与验证

标准起点集读取 `sweep_backtest_configs.DEFAULT_STARTS`，主窗口五年、计量 m3。现行生产两侧状态按 BASE 面板代码流式提取；逐行保留源字节并记录源与子集摘要，验证子集覆盖。冻结股票池、价格、公司行动、除权价格参数、国债和股债利差。先审计每个代码行情末日并报告实际末端；本研究不把盘后扫描行情当成已刷新历史价库。

独立实验入口只在内存改写引擎，所有替换须唯一匹配，默认关闭时复现原始引擎同输入锚点全部标准字段、窗口及净值。合成测试覆盖 0.59／0.60／0.61、低估越限、高估恢复上限不强卖、T 信号与 T+1 执行、整手兜底及资金约束。

## 对照与报告

四臂均跑全样本与固定 BASE 前五赢家剔除集 A；各候选另用 U=A∪候选前五赢家做同集合 BASE 对照，U=A 时复用已完成路径。按工作流 §12.1 严格同起点同窗口配对，报告五项决策读数、全期 CAGR、回撤、集中度、长跑锚点、非重叠五年、逐年及跨起点尾部。直接报告新增集中度与强平/现金风险；绝对收益不作为未来预期。

本轮只回答研究结果，不自动采纳。若达到“去赢家全面优秀”则登记待考察项；采纳前附加验证与用户裁定仍按 §12.1。作业及资源、输入哈希、边界测试、逐路径摘要和最终报告均留档。
