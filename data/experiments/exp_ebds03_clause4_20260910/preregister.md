# 预登记：OI-171 EBDS03 §12.1 第 4 款三项 ＋ 第 7 款滑点（2026-09-10）

候选：`EBDS03` = 沪深 300 股债利差 < 3pp 时总仓位上限 100%（不新增融资、按比例减仓先偿债），其余完整恢复 `BASE`（§12.220 实现
`high_base_engine.py`，与 §12.223 同一实现）。第 2 款：v4.175 回撤通道下可采纳（§12.224）。本批补第 4 款采纳前三项与第 7 款，结果只报不判，
三项齐备后走第 8 款用户裁定。所有回测走包装引擎，summary 落 `cache/`、不进台账；判定用现行 `adoption_verdict`（含回撤通道）。

## ① 换仓边际 0.01 一档重扫（`configs/margin.txt`，12 臂 × 14 起点 × 全样本／A）
- 读数：各档对 `BASE`（0.15）的第 2 款判定；`dose_table.py` 配对差与水平中位并列；`scan_plateau.py` 找相邻连续通过区间。
- 通过标准（§12.1 第 5 款）：至少 3 个相邻档通过第 2 款（可采纳或可采纳·回撤通道）且 0.15 在平台内，或取平台严格端；单点峰不算。

## ② 剔除赢家只数 K=1／3／5／10（`configs/dose.txt`，赢家取本批 `BASE` 2011-11-01 长跑 trades 的 `contrib` 前 K）
- 读数：`ex_winner_symmetry_report.py --challenger EBDS03`：各 K 下主读数／复利／P25／滚 5 回撤配对差与第 4 款劣项。
- 通过标准：四档主读数与复利读数符号一致（同向）；任一档反号即不同向、不通过。

## ③ 第 10~12 款读数（两臂 2011-11-01 长跑，`sig/`）
- 信号层三表 250／750 日、换仓四表对照 ±0.04／0.10／0.15、P/V 分档前向；报候选对 `BASE` 的逐日配对差中位、为正日数、逐年同号年数，只描述。
- 第 11 款 Δ 归因：前三只净额占比 ≤ 100% 才可作采纳依据。第 12 款：本族已试臂数按 `scan_arms_index.csv` 计。

## 第 7 款 执行成本压力（`configs/slip.txt`，8 臂 × 14 起点 × 全样本／A；U = A）
- 读数：各档候选对同档 `BASE` 的第 2 款判定与翻转档；`BASE` 各档对 0bp 成本表；档位不作否决线。
- 0bp 复现：`BASE`／`EBDS03` 14 起点读数须与 §12.223 `sweep_full_A.txt` 逐值相同。

作业：`scripts/slurm/ebds03_clause4_20260910.sbatch`（64 CPU，56 并发）。
