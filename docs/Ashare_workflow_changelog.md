# A股工作流版本记录（docs/000_Ashare_workflow.md）

自 v1.00 起版本记录移至本文件维护，工作流正文只保留指针。编号沿革：v1-v31 为初期整数系列，v0.31 起切换 v0.x 格式，v1.00 为四袖架构定版。

> **2026-08-17 纠偏说明**：§12.80 发现 v6b 成员区间曾被回测 loader 误当成按起点完整快照，故 **v4.01~v4.06 各行登记的回测绝对值与参数增量均已失效**；版本动作本身保留作沿革。纠偏读数见 `docs/Ashare_backtest_log.md` §12.80；采纳动作见 §12.81 与 v4.07。

> **2026-08-24 精简**：逐行只留规则变化与落点；全文见 `git show da63ff92:docs/Ashare_workflow_changelog.md`。

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| **v4.77** | 2026-08-24 | 结案 OI-099：银行/保险股利折现分子由「近 365 天除权日求和」改为「最近已知完整财年每股现金分红合计」——每笔按东财 `report_date` 归财年、自董事会预案公告日（`plan_notice_date`）计入、财年在年度分配已知或过次年 4-30 时算完整；此前两次年度除权相隔不足一年即翻倍、旧笔退出再腰斩（42 只 2022 起单日 \|ΔV\| 最大值全部 >30%：工行 2024-07-16 7.22→14.50→7.29、招行 2023-07-13 32.84→70.33、宁波银行 2024-07-10 +120% 且当日首次穿越买入线；2024 起中期分红普及后同一财年两笔除权更被当成加倍），修后 >30% 者余 8 只且全在预案日、对应真实分红变动（平安银行 2024-03 派息率 12%→30%、郑州银行 2025-03 恢复分红、宁波银行 2025-04 +50%）；新增 `scripts/divspread_dividend.py` 为分子唯一实现，`rebuild_bank_bands.py` 历史逐日与扫描器 `bank_dividend_intrinsic`（档案层同引）同读；`fetch_ohlcv_history.py` 除权事件加 `plan_notice_date`/`progress` 两列并保留预案已公告未除权行（除权侧读者按空除权日跳过，重取时旧预案行整体清换），53 只银行/保险重取（4 只 2026H1 预案在库未除权）；`rebuild_bank_bands.py` 改写行同步写 `valuation_label`（此前 93,812/106,985 行标签与 P/V 不符）；`is_bank_name` 弃「以行结尾」（世联行/任子行/华致酒行/喜悦智行/永安行/三人行曾被判成银行走股利折现），改「银行/农商/张家港行」，覆盖名单 53→47 只；§6.5.1 第 4 条与 §11.4 成文、README/档案/叠加文案同改；新增 `test_divspread_dividend.py`（工行/招行真实序列，11 例）；§6.7 第 3-6 步重跑（银行行重写 123,334、丢弃 15,797；池 207 只、银行改写 8、档位无变化、审计严重 0）；面板子集状态文件重建（287 只／953,085 行）；重登 `BASE` 在册读数（§12.1）：滚5 中位／P25／最差 61.55／54.34／29.09、滚5 回撤 48.1、年化中位 48.81、换手 8.71→4.93；三线不动（对齐解与配对 A/B 见回测日志 §12.127，待裁定）。 |
| **v4.76** | 2026-08-24 | 结案 OI-098：`fetch_a_share_financial_statements.py` 增量规则由「代码已在文件即整只跳过」改为按 `--as-of`（北京历日，缺省当日）判应到年报期 = 上一年 12-31——最新 `REPORT_DATE` 早于应到期的代码整只重取并替换（东财一次返回全部年报期，替换即超集），重取失败或四套表全空保留原有行，重取后仍未到期（尚未披露）者逐次运行再试，`--refresh` 仍为全量重取；此前 §6.7 第 1 步命令不带 `--refresh`，下一轮年报披露后已在库代码的三表输入永不更新、ROIC 路径（§6.5.2.3）静默停在旧年报，与逐季脚本披露窗强制重取属同一缺陷类；顺带修 `secucode` 北交所 `920xxx` 段误发 `.SH`（四套表全空，民士达/锦波生物两只 worth_attention 一直无三表），改 `.BJ`；§6.7 第 1 步命令加 `--as-of`；新增 `test_statement_refresh.py`（无网络，桩替 `fetch` 驱动 `main()` 锁定替换/跳过/保留/跨年四路径与交易所后缀）；实跑 as-of 08-24：333 只均已到 2025-12-31、零重取，按现行名单新增补取沃格光电 603773 与两只北交所（各 13 个年报期），三表 336 只；生产带未重跑，随下次 §6.7 第 2-6 步吸收。 |
| **v4.75** | 2026-08-24 | 结案 OI-024：分层表六个研究字段全池初稿回填——`q1_reason` 与 `q2_moat_type` 补齐至 208/208（moat_type 按既有取值词表分类：行政法律独占/资源地理独占/品牌与心智/成本与规模优势/转换成本与认证锁定/网络效应与生态/事实标准与技术代际/渠道/牌照/无），`q3_reason` 100/208（注明再投资/返还/混合路径）、`q4_reason` 42/208，`q2_erosion_paths` 转录再 +6 至 182/208；全部由该行已在库字段（moat_summary/score_reason/q2_reason/peer_comparison/evidence_note/flags）归纳，不引入新证据、不改任何档位与分数；行内无对应维度内容的单元格留空，余格与逐票确认随 §5.1 季度复核；决策日志记一行批次结论，阅读版 MD 重渲染。 |
| **v4.74** | 2026-08-24 | 结案 OI-097＋OI-096。OI-097：`fetch_cost_of_equity_inputs.py` 并入 §6.7 第 1 步（rf/ERP 序列此前无任何流程刷新、停在 2026-08-07，rf 滞后整组偏移银行/保险 V）；序列刷新至 2026-08-24（rf 1.7114%→1.6794%，−3.2bp，ERP 常数 5.14%），§6.7 第 3-6 步重跑把新 rf 传导到银行/保险生产带、档案与核心池。OI-096：扫描决策日志只记结论行——`data_error`/`insufficient_price_history` 与 §7.5 复核冻结（`decision_result=review_frozen`），正常行情行不再逐股写 `decision_result=ok`（此前每日全池逐股一行、占 47MB 日志的 23%，唯一用途是 `--since auto` 探测上次扫描日）；`detect_last_scan` 改读上一份 `daily_buy_candidates.csv` 的 `trade_date`；历史 `daily_signal_state` 行随后按用户指令整类清除（19,146 行 / 11.1 MB，含退役信号机制的 wait_*/signal_watch_only/buy_candidate 等取值；全文见 `git show d8e2ffdc:data/processed/a_share_workflow_decision_log.csv`）。 |
| **v4.73** | 2026-08-24 | 结案 OI-095：跟踪器 `fetch_raw_close` 弃用自带的腾讯主源/东财备源取数（与扫描器东财主源、两家前复权序列有差时同日入场闸门 MA60 与止损生效线 MA60 不同基），改经扫描器 `fetch_daily_rows` 同一实现取收盘与 MA60（新增 `fq` 参数供不复权序列；北交所自动改道腾讯，跟踪器东财备源把 BJ 代码按深市 secid 发东财K线的路径随之消失；实测 08-21 两侧 MA60 逐位一致）；`infer_secid` 成文北交所口径（深/京同 `0.`，与 `fetch_a_share_valuation_evidence.secid` 一致，K线不得直连东财查北交所）；跟踪器与池物化的生产带改运行时按 as-of 载入（`pv_ratio.load_model_bands` 新增 `as_of` 参数，只取 `available_at ≤ as-of` 的行），历史日期补跑不再用当日之后才可得的带；§8.3/§11.3 补成文。 |
| **v4.72** | 2026-08-24 | 结案 OI-094：§5/§7 两个队列脚本的财务判据弃用 2026-05-14 指标快照（生产者已退役），缺省改读 `data/raw/financials/` 逐季面板——新增 `quarterly_panel_indicators.py` 按报告期从新到旧取每码最新一期、公告日/报告期按 `--as-of` 时点过滤、净利率与经营现金流÷营收现算（股数按归母净利÷基本EPS估）；§5.6 重大变化清单删「负债、研发」（面板无此两列，判据同步移除）；§5.3 命令块加逐季面板刷新行；传文件路径仍按旧快照读（复现口径）；删除 `data/interim/a_share_financial_indicators.csv`；名单刷新至 2026-08-24（5,695 只、快照 20260824）、06-30 面板期 4,004→4,992 行、披露/预告物化重抓，两队列按 08-24 重建（质量复核 4,364 行、报告更新 104 行）。 |
| **v4.71** | 2026-08-24 | 结案 OI-093：扫描器 §9.3.1 相关性弃用 `data/raw/ohlcv/` 本地库（不随 §8 刷新、窗口末端落后信号日数周、缺文件按 0 相关静默放行），改用扫描当日逐票已取的前复权K线（`scan_one` 落入 `CLOSE_SERIES`，两侧同源、窗口末端即信号日）；`corr_252` 按交易日对齐取交集、重叠收益率不足 120 个返回未知（与回测 `Correlations` 同阈同语义），未知不当作 0——放行但在报告以 `[相关性缺数据·OI-093]` 列名单；§9.3.1 相关性行补数据源与 120 根阈值；删 `daily_returns_window`/`pearson`。 |
| **v4.70** | 2026-08-24 | 建仓止损锚不再按价格位置退档：锚恒为成交日 MA60，仅成交日取不到 MA60（停牌回落、上市不足 60 根）时退成交日 MA20 兜底（§9.3.5；回测日志 §12.126 追问检验 2：与 v4.69 口径 23 起点 20/23 逐位相同、配对差全 0.00）；回测 `BASE --entry-below-ma60 ma20_stop→ma60_stop`（新增该取值，`ma20_stop` 降为研究口径），参数同步测试同步；重登在册读数（§12.1）：滚5 62.47/54.91/29.65、滚5 回撤 48.2、年化中位 50.57、换手 8.71；三线不动。 |
| **v4.69** | 2026-08-24 | 取消 §9.3.1 走势行的建仓放弃规则（回测日志 §12.126 追问检验：与保留统计不可分、15 年仅触发 4~5 次）：走势行只留「T 日 `收盘 > MA20 > MA60`」，§9.3.2 第 5 步删同句，§9.3.5 改「T 日收盘低于成交日 MA60 的建仓锚退成交日 MA20、同周期判」；回测 `BASE --entry-below-ma60 skip→ma20_stop`，`skip`/`skip_fill` 降为研究口径，参数同步测试同步；重登新基准在册读数（§12.1）：滚5 62.47/54.91/29.65、滚5 回撤 48.1、年化中位 51.76、逐年中位 47.33；三线不动。 |
| **v4.68** | 2026-08-24 | 结案 OI-092（回测日志 §12.126）：三处引擎与 §9.3 成文不符处经 23 起点单变量 A/B，成文口径三臂主读数全负（建仓跳过按成交日收盘 −0.76、止损 T 日判 T+1 卖 −0.42、余仓不足一档清空 −0.44，叠加 −1.41），按用户处置指令「统一为回测更好的那个」将 §9.3 成文改从实现：走势行改「T 日收盘 < 成交日 MA60 放弃」、止损行改「执行时点现价跌破当日生效线即当日整仓清空」（`track_holdings_daily.py` 提示同步）、§9.3.2 第 4 步改「余仓不足一手时清空」；BASE 与在册读数不动、不换纪元；三口径留研究开关（`--entry-below-ma60 skip_fill`／`--stop-basis`／`--residual-clear`）并显式入 `BASE` 防缺省漂移，参数同步测试加断言；`--until` 缺省由硬编码日期改为跑满状态文件；§9.3.1 流动性行与 §9.3.2 第 1 步 `review_pending` 补「回测不复现（成文差异）」声明；面板子集状态文件按 §12.94.6 口径重建。 |
| **v4.67** | 2026-08-24 | 删除「券商实际授信 190 万」全部残留，授信口径唯一为 0.666 × 当日净资产；改 §2 台账行为「账户快照」，退役 `credit_line_cny` 列（历史数据保留、规则不再读）；同步个人体系 §4；标注回测日志 §12.77/§12.116/§12.125 中以 180~190 万封顶为现实约束的判读失效（§12.125.1 追记）。 |
| **v4.66** | 2026-08-24 | 改融资授信为比例口径：授信 = 当日净资产 × 66.6%、不设金额上限，取消 190 万台账绝对额上限（§10.2、§2 台账行、个人体系 §4 同步；回测日志 §12.125）；回测 `BASE --credit-ratio 0.6→0.666`，实盘与回测同口径，撤销 v4.44 起的成文差异；同步测试更新；重登新基准在册读数（§12.1）：滚5 64.41/54.91/29.69、滚5 回撤 48.1、年化中位 50.91；重算 08-21 信号：周一卖牧原 3,500／买格力 3,300（一档）＋爱玛 ≈1,400 股（部分档，条件 ≥MA60）。 |
| **v4.65** | 2026-08-24 | 删除工作流全篇解释说明性内容（判例、旧口径对照、版本/裁定出处标注、实测快照与机制动机叙述：§0/§3/§5.3-5.7/§6.3-6.8/§7.1/§7.5/§8.2-8.3/§9.1/§9.3.1/§9.3.1.1-9.3.1.2/§9.3.5/§10.2/§11.2/§11.4/§12.1/§15）；删除 §6.5.1 保险估值方法探索段；压缩 §12.1 历代纪元读数段为现行三线＋现行基准在册读数＋回测日志指针；不改任何阈值、命令或执行语义；73.2KB→59.5KB。 |
| **v4.64** | 2026-08-23 | 新增单票机械上限 60%（回测日志 §12.123）：持仓市值 ÷ 当日净资产 ≥ 60% 不再加仓、不足时本档只补到 60%（按手向下取整），只挡加仓、不触发卖出、上涨越限不回削，新建仓与换仓目标不受影响；落地 §9.3.1 表行、扫描器 `SEC93_POSITION_CAP = 0.60`（room 口径）、回测 `BASE --position-cap 0.6`、参数同步测试；改扫描器可用资金不足一档时与回测同义买到用尽 `min(一档, 可用)`（§9.3.1.1 补写）；新增回测研究开关 `--candidate-log`；重登新纪元在册读数：滚5 中位／P25／最差 61.93／55.74／29.47、滚5 回撤 47.1、年化中位 48.50；重跑 08-21 信号，上限不触发、换仓结论不变。 |
| **v4.63** | 2026-08-23 | 改回测滚动窗口为月末锚定（末日 = 自然月最后交易日、首日 = 60 个月前同月末，年化按实际日历年数，回撤与 Sharpe 用窗口内逐日净值；`rolling_windows` 替代 `rolling_calmar`）；新增 `summarize` 滚 5 年 CAGR P25／最差、滚 3／5 年 Sharpe 中位、滚 3 年 P25；改扫描器对照表为两层：决策读数四项（主读数 = 滚 5 年 CAGR 中位配对 Δ、坏情形 = 滚 5 年 CAGR P25 配对 Δ、闸门 = 滚 5 年回撤中位变深 >3pp、否决 = 负收益窗口占比由 0 转正且过半起点），辅助读数只描述不排序；改写 §12.1 第 2 款；重登在册读数：滚5 中位／P25／最差 63.93／54.33／34.01、回撤 48.2（回测日志 §12.121）；重读 v4.59／v4.61／v4.62 A/B（§12.122），处置均不翻案。 |
| **v4.62** | 2026-08-23 | 处置 OI-090：季报行「当期」= 年报最新比率 × `归母净利 TTM ÷ 年报归母净利`，λ 与中位仍取年报、守卫坡道按 TTM 当期重算；新增谷底对称守卫 `v = clip((十年中位/当期 − 1.3)/0.6, 0, 1)`，峰谷同按五年中位归一化；处置 OI-089：增速腿 `× d`，`d = min(1, NOPAT_最新/上年) × min(1, TTM 因子)`；处置 OI-091：新增薄权益守卫，净负债 ≥ 50% 企业价值判无法估值，否决企业口径 P/V；改 `scripts/pv_ratio.py` 为 P/V 唯一实现；带文件新增 `trough_weight`／`ttm_factor`／`growth_damp`，逐日状态新增 `ev_ps`／`pv_equity`；落地三线重解 0.9343／2.4671／0.1437（§12.120）；重跑 08-21 信号，17 只换档、换仓结论不变。 |
| **v4.61** | 2026-08-23 | 处置 OI-088：改周期守卫硬阈值为坡道 `w = clip((当期比率/十年中位 − 1.3)/0.6, 0, 1)`（中心仍 K=1.6），`ratio0 = (1−w)×非周期锚 + w×五年中位`、增速腿 ×(1−w)；改增长态「近三期严格单调」为分级信任 `λ = 近两次变动上行次数/2`，非周期锚 = 三年中位 + λ×(当期 − 三年中位)；带文件新增 `peak_weight`／`growth_trust`；§6.7 命令加 `--roic-cond-detect graded --roic-peak-ramp 0.3`（亦为代码缺省）；逐票追溯 13 只（回测日志 §12.119），跳变来源归 OI-089／OI-090／OI-091；落地三线重解 0.9505／2.5263／0.1462；改百济神州／寒武纪由「无法估值」为有带。 |
| **v4.60** | 2026-08-23 | 落地三线 v4.59 股本口径纪元对齐解：买入 0.9434→0.9503、减持 2.5008→2.5238、换仓 0.1451→0.1462（§9.3.1、扫描器 `SEC93_*`、回测 `BASE`、参数同步测试同步）；重跑 08-21 信号，买入为空、换仓结论不变。 |
| **v4.59** | 2026-08-23 | 修 OI-086 每股锚股本口径（§6.5.1）：经营账面 `BPS_op = BPS − x − X_cum/股数`（x = 最新年报后外生权益/股）；年报间外生权益 `X_y = ΔE − (归母综合收益 − 分红)`，只计 |X_y| ≥ 5% 上年权益；比率窗口与十年守卫窗口按 `E − X_cum` 计；外生权益按面值进每股净现金，增发与回购注销同式；带文件新增 `bps_operating`／`external_equity_ps`／`external_equity_cum_ps`／`shares_est`／`bps_basis_date`／`equity_anchor_mode`；修 OI-087 送转股本基准：按本行与上一行 BPS 对照送转因子判定 `bps_basis_date`，三处送转窗口自该日起算，预告叠加改按 `bps_operating` 缩放；新增 `test_equity_anchor.py`；重建全市场带、§6.7 第 3-6 步与 08-21 信号（回测日志 §12.118）。 |
| **v4.58** | 2026-08-23 | 入池中国国贸 boundary→worth_attention L2（参考分 70.6，K-稳态现金分配型）；并入三大报表至面板（332→333 只）；重跑全市场建带与 §6.7 第 3-6 步；叠加 2026H1 快报（§6.4）。 |
| **v4.57** | 2026-08-23 | 改保险估值沿用银行股利折现（§6.5.2.3 第 4 条补写）；新增 §5.4 行业口径「保险」行：同银行择要，取系统重要性代表＋长期 franchise 最优者，判据为十年维度 ROE 中位与稳定性高于同业；入池中国平安 boundary→worth_attention L2（64，J-金融资本型），人寿／太保／新华留 boundary，池 205→206。 |
| **v4.56** | 2026-08-23 | 结 OI-085：保险照搬银行股利折现；新增 `scripts/divspread_names.py`（银行名称判定＋保险代码清单 601318/601319/601336/601601/601628），`rebuild_bank_bands.py`／`build_pool_model_bands.py`／`screen_daily_volume_price_signals.py`／`apply_model_bands_to_dossiers.py` 同源；改 §6.5.2.3 第 4 条与 §6.7 第 3 步为「银行与保险」，池/档案路径标签改「银行/保险·股利折现」；重跑 §6.7 第 3-6 步；换回测基准纪元（回测日志 §12.117），重登 `BASE` 在册读数：滚3／滚5／逐年中位 51.72／61.67／35.93、年化中位 47.06、最大回撤中位 62.8%。 |
| **v4.55** | 2026-08-23 | 入池圆通速递 boundary→worth_attention L3（62）与招商轮船 boundary→worth_attention L3（60），战术理由均为条件式；建档并维持 boundary_pending 6 家：浪潮信息、万辰集团、中国平安、新华保险、云南锗业、中船特气；登记 OI-085（保险无专用估值口径，待裁定）；并入 7 家三大报表至面板（325→332 只），重跑全市场建带与 §6.7 第 3-6 步，池 203→205、档案 295 份；写决策日志 8 行。 |
| **v4.54** | 2026-08-23 | 修 OI-083：改 `build_pool_model_bands.py` 成员为分层表 worth_attention L1-L3 ∪ 池 CSV，不再并入逐票档案；新增 `apply_model_bands_to_dossiers.py --archive-bands`（缺省 `roic_bands.csv`），不在生产带文件中的档案行取全市场模型带最新 ok 带、说明前缀「池外档案带」，只折送转、不做现金分红归一化；改 `build_company_dossier_readmes.py` 分层/参考分读分层表，无分层行的 boundary 档案显示「boundary_pending（不评分）」；补写 §6.5.2.3 一句；重跑 §6.7 第 4-6 步，池 203。 |
| **v4.53** | 2026-08-23 | 结 OI-084：新增 §9.3.1「L3 战术闸门」行，§9.3.2 第 1 步与合格集定义同步——`quality_tier = L3` 且分层表 `tactical_thesis` 为空或判「无／暂无／不可买」者不进合格集（持仓照常跟踪与减持）；新增扫描器 `SEC93_L3_TACTICAL_GATE`／`load_tactical_gate_codes()`，合格集硬排除并打印「[L3 战术闸门排除]」；增 `test_strategy_parameter_sync.py` 断言；回测不复现（登记于 §9.3.1 行与回测日志 §12.116）；补齐 16 家 L3 `tactical_thesis`（有·条件式 8 家：中芯国际、菲利华、康龙化成、东方电缆、华秦科技、工商银行、建设银行、农业银行；无 4 家：京东方A、钢研高纳、航材股份、中控技术）；写决策日志 13 行。 |
| **v4.52** | 2026-08-23 | 补建档案艾力斯／中远海控／特变电工／戈碧迦（boundary_pending，只落档案不入池；戈碧迦无法估值）；并入三大报表至面板（323→325 只），重跑全市场建带与 §6.7 第 3-6 步，池 203、档案 287 份；补分层表三行 `special_advantage_check`（写在 `flags`）；改中创智领 `tactical_thesis` 由「暂无」为条件式战术理由；登记 OI-084（`tactical_thesis` 成文「L3 买入前置」但无脚本读取，待裁定）；扩注 OI-083（`build_pool_model_bands.py` 成员＝池∪档案，L4/boundary 档案带进了生产带文件，与 §6.1 不符）。 |
| **v4.51** | 2026-08-23 | 入池德业股份 boundary→worth_attention L2（67）与中创智领 boundary→worth_attention L3（59）；判沃格光电 boundary→documented_not_attention L4（36）为首个 L4 行，分层表按 §5.7 新增 `l4_since` 列；维持艾力斯、中远海控、特变电工、戈碧迦 boundary_pending；并入两家三大报表至面板（321→323 只），重跑全市场建带与 §6.7 第 3-6 步，池 201→203；同步三类表/分层表/档案/决策日志（7 行）；登记 OI-083（README 渲染器分层与标签读自池，L4 档案显示「—」）。 |
| **v4.50** | 2026-08-23 | 复核 OI-036 中报驱动重筛第二批：`pending_h1=1` 且中报已披露的 924 家逐家判、08-09 预审的 481 家中报核对，0 家名单迁移；登记池内 5 家复核触发（中兴通讯、晶盛机电、金石资源、威胜信息、奕瑞科技），大豪科技列年报后优先复核候选，新增 20 条 boundary 复核触发写入三类表；同步牧原参考分 64.0；改 `verdicts.csv` 1,405 行 basis→2026H1、`pending_h1` 清零，另 20 行类别/参考分与三类表对齐；刷新三类表 1,402 行证据日；写决策日志 8 行；余 2,824 家待中报（截止 08-31，按 OI-036 排期）；§7.2 池级质量复核（队列 84 行）仍为独立待办。 |
| **v4.49** | 2026-08-23 | 结 OI-082：A 股不采纳 per_share 锚，生产 ROIC 引擎与三线不动，海外维持 v4.47 口径；移登记项入本表，工作流只改版本号。 |
| **v4.48** | 2026-08-23 | 新增研究开关 `build_historical_valuation_bands.py --roic-nopat-anchor per_share`（缺省 ratio_bps 逐位复现生产）与 `roic_inputs.RoicYear.treasury_shares`；实测 OI-082 A 股引擎 23 起点 A/B 不成立（回测日志 §12.115），建议 A 股不采纳、待确认；生产引擎与三线不动，海外维持 v4.47 per_share 锚。 |
| **v4.47** | 2026-08-23 | 落地 OI-082 修法于海外引擎：改每股 NOPAT 锚为各年 NOPAT ÷ 最新稀释股数，周期守卫改比 NOPAT/(母公司权益＋累计回购)，增长态近 3 年严格上升取最新、否则近 3 年中位，守卫 >1.6×10 年中位 → 5 年中位每股 NOPAT（`build_overseas_roic_bands.py`）；新增 `fetch_overseas_statements.py` 回购／分红现金流列（SEC `PaymentsForRepurchaseOfCommonStock` 等、港股「回购股份」）；重算海外清单并更新阅读版／README／§6.8；A 股引擎未改，差异成文于 §6.8；保留 OI-082 待办（A 股部分）。 |
| **v4.46** | 2026-08-23 | 登记 OI-082（ROIC 引擎每股 NOPAT 锚在回购/现金堆积公司上失真，修法待裁定）；写明 §6.8 回购与分红的处理；补齐持仓表 `cost_basis`：汾酒 123.321／九号 44.663／牧原 39.225／卫星 26.861；重跑跟踪器，涨幅 ≥125% 无。 |
| **v4.45** | 2026-08-23 | 改 `build_a_share_core_valuation_pool.py` 主表与海外附表的「合理价区间／空间」为「合理估值 V／`P/V`」；新增 `fetch_overseas_statements.py`（美股 SEC XBRL companyfacts、港股东财 HK F10 三表 → `data/interim/overseas_roic_years.csv`）与 `build_overseas_roic_bands.py`（复刻 ROIC 生产路径，r = 美债 10Y 4.74% + β×经营地 Damodaran ERP，输入 `data/reference/overseas_valuation_inputs.csv`）；写明 §6.8 口径与命令；改 `track_holdings_daily.py` 在 `cost_basis` 为空时显式标注并汇总单列（§13 第 3 条）；重跑 08-21 扫描与跟踪，结论不变。 |
| **v4.44** | 2026-08-22 | 新增 §9.3.1「涨幅减持」行：收盘较持仓均价涨幅 ≥125% 且收盘 < MA20 → 减一档；改换仓行为资金不足时先换出满足该条件的持仓（涨幅最大者），否则按「最贵且弱势」选，§9.3.2 第 4 步同步；定持仓均价 = 买入按股数加权、减持不变、除权按 §11.4 折算（§11.2 `cost_basis`）；回测 `BASE` 改 `--credit-cap 999999999999`、加 `--gain-sell 1.25 --gain-sell-mode gated`，§9.3.1.2 写明融资口径（授信 = 净资产×60% 不设上限）；新增扫描器 `SEC93_GAIN_SELL = 1.25`，`track_holdings_daily.py` ≥125% 写入 note 并汇总单列，`test_strategy_parameter_sync.py` 增断言；重登新基准在册读数（回测日志 §12.114）：54.18／63.18／37.85、年化中位 47.59。 |
| **v4.43** | 2026-08-22 | 扫描授信比例 ≤63% × 125% 涨幅减持逐比例对照（不封顶、真实资金顺序；纯读数，`BASE` 不改；回测日志 §12.113）；留 `--gain-sell 1.25` 与授信比例两项候选待裁定。 |
| **v4.42** | 2026-08-22 | 扫描不封顶下授信比例 r=0~120%（真实资金顺序；纯读数，`BASE` 不改；回测日志 §12.112）；新增 summary `强平次数`／`最低担保比例` 两列；定授信比例为风险偏好约束下的选择（先定可承受回撤／强平概率，再取可行区间内最高中位收益），Sharpe 只用于比较策略规则。 |
| **v4.41** | 2026-08-22 | 定稿融资资金顺序：换仓卖出款同样先还超额负债、负债回到净资产×60% 内才可买入；改引擎缺省为所有卖出款先还超额、超额期间换仓每天卖一档弱势持仓去还款，撤回 v4.40 第④条，旧口径 `cash`+`keep` 仍逐位复现 v4.39；登记融资 vs 不融资对照与旧融资读数高估量（回测日志 §12.111，§12.107 的不封顶读数作废）；留涨幅减持候选 1.25 待裁定；重登新基准在册读数：39.71／42.10／30.76、年化中位 33.67、滚3回撤 35.0%、最大回撤中位 52.0%；工作流只改版本号与 §12.1 指针。 |
| **v4.40** | 2026-08-22 | 结 OI-081，落地融资资金顺序：授信额度每日按净资产计算，授信 = min(净资产×60%, 上限)、去掉已用负债托底，常规卖出后先还超额（`--credit-over-limit repay`），换仓与买入按现金＋剩余授信判（`--swap-trigger power`）；旧口径 `cash`+`keep` 逐位复现 v4.39；扫描涨幅减持 `--gain-sell G`（gated：收盘 ≥ 均价×(1+G) 且收<MA20 减一档／作换仓源）50~300%，得 125%~200% 平台，未采纳、候选 1.25 待裁定（回测日志 §12.109／§12.110）；重登新基准在册读数：38.05／42.54／34.95、年化中位 36.14、滚3回撤 35.2%、最大回撤中位 52.1%；工作流只改版本号与 §12.1 指针。 |
| **v4.39** | 2026-08-22 | 扫描融资授信上限 180 万／1,000 万／2,000 万／不封顶（`--credit-cap`；纯读数，`BASE` 不改；回测日志 §12.107）；新增研究开关 `--swap-trigger {cash,power}`（缺省 cash＝现行只判现金），登记 OI-081（换仓触发不计剩余授信，与成文 §9.3.2/§10.2 不符，待裁定）；新增 `--addon-max-gain 0.5`、`--gain-sell 1.0`、`--gain-sell-mode gated|ungated`（持仓均价买入加权、除权折算），不采纳「涨幅 ≥50% 不再加仓、≥100% 减仓／换仓」完整规则，留 G100 gated 作候选（§12.108）；开关缺省关，`BASE` 逐位复现。 |
| **v4.38** | 2026-08-21 | 归档 OI-040：v6b 历史关注池 ∩ 退市名册仅深赤湾A 1 只、影响噪声级；不改 v6b／`verdicts_pit_moat_v6.csv`，海印不写入，原余项（B 版回测／退市股三表／T3）不再登记；保留第二遍盲判 239 行与协议 §12、回测日志 §12.106 存档；工作流正文只改版本号（追平 v4.36/v4.37 标题版本号）。 |
| **v4.37** | 2026-08-21 | 核实 OI-040 退市股数据实况（名册 344、行情 344、逐季财务 339、逐日状态 297、三表 1；判定队列含 168 只退市股）；重判 207 公司-年＋前向复判 32 公司-年，落 `data/processed/pit_attention/judgment_log_delisted_rejudge.csv`（239 行），入选结论与主日志 0 差异；改写 OI-040 为待裁定（海印入选）＋余项（B 版回测、退市股三表、T3 触发线）（回测日志 §12.106、协议 §12）。 |
| **v4.36** | 2026-08-21 | 不采纳 OI-072 `ddm:peer`，股利折现维持（回测日志 §12.105）；退役 OI-055（数据边界不可修，§12.61 三档替代已成文）；新增 `backtest_valuation_strategy.py` 退市末日平仓：退市名册（344 只）内代码过末个交易日的持仓按最后成交价整仓清出（协议 §8），名册缺失告警；改脚本头尾「幸存者偏差不可测」陈述为实际覆盖；`BASE` 逐位不变；待处理 5→3（OI-040／OI-036／OI-024）。 |
| **v4.35** | 2026-08-21 | 归档 OI-073：新增研究开关 `--roic-growth-min-ratio`、`--roic-zero-anchor fcff`，连同 `--roic-peak-k`、`--roe-years` 扫描均不优于现行，K=1.6、5 年窗维持；归档 OI-071：新增 `--wacc-weights market`、`--rd-mode spread`，实测不采纳，账面权重＋历史债务成本维持；落地 OI-074：带文件新增 `valuation_quality_score`／`valuation_quality_notes`／`v_bear`／`v_bull`（§6.5.3，只读不进判定；Bear/Bull = g0×0.5/1.25、折现率 ±1pp、终值回报 ∓1pp、fade 7/13 年、g_T ∓0.5pp），重建生产 `roic_bands.csv`；新增 `rebuild_bank_bands.py` `ri:`／`ddm:` 模式，改登记 OI-072 为待裁定；研究开关缺省均逐位复现生产带（回测日志 §12.103/§12.104）；待处理 8→5。 |
| **v4.34** | 2026-08-21 | 撤回三线两位小数取整，改为 v4.32 封顶纪元对齐解 0.9434／2.5008／0.1451、保留四位小数（结 OI-079；回测日志 §12.102）；同步 §9.3.1 三行、§12.1（对齐解不取整）、`SEC93_BUY_LINE=0.9434`／`SEC93_SELL_LINE=2.5008`、`sweep_backtest_configs.py` `--width 0.0566 --sell-line 2.5008 --swap-margin 0.1451`、`align_buy_line.py` 去掉取整打印、参数同步测试；重登新基准在册读数：滚3/滚5/逐年中位 41.18/43.86/37.58、滚3回撤 35.6%；v4.33 读数作废。 |
| **v4.33** | 2026-08-21 | 改三线为两位小数：买入线 0.94／减持线 2.50／换仓改善 0.15（回测日志 §12.102）；同步 §9.3.1 三行、§12.1、`SEC93_BUY_LINE=0.94`／`SEC93_SELL_LINE=2.50`、`sweep_backtest_configs.py` `--width 0.06 --sell-line 2.50 --swap-margin 0.15`、`align_buy_line.py` 加印两位小数取值、参数同步测试。 |
| **v4.32** | 2026-08-21 | 结 OI-054：新增回测 `--ma-basis adjusted` 与 `--exright-stop adjust`（前复权均线、除权折算 `entry_stop`／`peak_price`）并设为缺省，`raw`／`frozen` 只用于复现旧读数；结 OI-042 建带侧：新增 `scripts/disclosure_dates.py`（可得日 = min(记录公告日, 法定截止日)）与 `build_historical_valuation_bands.py --notice-cap statutory`（缺省）；登记 OI-079（三线对齐解 0.9434/2.5008/0.1451 待裁定，生产三线暂不动）；结 OI-041：`incremental_roe` 的 `eps_old` 是否已按送转折算改由本地判据决定，新增列 `incremental_roe_basis`；结 OI-080：`track_holdings_daily.py` 当日 MA60 改按前复权序列计算；重建生产带与逐日状态为新基准纪元（回测日志 §12.101）；成文 §6.3 第 2 条、§6.5.2.3、§8.3、§9.3.5、§12.1。 |
| **v4.31** | 2026-08-21 | 归档 OI-069：新增研究开关 `--roic-iroic-mode {multiwindow, allpairs, allpairs_guarded, regression}`（缺省 `endpoint`＝生产）与 `--roic-tax-mode median`（新字段 `RoicYear.tax_rate_observed`），6 臂 23 起点 A/B 无一可采纳，生产口径不变（回测日志 §12.99/§12.100）；不修第 1/3 条，第 4 条并入 OI-071；写明 §6.5.1 `g0` 两腿取大与 `roic_g_source`；改 `build_company_dossier_readmes.py` 第八节 g0 归因按 `roic_g_source` 如实写腿；重算 `roic_daily_raw.csv` 至生产口径；待处理 12→11。 |
| **v4.30** | 2026-08-21 | 结 OI-078：改 `build_company_dossier_readmes.py` 恒渲染档案第八节「现价隐含了什么」首段（现价 ÷ 生产带中值 = P/V → §6.5.1 路径与假设 → V 与现价各自的归一化盈利倍数 → 固定句），无法估值／人工覆盖／不在池内各有固定句式，模型参数取自 `a_share_pool_model_bands_adopted.csv` 且与档案中值相差 ≤0.5% 才写入；清理 `implied_growth_years` 旧口径文本（`scripts/archive/cleanup_dossier_implied_growth_oi078.py`，62 份清空、116 份只留手写可证伪命题）；重渲染 README 273 份，`--check` 漂移 0；新增 §6.5.2.3 一句。 |
| **v4.29** | 2026-08-21 | 结 OI-077：并入 `fetch_ohlcv_history.py --as-of <证据日> --actions-only`（缺省 universe＝池∪持仓）为 §6.7 第 1 步第三份输入；并入 `build_company_dossier_readmes.py` 至 §6.7 第 4 步末尾；改 `apply_model_bands_to_dossiers.py` 只在带值变化时向 `notes` 留痕，清理历史无变化条目 3,732 条、重复 371 条，README 全部重渲染、`--check` 漂移 0；结 OI-076①：改格力档案第八节以生产带为中枢，股利折现降为交叉核对并登记为方法分歧，分歧归 OI-069；并入 OI-076② 至 OI-069；重写牧原第八节/跟踪字段；登记 OI-078（121 份档案第八节仍引用建档时「本档中枢」数字，待批量核查）。 |
| **v4.28** | 2026-08-21 | 改回测逐日状态的带生效日为「可得日前一市场交易日」，与生产当晚吸收同构：新增 `build_historical_valuation_bands.py --state-effective {prev_trading_day,notice}`，缺省 `prev_trading_day`（按 `INDEX_000001.csv` 日历取，停牌股在复牌首行起用；陈旧序列只认间隔 ≤ 4 个日历日，否则退回可得日），`notice` 仅用于复现旧产物；三线重解在取整噪声内，生产三线不动（回测日志 §12.97.1）；改写 §6.5.2.3 为两侧同构（例外只剩 §6.4 叠加行）；重建生产 `roic_daily_raw.csv`／`a_share_daily_states_adopted.csv`；记录 `data/raw/ohlcv/` 全库末行停在 2026-08-07。 |
| **v4.27** | 2026-08-20 | 改正式定期报告为扫描当晚吸收：§6.7 的 `--as-of` 一律取证据日（运行时北京当日历日）而非信号日；改队列以同一证据日重建；改扫描当晚发现新披露即当晚跑全链、当日信号直接用新带（§9.1 第 2 步、§6.7 头注、§7.5 新段）；收窄冻结为链跑完仍吸收不了的 4 类行；成文 §6.5.2 第二条例外（实盘对晚间披露报告的反应比回测口径早一个交易日，已接受的未测差异）；重跑 08-19 扫描（as-of 08-20 全链＋队列＋重扫），执行方案以重扫版为准。 |
| **v4.26** | 2026-08-20 | 改成交日收盘 < 当日 MA60 的新建仓为放弃该笔、资金顺位下一名（加仓不设锚不受影响），新锚恒为成交日 MA60，结束 MA20 退档锚；落地 §9.3.1 走势/止损两行、§9.3.2 第 5 步、§9.3.5、回测 `BASE`（`--entry-below-ma60 skip`，argparse 缺省仍 `ma20_stop`）、`track_holdings_daily.py` 与 `entry_stop_price` 注释；MA20 锚只剩 v4.26 前存量旧仓与 `--stop-ma 20` 研究口径；重登新基准在册读数：滚3/滚5/逐年中位 38.33/40.94/40.49、滚3回撤 38.1%（回测日志 §12.90）。 |
| **v4.25** | 2026-08-20 | 改止损口径为「生效止损线 = min(建仓日锚, 当日同周期均线)」（采纳 §12.88.2 STOPMIN）：锚及其记录/复位/除权调整规则不变（§9.3.5），均线跌到锚下时生效线跟随下移、上移不抬线，价格同时低于锚与当日均线仍首日整仓清空；落地 §9.3.1 止损行、§9.3.2 ⓪、§9.3.5、回测 `BASE`（`--stop-line min_entry_current`，argparse 缺省仍 `entry`）、`track_holdings_daily.py`（`fetch_raw_close` 扩窗至 130 天并返回当日 MA60；均线不可得退回按锚判读并注明；§11.2 五列不变、`entry_stop_price` 始终存锚）；重登新基准在册读数：滚3/滚5/逐年中位 38.30/41.00/40.49、滚3回撤 37.6%（§12.89 后记）。 |
| **v4.24** | 2026-08-19 | 结 OI-075：按 §12.1 合格面（下 17.960%/上 30.465%）重解三线——买入线 0.9407（`--width 0.0593`）、减持线 2.5236、换仓改善 0.1447，同步 §9.3.1、`SEC93_*`、回测 `BASE`，参数同步测试过；复跑基准 23 起点（回测日志 §12.88.1），重登在册读数：滚3/滚5/逐年中位 38.91/40.89/43.40、滚3回撤 38.8%，此后引用历史配对差须注明纪元；新增研究开关 `--stop-line min_entry_current`（生效止损线 = min(建仓日冻结线, 当日同周期均线)，缺省 `entry`），生产参数不动、采纳待裁定（§12.88.2）。 |
| **v4.23** | 2026-08-19 | 修 `intrinsic_value.py` 留存率时序（修正四）：显式期第 t 年派息改乘本年留存率 `b_t`，利润路径、终值、护栏与拒绝集逐位不变，`b_0` 仍作 g_1 可行性约束但不进现金流；新增「清洁盈余重建」自检；重跑 §6.7 全链（as-of 08-19），档位变化 13 只，README 重渲染 273 份；改写 §6.5.1 为四条成文路径 growth／zero_growth／equity_fallback／bank_divspread（`roic_path` 列逐行自证），定名 r=10% 为统一要求回报率（hurdle rate）、±10% 为执行分档带宽；核对 P0 其余四项无缺陷；登记 OI-069／070／071／072／073／074／075（OI-075 为修正四纪元三线重解与基准复跑，须裁定）。 |
| **v4.22** | 2026-08-19 | 结 OI-068：收紧 §6.5.2.4 手工带为「主体不可比」唯一情形（宏桥为唯一手工带），模型无法计算（亏损/护栏拒绝/零增长价值≤0/带过旧）一律判「无法估值」——可见、带显示 —、无 `P/V`、不进 §9.3，模型重新可算后自动回归；撤出京东方A/中芯国际覆盖并判无法估值，清除 13 只档案手工带；改校验器对无法估值行不报 blocking；废止旧 §6.5.5.2「不得判无法估值」条款；新增 §5.3 新股入池节拍：新股判定挂在每个季度报告法定截止日（4-30/8-31/10-31/次年4-30）后的全量更新，队列 `new_or_unreviewed_security` 为载体，`boundary_pending` 在下一个季度截止日批次强制重判；改登记簿待处理项为三级标题。 |
| **v4.21** | 2026-08-19 | 结 OI-033，落地 L4／`documented_not_attention`：§3 新增术语，§5.5 三条迁移通道，§5.7 L4 档判据（资本复制测试不通过且须点名缺失载体+可指事实+同业锚对照；`l4_since` 满一年停止复核），§6.1 点名建档范围（L4 给参考分与估值区间，不入池、无 P/V、不进交易层）；改写 OI-037 为回测实验：新增 `--corr-conflict {skip,swap_space,swap_strength}` 与 `--corr-strength-days`，生产维持 skip、采纳待裁定（回测日志 §12.87）；结案移出 12 项（OI-067/066/063/060/056/052+039/050+046/047/034/034-A/033/029），登记 OI-068。 |
| **v4.20** | 2026-08-19 | 结 OI-052+OI-039：统一分红口径为「带跟随交易所除权调整」——回测 `build_historical_valuation_bands.py` 逐日展开按除权参考价公式逐事件复合 `(v−现金)÷(1+送转)`、锚在带公告日，新增 `cash_adjustment` 列，银行不参与减项；生产 `apply_forecast_band_overlay.py` 末段新增除权归一化（`exright_note` 列自证）；结 OI-066：新增订正层 `data/reference/financials_corrections.csv`＋`scripts/financials_corrections.py`；结 OI-067：`track_holdings_daily.py` 价格改按 `--as-of` 取不复权日线收盘，减持线改引用扫描器常量并纳入参数同步测试；登记 OI-068 并缓解（扫描器加 `2025-01-01` 陈旧带闸）；重解三线 0.9528／2.5690／0.1466，同步 §9.3.1、`SEC93_*`、回测 `BASE`；重跑 §6.7 全链。 |
| **v4.19** | 2026-08-19 | 统一压实子级编号，顶层 §0-§15 不动。**新旧对照**：`5.4.7 状态迁移→5.5（升为二级）`；`旧5.5 复核队列条件→5.6`；`6.3 第 5 条（预告快报叠加）→新设 6.4`；`6.5.0 策略标签→并入 6.5 导语（无号）`；`6.5.7→6.5.2`（子节 `6.5.7.1/2/3/4→6.5.2.1/2/3/4`）；`7.5.5→7.5.1`；`7.5.6→7.5.2`；`8.6→8.4`；`9.7→9.3`（`9.7.0 输入边界→并入 9.3 导语`，`9.7.1→9.3.1`、`9.7.1.1/9.7.1.2→9.3.1.1/9.3.1.2`、`9.7.2~9.7.5→9.3.2~9.3.5`）。**注意两处编号复用**：新 `6.4`／`9.3` 与 v4.07 精简前的旧 `6.4`／`9.3`（已删条款）含义不同；新 `6.5.2.2`（模型计算）与旧 `6.5.2.2`（预告快报入锚，现 §6.4）含义不同——**2026-08-19 以前的历史记录一律按旧编号解读，不回改**。同步 `SEC97_*`→`SEC93_*` 与 39 个脚本/文档/笔记本引用（含 `§6.7.8/§6.7.9→§7.1`、`§6.2.1.6→§6.2`、`§15.2→§13`、`§11.8→§10.1`）。 |
| **v4.18** | 2026-08-19 | 结 OI-063：删除 `screen_daily_volume_price_signals.py` 全部已退役展示机制（§8.5 放量条件、§8.7.x 形态、§8.9 watch_only、§8.11 强势跟踪、§8.12 市场状态、§8.13 入场阶段、§9.6 深度低估、优先级 S/A/C、信号分级），只保留取数、MA20/MA60、20日均额、`P/V`、§6.2 自动定档、§7.5 冻结、§9.7 机械层、§8.6 缺口回溯；删除 `build_a_share_core_valuation_pool.py` §6.2.1 三态矩阵与 `_evidence_retrieved` 死代码；删除 `backtest_signal_gate.py`／`backtest_8711_threshold.py`，结 OI-029；改 §10.1 第 3 条流动性门槛（20日均额 ≥5,000 万）为合格集硬执行；`daily_buy_candidates.csv` 70 列减至 36 列。 |
| **v4.17** | 2026-08-19 | 修复 §7.5 复核冻结未作用于 §9.7 买入计划：`section97_entry_plan` 新增 `blocked` 参数，在合格集构造处硬排除并打印 `[冻结排除·review_pending]` 明细（含只数落地校验，§15.2 第 2 条）；冻结 6 只当日正式中报公司待盘后 §6.7 全量重建；§9.7.1 参数逐位不变。 |
| **v4.16** | 2026-08-18 | 通过全链一致性审计（参数同步／脚本／§ 引用／data 路径／对账不变量全过）；修 OI-065：空买入计划落盘只含表头，`daily_entry_plan.csv` 新增 `trade_date` 列；完成 OI-066 ①，订正层设计写回登记项；移出登记册十项 OI-023/025/043/044/045/048/049/057/058/059；撤销 §12.60 质量股免换仓（回测日志 §12.86.2），修 `build_quality_quota.py` 区间当快照缺陷；完成关键时点验证（§12.86.1）；修回测 `--out-dir` 自建目录、`workflow_decision_log.py --help` 崩溃、个人体系 §9.3 的 §11.4→§11.3 指针；不改任何交易参数。 |
| **v4.15** | 2026-08-18 | 清空 `review_pending` 41→0；新增 `data/processed/manual_band_overrides.csv` 与 `apply_forecast_band_overlay.py` 覆盖分支，§6.5.7.4 手工带同时写入生产带文件与逐票档案；改 `bank_divspread` 路径对利润类证据只推进证据日、不动带值（§7.4）；改 `zero_growth` 路径 `ev_ps` 由 `IV + net_debt_ps` 反解；改叠加基线只接受「同年更早一期」或「上年年报」；新增 `scripts/audit_financial_panel_consistency.py` 写入 §6.7 第 5.5 步，登记 OI-066（宏桥控股严重级异常）；重建三只手工带（§6.3 第 4 条）：京东方A 5.76-7.04、中芯国际 63.74-77.90、宏桥控股 27.15-33.18；不改任何交易参数。 |
| **v4.14** | 2026-08-18 | 结 OI-064：新增 `scripts/apply_forecast_band_overlay.py`，落在 §6.7 第 4 步之后，§6.3 第 5 条自此有执行点；叠加通道：预告归母净利经留存收益改变每股净资产，`nopat_ps = ratio0 × bps`（growth／zero_growth）与 `eps0 = roe0 × bps`（equity_fallback）等比缩放，归一化锚 `ratio0`／`roe0` 不动，净负债不调整、偏差写进 `overlay_note`，幂等；跳过四类：银行股利折现、`zero_growth` 输入不全、股本多期倒推不一致、手工带；登记 §6.5.7.1 口径背离（叠加只写生产带，回测输入三个文件不碰）；改写 §7.5／§7.5.5，预告触发的复核不再产生人工 express 义务。 |
| **v4.13** | 2026-08-18 | 改 §0 与 §9.7.0「账户级风险与行为红线只认个人投资体系」为「账户级风险只认个人投资体系 §4 的两条外生硬约束（券商授信额度、130% 强平线）」，随个人体系 v1.27 同步；不改任何交易参数。 |
| **v4.12** | 2026-08-18 | 改 §10.2 账户级防护为两条外生硬约束——券商授信额度与 130% 强平线（随个人体系 v1.26 删除账户回撤梯与担保比例预警）；`--funds` 公式、超授信禁买与「卖出款先偿还超额负债」三条不变；每日扫描不再输出自峰值回撤与担保比例预警行；不改任何交易参数。 |
| **v4.11** | 2026-08-17 | 改收益判优口径：预计持有期须预先指定，缺省用 5 年 CAGR 作主口径，标准 23 起点报告配对差、正号数与 P10，滚动 3 年只作较短状态诊断，逐年中位不再作复利指标，另补互不重叠时期；改扫描器按滚动 5 年排序；不采纳 `MA10/30`（阶段依赖）与普通/soft conditional3 参数优化（回测日志 §12.84）；生产参数不变；测试沿用 300 万本金、60% 授信比例、180 万初始授信上限、130% 强平线、3.5% 年息。 |
| **v4.10** | 2026-08-17 | 修复最新财报进不到估值带的断链：改 `fetch_a_share_quarterly_financials.py` 按法定披露截止日判断，窗口未关的报告期强制重取（一季报 4-30／半年报 8-31／三季报 10-31／年报次年 4-30）；补齐 §7.1／§6.7／§9.1 的 `fetch_a_share_report_disclosures.py`／`fetch_a_share_earnings_forecasts.py` 取数落点与 `data/raw/financials/` 刷新；改 `apply_valuation_band_cards.py` `_load_model_bands()` 指向 `data/processed/a_share_pool_model_bands_adopted.csv`，读不到即硬失败；重跑三个证据源、全量重建带、池物化。 |
| **v4.09** | 2026-08-17 | 补记各估值臂（conditional3／median／旧 DCF）三条线与基准合格面 17.960%/30.465%（§12.83）；重扫六个共适配旋钮：维持 `--addon-trend ma-only`、`--sell-trend-ma 20`、`--trend-ma 20 60`，`--corr-window 252` 变差，查明 `--scan-depth` 为惰性参数，`--trend-ma 10 30` 与 `--no-swap-require-weak` 为分裂判定，生产参数不改；重写七个技能文件为纯路由；修正两个生产脚本用户可见章节标签；登记 OI-063。 |
| **v4.08** | 2026-08-17 | 复核 v4.07 纠偏与精简通过（区间消费缺陷复算、纠偏后读数独立重跑、对账不变量）；补登采纳配置的 2011-11 标准长跑与风险实况（§12.82）；补回 `portfolio_account_snapshot.csv`（含 `credit_line_cny` 列）至 §2 固定产物表并在 §10.2 指明；改 §10.2 的 `--funds` 指向由 §8.1 为 §8.2；登记遗留于 §12.82（技能文件旧编号指针、各估值臂三线数值未记录、未重扫的共适配旋钮）。 |
| **v4.07** | 2026-08-17 | 改相关性上限 0.75 为 0.70，一档维持 5.0%；同步 §9.7.1、日扫 `SEC97_MAX_CORR` 与回测 `BASE --max-corr`，新增参数同步回归测试；精简工作流 2,003 行→663 行，只保留当前阶段路由、质量规则、估值生产链、披露触发、每日执行、唯一参数表、持仓处理和规则验证，历史参数、实验读数、判例、已否决方案与版本叙事移出正文；删除个人投资手册正文买卖阈值副本，只引用工作流 §9.7。 |
| **v4.06** | 2026-08-17 | 改一档 N × 2.0% 为 N × 5.0%（依据 §12.78）；同步 §9.7.1「单次买入」行与 §9.7.1.1 算例、扫描器 `SEC97_TRANCHE_PCT = 0.05`、回测 `BASE --x 5.0`；改个人体系 §2.1/§3.2 与 §18 卡片为引用。 |
| **v4.05** | 2026-08-17 | 改回测基础设置为融资口径：本金 300 万、授信 = min(净资产×60%, 180 万)、强平线 130%、年利率 3.5%（`--capital/--credit-ratio/--credit-cap/--maintenance-ratio/--margin-rate` 四参缺一不可；依据 §12.77）；改 §9.7/§10.2「回测无杠杆」表述为「已含融资与强平，但不模拟授信调整、券商加保、资金增减与行为反应」。 |
| **v4.04** | 2026-08-17 | 改估值口径为「分型锚＋增长腿」`--roic-nopat-source conditional3 --roic-growth hybrid --roic-cycle-guard peak`（增长态采信当期 NOPAT 比率、否则三年中位，周期顶只吃中位；`g` 取资本口径与利润增速两腿较大者；依据 §12.74/§12.75）；退役单票仓位上限；重解三线为 0.9493／2.5548／0.1461；留痕 R5/R7（`dec_r5r7_capoff_20260817`），个人体系升 v1.23。 |
| **v4.03** | 2026-08-17 | 退役融资棘轮与 100-160%N 仓位包络：删除 §10.2 两行与 §10.2.1 整节，不保留「已退役」标记；改账户级防护层为 §13.1 账户回撤梯、§4.1 担保比例预警、券商授信额度、130% 平仓线，总仓位不再有内生上限；同步个人体系至 v1.22（§4.1 改为专述担保比例预警、§13.2 融资棘轮整节删除并重编号）；保留回测 `--margin-ratchet` 为纯研究开关（复现 §12.70）。 |
| **v4.02** | 2026-08-17 | 换代交易参数 P75B98（依据 §12.72）：相关性 0.85→0.75、买入线 1.0059→0.98（收严至合格面 19.09%）、一档 1.5%→2.0%、单票上限 20%→25%、止损参照线建仓日 MA20→MA60（旧持仓不重设）；同步个人体系 §10 单票机械线 25%，30% 绝对红线不动。 |
| **v4.01** | 2026-08-17 | 换回测宇宙为 v6b 并与关注池归一（依据 §12.71）：宇宙 = §5.4 全口径逐股逐年判定（`verdicts_pit_moat_v6.csv` + `build_moat_panel.py` 可复现装配），三线对齐解 1.0059/2.8967/0.1508；定对账不变量：关注池成员全在面板现役集内，面板多出的只有全银行子册；池 181→201（15 只候选复核全入池＋银行择五）；修 `apply_model_bands_to_dossiers.py` 缺省带文件指向 v2.72 陈旧物化的污染缺陷。 |
| **v4.00** | 2026-08-16 | 立 V1FINAL（ROIC 估值＋重适配交易规则）为唯一基准（回测日志 §12.66~§12.70）：改估值口径为 ROIC/FCFF 真口径（§6.5.7.3，输入来自东财 F10 三大报表，新增 `data/raw/financials_statements/`＋`fetch_a_share_financial_statements.py`；银行仍走股利折现）；重适配交易规则：单票上限 15%→20%、止损 MA60→MA20、一档 1.0%→1.5%、恢复减持线，三线按 §12.30 重解为 0.9963／2.8091／0.1494；重登在册读数：滚3/滚5/逐年中位 27.62%／28.04%／22.91%、滚3回撤 28.5%；重写 §9.7.5 为 MA20；同步 `sweep_backtest_configs.py` `BASE`、`screen_daily_volume_price_signals.py` `SEC97_*`；重建 `a_share_daily_states_adopted.csv` 为 ROIC 口径。 |
| **v3.04** | 2026-08-15 | 修回测宇宙入选前视（新增回测日志 §12.64）：新增 `fix_panel_entry_lookahead.py` 产出 `panel_moat_bank_v5.csv` 立为宇宙（丢 51 个档位行、211 只不变、前视归零），23 起点 A/B Δ 三条全 +0.00；新基准读数：滚 3/5 年 22.15%／23.59%、逐年中位 20.69%、全期年化 20.91%、滚 3 年回撤 23.19%，长跑 4,925 万／20.96%／回撤 41.9%；删 §9.2 残留的「P/V ≥ 1.10 减一档」、旧 MA20 止损行与 §12.2「关止损 −2.36pp」行 |
| **v3.03** | 2026-08-15 | 结 OI-058／OI-059：`screen_daily_volume_price_signals.py` 加 `load_holdings()` 落地单票上限与加仓放宽（在手仓走势只须 `MA20>MA60`，买入后市值÷N 超 15% 即跳过，读不到持仓显式告警）；新增滚动 10 年读数（中位 23.49%、P10 17.36%，不入扫描器 FIELDS）；§12.1 新增两条读数纪律（滚动口径只比较不当预期、全期年化中位≠长跑年化）；正文版本号补正为 v3.03（新增回测日志 §12.63） |
| **v3.02** | 2026-08-15 | 四条交易规则并入基准臂（回测日志 §12.62）：止损改建仓日 MA60（`--stop-ma 60`，收盘低于 MA60 时退回 MA20）；已有持仓加仓只须 `MA20 > MA60`（`--addon-trend ma-only`）；删除减持线整条路径（`--no-value-sell`）；换仓卖出源须 `收盘 < MA20`（`--swap-require-weak`，与加仓放宽必须成对启用）；读数改为全期年化 20.83%、滚 3 回撤 23.6%、换手 3.28 |
| **v3.01** | 2026-08-15 | 单票上限并入基准（回测日志 §12.61.16）：§9.7.1 规则表新增「单票 ≤ 净资产 15%，只挡加仓不强制减持」，§9.7.1.2 命令块与映射表加 `--position-cap 0.15`，`sweep_backtest_configs.py` 的 BASE 同步；个人体系结构上限拆为机械买入线 15% 与账户级绝对红线 30% 两行；新基准读数：滚 3/5 年 20.13%／20.50%、逐年中位 20.37%、全期 19.94%、滚 3 回撤 26.8%，长跑 4,061 万／16.89%／回撤 38.9% |
| **v3.00** | 2026-08-15 | 判优劣读数口径改为滚动 3 年／滚动 5 年／逐年三条（§12.1 新增首行，逐年只取完整自然年），落点为 `summarize` 相应列与 `sweep_backtest_configs.py` 的 DELTA_KEYS；「每只股票只跟自己比」重构实测否决（新增回测日志 §12.61，`--gate self-pct` 与 `build_metric_states.py` 保留，23 起点滚 3 Δ 全负）；分位表改为用起点之前历史预热；登记 OI-055／OI-056／OI-057（基准无单票上限，`--position-cap` 四层全过、待用户裁定） |
| **v2.99** | 2026-08-15 | `--n1` 七档（0~6）剂量实测全部不采纳，`n1=0` 维持，OI-050 改「已封死」并与 OI-046 汇合（新增回测日志 §12.59）；`rebuild_bank_bands.py` 缺省输入缺文件时由静默丢弃银行行改为硬失败，并支持指定带文件 |
| **v2.98** | 2026-08-15 | 买入线 1.63 改 1.00、减持线 1.10 改 2.50（新增回测日志 §12.57~§12.58，23 起点 Δ −1.22pp 已知情采纳）；废止基准臂单解对齐线的双轨制，生产线与基准臂同值；修池 MD 表头旧线与 `track_holdings_daily.py` 5 处硬编码 1.10，`test_failure_semantics.py` 改从 `tracker.SELL_LINE` 读 |
| **v2.97** | 2026-08-14 | 口径统一 V4 为唯一回测标准（新增回测日志 §12.55）：§9.7.1.2 命令块直接改为基准臂并补 `--universe-file`／`--daily-states` 两行；`align_buy_line.py` 改为支持两侧不同面板并同解三条线（1.5884/1.1044/0.1503 逐位复现）；扫描器 BASE 并入全部开关；正文 17 份过期阈值副本改指针、阈值只在 §9.7.1 出现一次，§9.7.1.1 算例更正为「500 万下一档 = 5 万元」；`panel_moat_bank_adopted.csv` 改名 `panel_moat_bank_v2.csv`；结 OI-053、落实 OI-045②；归档 `backtest_candidate_strategies.md` |
| **v2.96** | 2026-08-14 | 把 `exit_log.csv` 的退出/重入判定（77 条、按 `notice` 披露可得日切换在册状态）接回回测宇宙，立 V4（211 只、15,841 行）为基准（新增回测日志 §12.54）：收益 −0.07pp（10/23）、滚 3 年回撤 39.9% 改 34.3%、换手 −24% |
| **v2.95** | 2026-08-14 | 回测基准改用 V3 面板（新增回测日志 §12.53）：基准读数 23 起点年化中位 15.16%、滚 3 年回撤 39.9%、换手 6.93，长跑 3,730 万／16.29%／回撤 51.4%；实测确认成文参数 1.5853/1.10/0.15 可沿用，对齐规则只约束 A/B 对比臂 |
| **v2.94** | 2026-08-13~14 | 五轮估值校准与两个买入线方向实测全部否决（新增回测日志 §12.44~§12.50）：按护城河/行业定中轴、公平 P/V 上收紧买入线、`--buy-floor` 双边区间、方差归一与部分校准、居中判据改 60 日均线均不采纳；登记 OI-052（估值带做了送转折算、未做现金分红折算） |
| **v2.93** | 2026-08-14 | 结 OI-051：每日扫描两套实现合并（新增回测日志 §12.42）——生产入口新增 §9.7 机械执行层与 `--model-bands`/`--nav`/`--rf`/`--plan-out` 四参数，产出 `daily_entry_plan.csv`，输出加 `model_intrinsic_value`/`model_band_source`/`model_pv` 三列；删除 `scripts/experimental/daily_scan_adopted.py`，§8.2 命令、§8.3 产物表、§9.7.2 第 5 步同步；修三个新列落盘为空的缺陷并补落地校验；卖出仍走 §9.7.5 与 `track_holdings_daily.py` 人工 |
| **v2.92** | 2026-08-14 | 仓库瘦身与回测提速（新增回测日志 §12.41）：删可重建产物 10 GB 改 1.7 GB，8,733 个 summary 归并为入库的 `scan_summaries.csv`，清理入口固化为 `scripts/clean_derived_artifacts.py`；固化 §9.7.1.2 两个输入为 `a_share_daily_states_adopted.csv` 与 `panel_moat_bank_adopted.csv`；扫描 75 分钟改 6 分 24 秒（`--no-artifacts` 成扫描默认、相关性缓存改走 C 层、CSV 改 `csv.reader`，重跑 253/253 逐位一致）；新增 `scripts/sweep_backtest_configs.py`（BASE 固化＋Δ 对照表）；归档 5 个无引用脚本，归档表新增「归档日期」「可永久删除日」两列 |
| **v2.91** | 2026-08-14 | 两项定投端改动实测否决（新增回测日志 §12.40）：偏离 MA60 超阈停投（`--entry-mode both --dev-buy-max`）与卖出改定投式减仓（新增 `--stop-partial`/`--stop-tranche`）均不采纳；§9.7.1.2「不得再打开」清单由四个扩为六个；修 `--entry-mode` 偏离过滤后视（两侧同取信号日，§12.26.3 读数作废重测）；确认 λ=2.0 下 `--width` 1.63 与 1.585 可换用、生产不改线，对齐规则只约束 A/B 对比臂 |
| **v2.90** | 2026-08-13 | `roe0` 的 λ 由 1.5 改 2.0，按新口径重出全池估值带与当日操作清单；池 MD「合理价区间」由逐票档案带改为 §6.5.7.1 批量模型带（档案带并列保留，两带偏离 >50% 的 28 只标 ⚠ 建议进 §7 复核）；按 08-13 收盘全池扫描得 21 只从零建仓清单；遗留 `--width` 未同改与档位阈值脱节两处、于 v2.91 处置 |
| **v2.89** | 2026-08-13 | 采纳三项联动改动（新增回测日志 §12.39，23 起点 +9.88pp、23/23）：估值锚改 `onesided_max` 且 λ=1.5（`roe0 = 五年中位 + 1.5×max(0, 当期TTM − 五年中位)`，登记 OI-050）；银行改股利折现 `V = 近12月每股分红 ÷ (十年国债+2%)`；执行三参数改 `x1.0/corr0.85/swm0.15`；v2.86 判例改为「要求补做该层检验」而非永久结论；更正 `build_historical_valuation_bands.py --since` 缺省 2016-03-31 致变体带不同期（§12.36/§12.37 读数作废重跑）、§12.35 银行盈亏贡献实为 26.0% |
| **v2.88** | 2026-08-11 | 历史前视四项中前两项落地（新增 §12.25）：登记 OI-040——幸存者偏差经九只退市股核对为不可测数据缺口，与选样前视拆成两个术语；重建时点库 141 只估值带（非池内公司回落 `DEFAULT_TIER=L2`），三臂分解实测选样前视合计 −10.27pp；单票上限 `--position-cap` 五起点压力测试通过、§9.7.1「无单票上限」维持不改；回测脚本未给 `--universe-file` 时明确打印「本轮读数含选样前视」；§12.2 加限定框：表内绝对年化一律按上界读、配对差不受影响 |
| **v2.87** | 2026-08-11 | 查实 λ=1 逐位复现 S-B（原重算表自身算错）、λ=0 与 S-A 差异为噪声，S-B 判定不变（新增 §12.24.5，判否依据重述为剔 2021 后 −1.31pp）；修 `backtest_valuation_strategy.py::period_returns` 分期基数改取上一期末（逐年/逐月链乘精确等于全期总收益，§12.21.8 逐年表重出）；撤销「2021 收益随 λ 单调放大」「最长起点 λ=1 为负」两条表述；§12.1 新增读数纪律：逐年收益链乘须等于全期且年化 T 取回测口径、单起点只可读符号一致性 |
| **v2.86** | 2026-08-11 | `build_historical_valuation_bands.py` 新增 `--roe-lift λ` 做剂量-反应扫描（新增 §12.24）：响应曲线 W 形、剔除 2021 后 λ>0 六档全负，S-B 由候选降为「不采纳（存疑登记）」、不得再以「通过四层复核」引用；§12.1 新增读数纪律：候选全部起点覆盖同一段特殊行情时起点类检验不独立，须补做剔除该段的重算 |
| **v2.85** | 2026-08-11 | 登记 `S-A`（现行 normalized@0.90）与 `S-B`（候选 onesided_max@0.8336）为具名配置（§12.22）；融合（并集）与交集两种组合口径实测均不采纳（§12.23）；§12.1 新增纪律：两口径排序不同时验证须单独替换并对齐合格面；补齐候选审核数据（§12.21.8）；修 `ledger` 漏记全部「减一档」的缺陷——补记三条部分减持路径并把 `top1_weight`/`top3_weight` 写进逐日净值曲线（成交与年化逐位不变） |
| **v2.84** | 2026-08-11 | 口径同源核实查出止损静默失效并重做完整五层（§12.20 重写、§12.21 整体取代）：修 `entry_stop_price` 成交日停牌返回 0.0 被 falsy 短路致该仓永不受止损约束（修后 2002 起点 29.42%，加计数器）；修成交流水 `price` 列打印错误；§11.5 更正为 `entry_stop` 取成交日 MA20；止损规则不撤销、依据换为关掉后滚动胜率四档全 <50% 与 Calmar（五起点读数更正为 −2.36pp）；`onesided_max` 单独换锚被否，同步收买入线 0.834 对齐合格面后 +2.30pp 登记为候选不落地；`ttm` 更正为 −2.21pp；建带脚本新增 `roe0_normalized` 列 |
| **v2.83** | 2026-08-11 | 把回测一直在跑的建仓日 MA20 止损补进正文（新立 §9.7.5，`--strategy trend` 的 `trend_stop` 缺省 True）：§9.7.1 参数表与 §9.7.1.2 对照表补行、§9.7.2 第四步新增第 ⓪ 条唯一整仓路径、§11.2 持仓清单加 `entry_stop_price` 列且须持久化、§11.5 按信号日 MA20 回写、`track_holdings_daily.py` 加 `entry_stop_price`/`stop_hit` 两列；存量 25 只持仓按留空过渡；§12.2 基准标注更正为 `--exec-delay 0` 口径；建带脚本新增 `onesided_up/max/min` 三个 `--roe-source`，`onesided_max` 通过四层登记为候选、不落地待裁定（新增 §12.20/§12.21） |
| **v2.82** | 2026-08-11 | 正文按「只保留现行口径」重构（2727 行改 1721 行）：§12.5~§12.18 回测实验移出到新文件 `docs/Ashare_backtest_log.md`；通用十一类建带公式退役删除，§6.5.0~§6.5.3、§6.5.5.1、§6.5.6 删除，§6.6 重写为六步链，新增 §6.5.7.4 手工带例外口径；新增 §9.7.1.2「规则 ↔ 回测开关」对照表含完整命令；修 `--lot-size` 缺省 0（回测允许零股）并按完整命令重跑五起点，六项对照全负、现行规则集全部确认；作废 §12.16~§12.18 三处零股口径结论；§12.1 新增「命令逐字照抄 §9.7.1.2」「可执行性是读数前置条件」两条纪律 |
| **v2.81** | 2026-08-10 | 「整仓+闸门」与「现行+走势容差 0.5%」两个候选各走完五层复核，均不采纳（新增 §12.18）；更正 §12.17 排名口径为配对差中位（「现行配置排第 6/第 4」说法作废）；§12.1 新增读数纪律：第 1/2 层同属起点检验、第 3/4 层才独立，四层「前两正后两负」即判不采纳；§9.7 全部参数维持现状 |
| **v2.80** | 2026-08-10 | 有无相关性约束的两个最优正面对比（新增 §12.17）：§9.7.1 的 ≤0.7 维持，理由由分散度纪律升级为实测依据（收益 +0.62pp、回撤 −1.9pp）；闸门与换仓方式的交互方向以本表为准，§12.11/§12.13 机理段降为待验；「整仓+闸门」登记为下一候选、不改规则 |
| **v2.79** | 2026-08-10 | 全部既往结论带 `--max-corr 0.7` 重跑（新增 §12.16）：确认取消持仓上限、腾位豁免不采纳与 T+1 方向；换仓减一档与减持走势闸门两条降级为弱证据（规则不撤销、补检验列为待办）；走势容差 ±0.5% 由「已否定」改回「未定、方向为正」；§12.1 补强纪律：§9.7.1 参数表每一项回测命令必须显式给出、不得依赖缺省；池 MD「估值时间/估值事件」两列改取 `pool_model_bands.csv` 的报告期与生效日 |
| **v2.78** | 2026-08-10 | 簇内升级（相关性由「排除谁」改「换谁」）实测不采纳（新增 §12.15，`--cluster-swap` 改造支持 `--swap-partial` 后带 `--max-corr 0.7` 重测中位 −1.22pp）；确认相关性过滤器本身正向（+0.43pp），§9.7.1 的 ≤0.7 维持；§12.14 补注并下调强度（复验中位 −1.12pp、符号措辞作废）；§12.1 新增读数纪律：跑对照前先确认默认值就是要测的配置；按 CSV 重渲染 264 份逐票档案 README |
| **v2.77** | 2026-08-10 | 口径同源核实修两个生产侧缺陷：`apply_model_bands_to_dossiers.py` 取带改 `(available_at, report_date)` 双键（年报与一季报同日披露时选错报告期，59 只）；生产侧补送转折算（17 只，兴齐眼药 `P/V` 修正为 2.41）；明确 `--md-only` 不写池 CSV、带改动后必须跑 §6.7 全量命令；§9.7.3 改为比例冷却——令 `x = 一手金额 ÷ 一档`，成交一手后跳过 `round(x)−1` 次合格机会、买卖共用计数器（脚本新增 `--lot-ratio-cooldown`），减持侧同规则，结 OI-040 |
| **v2.76** | 2026-08-10 | `roe0` 归一化首次实测：同参数只改 `--roe-source` 的全历史面板 A/B，五起点收益全负（中位 −1.42pp）、回撤全部改善，判定维持 `normalized`、当前配置下不采纳 `ttm`（新增 §12.14，登记 OI-039／OI-040）；记 `P/V` 读法为横截面便宜度排名、不是价格目标 |
| **v2.75** | 2026-08-10 | 采纳减持走势闸门 `收盘 < MA20`（新增 §12.13，五层复核全正：五起点 +2.39pp、滚动胜率随窗口拉长而变强、长跑 29.31%），并更正 §12.11 的相反结论（旧结论产生于整仓换仓基准）；不采纳「贵+连破年线/半年线清仓」（样本过薄）与换仓「腾位后豁免相关性」（新基准下失效） |
| **v2.74** | 2026-08-10 | 换仓由整仓卖出改为减一档——采纳（新增 §12.12，五层复核通过：五起点 +0.83pp、滚动胜率随窗口拉长而变强、长跑 27.07%）；脚本新增 `--liquidate-ma`/`--liquidate-days`，「贵+连破年线一键清仓」实测 24 年仅触发 10 次、不采纳且不建议改阈值重试；§12.2 旧记「减一档 −6.24pp」当前配置下不复现、如实标注 |
| **v2.73** | 2026-08-10 | 卖出端右侧化（`P/V ≥ 减持线` 时先等空头排列再按档减）实测不采纳（新增 §12.11，脚本新增 `--sell-trend-ma`）：五种均线组合五起点全负、具体闸门排序不可复现（ρ = −0.60），仅回撤侧一致改善记录在案 |
| **v2.72** | 2026-08-10 | 带的来源由逐票手工档案改为内在价值模型（新增 §6.5.7.3 与 `scripts/apply_model_bands_to_dossiers.py`，建带链四步改六步）：`band = IV × [0.90, 1.10]`，生产 `P/V` 与回测逐位一致；263/273 换用模型带、10 只保留手工带；硬规则 `bespoke` 必须保持 `true`（否则通用路径覆盖模型带）；逐票档案只覆盖带相关六列、研究结论原样保留 |
| **v2.71** | 2026-08-10 | 按 §12.1 秩相关纪律回头重判当日全部结论（新增 §12.10）：持仓上限取消与 T+1 尾盘执行 ρ=+1.00 通过；压 `g0` 有害确认；抬 `g0` ρ=+0.03 判噪声；`n1` 系列（fade 路径）降为「未定」——维持 `n1=0`、理由改为未获可复现证据支持变更，§12.9.5「带的水平决定成败」降为待验假说 |
| **v2.70** | 2026-08-10 | `g0` 乘数步长 0.05 精细扫描判定为随机波动（新增 §12.9.12：R²=0.042、日历年秩相关 ρ=+0.10），§12.9.10 的不采纳维持、理由更换为「效应在独立检验间不可复现」；§12.1 新增「秩相关复核」读数纪律：判据是换一套互不重叠的检验后排序是否稳定 |
| **v2.69** | 2026-08-10 | `g0×1.25` 五层扩样本复核未通过、不采纳（新增 §12.9.10/§12.9.11：10 年滚动档胜率 33.0% 翻负、紧邻 1.5 档掉到基准以下，判孤立尖刺非单峰顶点）；§12.9 全轮六个变体全部不采纳，基准模型参数维持原样 |
| **v2.68** | 2026-08-10 | 解掉 `g0=trailing` 的覆盖与截断两处混淆后重测（新增 §12.9.8/§12.9.9，脚本新增 `--g0-source trailing_fb`）：全修完五起点 −0.55pp、日历年 −1.01pp，不采纳——信息在「增长是否由内生资本回报支撑」而非「增长」本身；并发现 `g0_cap` 在 0.50 以上被 `max_retention=1.0` 先咬住、是死参数 |
| **v2.67** | 2026-08-10 | 测掉 §12.9.6 留下的两个反向偏差（新增 §12.9.7，脚本新增 `--g0-shrink`）：响应曲线 0.6~1.25 单调递增、压 `g0` 五起点全负，确立「带里携带信息的是 ROE 通道而非整体水平」、不得据 `incremental_roe` 下调 `g0`；`g0=trailing` 不采纳（截断率 41.1%、覆盖掉 20%，须先解再测） |
| **v2.66** | 2026-08-10 | 补测第三种 fade 路径 `n1=3/n=5` 判为平局（新增 §12.9.5：带倍数 0.998x，核心结论「决定成败的是带的水平即等效买入线、不是 fade 的形状」）；重算 169 份 PEG 档案（新增 §12.9.6）：档案带对基准带 = 锚因子 1.06x × 倍数因子 1.30x，1.42x 系增长溢价而非均匀偏高 |
| **v2.65** | 2026-08-10 | 第一次检验估值带模型本身（新增 §12.9）：查出回测读 `intrinsic_value.py` 机械模型带、实盘出单用 273 份手工档案带，两套中位差 1.24x，登记为当前最大未验证风险；脚本加 `--n1`，`n1=3` 实测 −1.71pp 更差、不采纳（带乘 k 等价于把买入线抬到 0.90k） |
| **v2.64** | 2026-08-10 | §11.3 除权除息调整口径成文，结 OI-038：现金红利 `band − D`、送转 `band ÷ (1+k/10)`、并存 `(band − D) ÷ (1+k/10)`，差异化分派按价格口径；送转不需持久化（建带引擎股本取实时行情自动折半），`−D` 自除息日生效、到下一次基本面重建为止；检出机制职责定为确保除息日当日调过 |
| **v2.63** | 2026-08-10 | §9.1 六步时序正式改为「信号 T 日收盘、执行 T+1 尾盘（14:45-14:55）」；新增 §9.1.1 三条边界：不用容差替代、执行放尾盘不放开盘、执行日行情已变不重算合格集（例外仅停牌与 §7.4.1 触发）；废除盘中口径三类标注，§8.3 与 §9.2 模板同步；第五步表头写明执行日，一档金额为准、股数为估 |
| **v2.62** | 2026-08-10 | 三种成交时序回测（新增 §12.8.1，脚本新增 `--exec-delay`/`--exec-price`）：T+1 尾盘成交无系统性代价（24 年配对中位 +0.08pp）、T+1 开盘略差（中位 −1.68pp）；修首版记账错配为「移信号不移价格」，`--exec-delay 0` 精确复现基准 |
| **v2.61** | 2026-08-10 | 走势条件 ±0.5% 容差实测不采纳（新增 §12.7/§12.8，脚本新增 `--trend-tol` 缺省 0.0）：日历年 24 年中位 −1.26pp、长跑六项全输；实测当日新进合格标的 73.8% 次日仍合格、次日收盘中位 −0.04%，推迟一天执行代价在噪声内且时点歧义消失 |
| **v2.60** | 2026-08-10 | 修 §9.1 盘中口径单向检查缺陷：收盘后必须重算整个合格集、不得只复核盘中清单内临界项；执行纪律：盘中清单只能用于卖出与清单内标的的买入，「今日无合格标的持币」只有收盘重算后才能下；收盘更新行须写明新进与掉出两个方向、为空写「无」 |
| **v2.59** | 2026-08-10 | 买入线六档扫描加四层扩样本复核，维持 0.90（新增 §12.6：0.80~1.00 是平台、收紧到 0.70 五起点全负，放宽只压赔率不改胜率）；更正初测误用分布中位数之差（§12.1 明禁），同批数据配对差实为 −0.30pp |
| **v2.58** | 2026-08-10 | 取消持仓上限 10 只（§9.7.1/§9.7.2/§9.7.4/§12.2 同步，新增 §12.5 实测：五起点年化中位 22.50% 改 23.82%，上限管的是换手不是分散度）；分散度改由相关性 ≤0.7 单独承担；同日提出的退役融资棘轮与 100-160%N 仓位包络命中 R7、进 5 个交易日冷静期，留痕新增 §10.2.1，两行规则 2026-08-17 前照常执行 |
| **v2.57** | 2026-08-09 | 精简为纯执行规范（4,865 行改 1,853 行）：§12 全部 43 轮参数检验过程、§8 信号十二型与分级/优先级/入场段位原文、各章退役清单、§10 成交回写闸门章、§14.7 验算记录、§6.2.1 矩阵条文移出，归档于 `docs/archive/Ashare_workflow_v2.56_full.md`；新增 §3 核心概念术语表、§12 参数依据速查表、§13 改规则前自检清单 10 条；§5/§6/§7 章号保持不变 |
| **v2.56** | 2026-08-09 | 按回测口径重构，买卖决策权全部收归 §9.7、未经回测检验的机制不得产生买卖结论：退役割肉价与一切止损、信号十二型/分级/优先级/入场段位、建仓金额 50万/25万 二档（改为当日净资产 N × 0.5%），§6.2.1 矩阵降为估值档标签、§9.6 整节删除、§11 八条缩四条；§13.4 账户级防护层与 §5/§6/§7 原样保留（写入 §9.7.0）；§14.4 除权除息调整范围由持仓扩到全池；§9.1 五步改六步（新增机械执行清单四张表）、持仓清单五列改四列、`track_holdings_daily.py` 移除止损四列新增 `pv` 列；个人体系冲突项不代改，R4 适用边界写入 §9.7.6 |
| **v2.55** | 2026-08-09 | OI-036 第一阶段收口：有中报或中报预告的 1,929 家全部判完（worth 49／boundary 1,868／garbage 12，never_admit 首次使用 11 家、均满足三条财报直读硬条件；有线电视三家按 §5.4.3 结构性绝望行业成立，分界画在毛利率是否转负）；新股名录缺 24 家经并集修复全在队列且行情齐备 |
| **v2.54** | 2026-08-09 | OI-036 修分层口径错误：`classify()` 营收门槛由当期单季营收改为 2025 年报年度营收（年报缺失按当期折年并标注），A_核心由 778 家改 2,355 家；已判 889 家逐家判定不受影响，A/B 层有中报预告者判完 |
| **v2.53** | 2026-08-09 | OI-036 进度存档：A_核心有中报预告的 282 家判完、累计 456 家（对账核验通过，均带独立 `moat_note` 与 `rule`）；worth 39 家，对现有 261 池新增 7 家、移出 20 家；永不录用全库仍 0 家 |
| **v2.52** | 2026-08-09 | 防3.1 检验项修订为 ROE 口径：规模制造型看 ROE 多年稳定或上升、不看毛利率；可竞争买方型另加「能力须已跨客户迁移」一条；工业富联、立讯精密改判 worth(68)，牧原股份改判 worth(70)（同业对照决定性、锚12 政策性产能红线）；已判 214 家对 261 池新增 4 家、移出 5 家、维持 19 家 |
| **v2.51** | 2026-08-09 | §5 新立防3.1 客户集中分型：可竞争买方的集中仍是否决项；结构性单一买方＋供应商持准入资质不构成否决，强制检验买方是否压价（判据为毛利率与 ROE 多年稳定或上升，持续下滑退回 boundary、仅一期报告一律 boundary）；思维列控改判 worth_attention(72)、汤臣倍健降为 boundary(52)；OI-036 首阶段已出中报的 174 家判完（worth 14／boundary 159／garbage 1） |
| **v2.50** | 2026-08-09 | OI-036 立项全市场逐股重筛：新增 `scripts/build_full_market_screen_queue.py`，本阶段只判已出 2026 中报的 174 只；「永不录用」限财务造假／连续多年主业空心化／退市已成定局三类不可逆情形，「当前无护城河」记 `garbage` 并每年复查；取数改为名录与财报并集，补漏只接受沪深 0/3/6 段；全市场口径 5,677 只（A_核心 778／B_观察 1,486／C_排除 3,413） |
| **v2.49** | 2026-08-09 | OI-034 收口，定量策略立 §9.7 写入每日流程：候选池=今日 §5 名单、买入线 `P/V≤0.90`、走势 `收盘>MA20 且 MA20>MA60`、`P/V` 升序取前 10、相关性 ≤0.7 顺位补位（下扫 40 名）、单次买入总资产 0.5%、持仓上限 10、减持线 `P/V≥1.10` 按同速每日减 0.5%、换仓整仓卖出且需低 0.10 以上、无止损，每日五步；新增 §9.7.3 高价股口径：一档买不起一手改每次买一手、同标的间隔 ≥5 自然日（`--min-lot-cooldown 5`，按设计理由采纳）；§9.7.4 与现有持仓衔接、不做一次性重构 |
| **v2.48** | 2026-08-09 | OI-034 第 32 步（立 §12.9.41）：减持线响应曲线 1.00~1.10 为平台、1.10 以上断崖，1.05 不采纳（多重比较）；去掉第二条均线中位 −1.17pp 且 2018 熊市起点回撤反升；结构定死为快线必须 MA20、慢线 40~60、减持线 1.00~1.10，参数搜索到此为止 |
| **v2.47** | 2026-08-09 | OI-034 第 31 步（立 §12.9.40）：减持线 1.20 不成立（中位 −4.56pp）；均线网格加密显示绑定约束是快线 MA20，`收>MA20>MA40` 虽 +0.19pp 仍按多重比较不采纳；立判据「参数搜索应在机理清楚后停止，而不是在找到最高值后停止」 |
| **v2.46** | 2026-08-09 | OI-034 第 30 步（立 §12.9.39）：走势均线梯度扫描，`收>MA5>MA20` 与 `收>MA10>MA30` 均五起点全负、收益回撤双输，顶点落在现行 `MA20>MA60`（一开始就定的口径、非调参结果）；修静默失效：均线窗口由固定六档改为按 `--trend-ma` 等五个参数的并集按需收集（此前不存在的窗口致零交易且不报错） |
| **v2.45** | 2026-08-09 | OI-034 第 29 步（立 §12.9.38）：新增 `--rebuy {off,lump,gradual}`，割肉补丁三条买回口径五起点全负（−9.43~−14.79pp、幅度为系列最大）、不采纳；立判例：入场条件已内含趋势过滤，再加趋势止损是重复施加同一约束 |
| **v2.44** | 2026-08-09 | OI-034 第 28 步（立 §12.9.37）：新增 `--lot-size`（买入按手向下取整、买不足一手跳过，分批卖出同规则），500 万本金下 0.5% 档可执行、整手配对差 +0.15pp 在噪声内，档位 0.5% 维持（放大到 1%/2% 回撤显著恶化）；立读数纪律第一道筛子「±0.15pp 以内的年化差异是纯数值噪声」；明确面板股票库是消偏回测工具、前瞻方向用今日 §5 池，新增 `universe_live261.csv` |
| **v2.43** | 2026-08-09 | OI-034 第 27 步（立 §12.9.36）：补齐买卖节奏 2×2——「一笔建仓＋换仓」五起点全负（中位 −6.32pp），「一笔建仓＋减一档」结构上不可达；立结论：买入方式决定绑定约束（分批绑定现金、一笔绑定槽位），「买入渐进、卖出果断」是策略结构而非疏漏；换仓分支加 `换仓·减一档`/`换仓·整仓卖出` 计数 |
| **v2.42** | 2026-08-09 | OI-034 第 26 步（立 §12.9.35）：换仓改减一档 `--swap-partial` 五起点全负（中位 −6.24pp、回撤 +9.5pp，系列最大单项负效应）、不采纳，立判例「本策略买入侧偏好渐进、卖出侧偏好果断，两侧不对称且不可互换」；新增 `scripts/report_backtest_yearly.py`（按自然年拆收益、年内最大回撤、年末仓位并对照沪深300） |
| **v2.41** | 2026-08-09 | OI-034 第 25 步（立 §12.9.34）：簇内升级（相关性由排除过滤器改替换判据，新增 `--cluster-swap`/`--cluster-delta`/`--cluster-min-upside`）五起点三档中位全负、不采纳；记「检验方案前须先闭合资金流」与「涉相关性改动不可只看 2016 起点」两条纪律；副产物 δ=0.95 记为低换手可选口径 |
| **v2.40** | 2026-08-09 | OI-034 第 24 步（立 §12.9.33）：相关性阈值 0.85 与换仓目标豁免相关性均不成立（各 4/5 起点为负），换仓块先于相关性过滤的顺序不一致判为已知不修；记读数陷阱「两分布中位数之差 ≠ 配对差值中位数」；`--swap-bypass-corr` 保留为低换手场景选项 |
| **v2.39** | 2026-08-09 | OI-034 第 23 步（立 §12.9.32）：持仓上限 15/20 五起点符号翻转且响应非单调、判噪声——上限本就极少绑定，绑定约束是合格候选数量；§12.9.5「N=20 提高年化」限定为只对估值组＋261 池成立 |
| **v2.38** | 2026-08-09 | OI-034 第 22 步（立 §12.9.31）：面板剔除的 137 只加回重跑无增益（+0.02/−1.28pp 噪声内；持仓上限是硬约束、`P/V ≤ 0.90` 仍挡掉 94~96%），绑定条件确认为估值模型，OI-034-A 由开放问题降级为口径瑕疵；逐家逐年重判评估为不建议（精细度与无后视性在同一模型内对立）；剩余缺口记为第三类名单（当年够格、后来护城河丢失者） |
| **v2.37** | 2026-08-09 | OI-034 第 21 步（立 §12.9.30）：今日 §5 名单与历史逐年名单标准一致性核对——面板 2025 名单几乎是 261 池严格子集（收录率 44%）、剔除方向单调偏成长，历史库价值倾斜确认为独立于估值模型的第二条机制并登记开放问题；修正方向定为放宽历史名单而非收紧今日池，登记待办不执行；复现推荐配置五起点中位 21.95%、回撤 33.5%，交易成本拖累约 0.20pp/年 |
| **v2.36** | 2026-08-09 | OI-034 第 20 步（立 §12.9.29）：新增 `fetch_a_share_research_reports.py`（2017-01~2026-08 共 145,460 篇）、`ResearchGate` 与 `--research-gate`/`--research-window`/`--research-tol`/`--research-missing`/`--research-permute`、`analyze_research_expectation_signal.py`；研报预期门槛信号真实但小（十起点中位 +1.88pp、低于 20% 计数门槛），保留为可选口径缺省 `off`，若用须 `target/180d/missing=pass` 且不早于 2018；`--research-missing block` 必须禁用 |
| **v2.35** | 2026-08-09 | OI-034 第 19 步（立 §12.9.28）：「只赚 30 点就卖掉 20 倍股」根因是报告期之间 V 冻结、卖出信号不含新信息；四种解法实测均未采纳（减持线 1.50 代价最低），残余收益模型受 `g_T ≤ R_f` 与 `ROE_T ≤ ROE0` 压制、没有退出规则能捕获该类行情 |
| **v2.34** | 2026-08-09 | OI-034 第 18 步（立 §12.9.27）：新增 `--rank-mode ratio`（当前 P/V ÷ 历史中位，O(1) 窗口中位、无前视），分位饱和缺陷已解但年化五起点全负（中位 −3.4pp），保留为可选口径、留待用户裁定 |
| **v2.33** | 2026-08-09 | OI-034 第 17 步（立 §12.9.26）：新增 `--rank-mode quantile`/`--quantile-window`/`--quantile-min-obs`（按自身历史 P/V 分位排序、严格无前视），五起点失败——分位在分布底部饱和并列、恰在买点退化为无信息，判构造性缺陷非参数问题 |
| **v2.32** | 2026-08-09 | OI-034 第 16 步（立 §12.9.25）：新增 `growth_confirmed`（ROE 连续两年上行＋净利率、营收同向，无前视）与 `--growth-latest-weight`/`--growth-recent-weight`，选择性验证通过但全局回测变差、目标股仍进不了前十；结论「抬高 V 只解决合格不解决入选」，排序口径留作开放问题；缺省维持 0.6/0.6，`growth_confirmed` 列写入带文件 |
| **v2.31** | 2026-08-09 | OI-034 第 15 步（立 §12.9.24）：新增 `--hold-strong {off,swap,sell,both}`/`--hold-strong-ma`，「强势多头不减仓」六变体全部低于基准、不采纳；查明前提不成立——收益集中标的全是 `P/V` 长期低于 1 的价值型，成长股被残余收益模型终值双重压制按定义排除，留作开放问题 |
| **v2.30** | 2026-08-09 | OI-034 第 14 步（立 §12.9.23）：新增 `--entry-mode {trend,deviation,both}`/`--dev-ma`/`--dev-buy-max`/`--dev-sell-min`，偏离度作入场替代、定投过滤、卖出三条思路全部不采纳；方法论「参数响应是否单调是区分信号与噪声的第一道筛子」建议补入 §12.9 读数纪律 |
| **v2.29** | 2026-08-09 | OI-034 第 13 步（立 §12.9.22）：新增 `--trend-exit-ma` 与 `--no-rank`，跌破均线清仓（风险调整后仍净损失）与空间只作阈值不作排序（全面差于排序式）两条路径均不成立；确认当前配置不按走势减仓、空间排序是收益主要来源，换仓完全依赖 `P/V` 排序 |
| **v2.28** | 2026-08-09 | OI-034 第 12 步（立 §12.9.21）：`backtest_valuation_strategy.py` 增 `--trend-ma`/`--sell-line`，仓位上限、最低空间、均线组合、减持线十七个变体无一稳健改进基准；`MA20>MA60` 在四个均线组合里收益与回撤同时最优；§15.2 建议增补「按结果分组的统计不可直接改成规则」 |
| **v2.27** | 2026-08-09 | OI-034 第 11 步（立 §12.9.20）：澄清 `--swap-margin` 是 P/V 差值不是空间差值（低估值区更严格）；相关性过滤对走势组判无效改动（`收盘>MA20>MA60` 已排除同跌同行业股，两道过滤重叠），推荐配置维持不变 |
| **v2.26** | 2026-08-09 | OI-034 第 10 步（立 §12.9.19）：新增 `--trend-tranche`（走势组分批建仓，换仓复用 `--swap`），「分批 0.5% ＋ 换仓 margin 0.10」五起点全正、年化中位 21.95% 为本轮全部实验最优——+5~6pp 是交互项不是加总；x 非越大越好（1.0 反低）、换仓阈值取 0.10 防抖；代价为回撤 24.3% 升至 33.5%，要低回撤仍用原走势组 |
| **v2.25** | 2026-08-09 | OI-034 第 9 步（立 §12.9.18）：无前视名单上五起点复核——x=1.0 唯一通过（全正 +1.9~+4.9pp，推翻 §12.9.5「x 无稳健差异」）；w=0.10 未通过；走势组定性为稳健的风险交换（年化全负、回撤稳定性全表最强、平均仓位仅 72%，一笔建仓 10% 永不加仓不换仓） |
| **v2.24** | 2026-08-09 | OI-034 第 8 步（立 §12.9.17）：在 141 只无前视名单上重跑全部变体（§12.9.15 含前视读数弃用）——x=1.0 与持仓 5 只的增益是集中度，走势组回撤 23.9%/Sharpe 0.61 全表最佳，换仓与相关性过滤作用都在风险侧，止损与一笔建仓仍为负；分档子集只买 L1 得 9.75%（小样本与估值择时冲突），L2/L1+L2 两行含前视已标注 |
| **v2.23** | 2026-08-09 | OI-034 第 7 步（立 §12.9.16）：新增 `scripts/pit_panel.py` 与 `verdicts_panel.csv`（3,402 家逐家区间制独立判定、只用该年及之前事实），worth 141 家、逐年名单 35~116；全量独立判定主结果 15.88%／回撤 36.9% 立为唯一可复现成绩，§5 选股超额 +11.9pp 为真，今日 261 池 25.25% 中约 9.4pp 系不可复现后视 |
| **v2.22** | 2026-08-08 | OI-034 第 6 步（立 §12.9.14/§12.9.15）：新增 `scripts/build_pit_attention_yearly.py`（进池按首次护城河签名年、留池不因低谷剔除、出池须逐家判定）、`setb_intervals.csv`（14 家含区间）、`entry_overrides.csv`（72 条），逐年区间制名单并集 263 只，主结果 25.82%／回撤 31.4%；Set B 敏感度每家约 −0.11pp；变体重测改写 x=1.0 与 w=0.10 两条旧结论，均单起点、须多起点复核后方可采纳 |
| **v2.21** | 2026-08-08 | OI-034 修正：时点候选门槛对今日 261 只召回仅 41%，改为四条并联门槛后升至 87%；2010 名单修正为 45 家、年化 11.32%，结论不变；2015/2020 两期待按新门槛补判，此前 §12.9.12 的 8.50% 视为下界（新增 §12.9.13） |
| **v2.20** | 2026-08-08 | OI-034 第 5 步（立 §12.9.12）：新增 `verdicts_2015.csv`（311 家）与 `verdicts_2020.csv`（152 家），三期时点名单串成时变可选池实测 8.50%；结论：§5 选股超额为真（+4.6~+7.7pp）、名单内幸存者偏差仅 0.5~2.3pp、今日 261 池 25.25% 中 13.6~16.8pp 无法用定期重筛解释（每五年重筛反 −3.1pp） |
| **v2.19** | 2026-08-08 | OI-034 第 4 步（立 §12.9.11）：新增 `scripts/build_pit_attention_candidates.py` 与 `verdicts_2010.csv`（323 家逐家按 §5 判定、写明命中规则编号），2010 时点名单 35 家；实测 §5 选股本身 +7.7pp、名单内幸存者偏差 2.3pp，今日名单相对时点名单的 13.6pp 待 2015/2020 期名单拆分 |
| **v2.18** | 2026-08-08 | OI-034 第 3 步（立 §12.9.10）：新增 `scripts/build_point_in_time_universe.py`（逐年只用已披露年报重筛），`backtest_valuation_strategy.py` 增 `--daily-states`/`--universe-file`，`build_historical_valuation_bands.py` 增 `--codes-file`/`--uniform-tier`；时点库对 261 池五起点差中位 20.4pp；修次新股 ROE 虚高坑（要求回看窗口内年报全部在上市之后）；回测内 §5 人工分档否掉（统一 L2 反升至 25.78%）；全市场行情取数 98.7%（5,160 只，含已退市） |
| **v2.17** | 2026-08-07 | 缺陷集中修订＋OI-034 前置与首轮回测：结 OI-030（`track_holdings_daily.py` 除权每日检出、固定输出一行）、OI-032（`workflow_decision_log.py` 导入期版本自检）、OI-038（体系偏离留痕只落决策日志）、OI-039（新增 `fetch_overseas_earnings_calendar.py`，§6.8 清单加三列，判据取已披露日 vs `evidence_available_at`）；OI-023 PEG 由输入降为输出，立 §6.5.7.1 三层估值体系（新增 `justified_multiple.py`）；立 §6.5.7.1.1——r 改 `R_f+β·ERP`（质量惩罚移交 MOS L1 10%/L2 20%/L3 30%）、`g_T ≤ R_f` 与降 r 捆绑生效、ROE 归一化改长期锚＋趋势识别＋净利率同向印证；`intrinsic_value` 加 `max_retention=1.0` 与 `min_retention=0.0`；新增 `fetch_ohlcv_history.py`（OI-035，不复权价＋除权事件分开存，立 §12.4.1）、`fetch_a_share_quarterly_financials.py`（立 §12.4.2，`available_at` 取公告日）、`fetch_cost_of_equity_inputs.py`（R_f/ERP 月末序列，立 §12.4.4，`--r-mode market` 可跑全历史）；`build_historical_valuation_bands.py` 铺全池 261 只历史带（立 §12.4.3，加「IPO 前带不参与定价」「`ROE_T ≤ ROE0`」「`ROE_T − g_T ≥ 2pp`」三条护栏，落点 `a_share_historical_valuation_bands.csv`）；OI-034 第 2/3 步回测立 §12.9~§12.9.9（新增 `backtest_valuation_strategy.py`/`analyze_survivorship_bias.py`，估值组推荐配置定为定投＋换仓＋相关性 ≤0.7、不加入场过滤不做档位偏置，滚动 3 年口径立为主指标，跨 2002-2009 段差异 <20% 不作数）；OI-026 立 §7.5.6（新增 `check_report_day_price_divergence.py`，判据取「到带的距离扩大」）；OI-029 三个被否信号重测全部维持（新增 `backtest_signal_gate.py`，立 §12.8）；OI-025 删除前置改挂 OI-037 建成；OI-024 六列补建（新增 `backfill_quality_tier_columns.py` 与 `apply_quality_tier_column_backfill.py`）；OI-033/OI-036/OI-034 关键裁定入册 |
| **v2.16** | 2026-08-07 | 割肉价判定由盘中触及改为收盘口径：§14.3 改 `收盘价 <= stop_loss_price` 才提醒、盘中一律标注待收盘定型；§9.1 取消割肉价盘中例外、全流程统一收盘口径；`track_holdings_daily.py` 改回 `close <= stop`（`day_low` 降为参考读数、删 `stop_hit_basis` 列）；正文版本行补正为 v2.16 并登记 OI-032；同批登记 OI-033~OI-037 五项待办 |
| **v2.15** | 2026-08-06 | 海外关注清单扩至 26 家：新增伯克希尔哈撒韦、AMD、SpaceX、特斯拉、英特尔并逐票建档；脚本新增 `justified_pb` 路径（口径与带系数取 §6.5.2 J 行）；特斯拉与 SpaceX 按 §6.5.5.2 记「无法估值」并写明缺口与解锁条件、代以可证伪反解；附表删「市场」「代码」两列（CSV 保留）；修 `build_overseas_dossiers.py` 虚报复核日缺陷——`valuation_reviewed_at` 只在带/档/分真变时改戳；增速一律按 §6.5.3 自行拆解（AMD 25%、英特尔 18%） |
| **v2.14** | 2026-08-04 | 每日扫描输出格式三项修订：新增 §9.2.1——持仓跟踪、买入候选、§9.6 三张表加 `quality_score` 列，唯一取数口径为分层表经池 CSV 透传并由两个每日脚本写入，报告不得手填或重算（落地校验打印非空行数）；新增 §9.2.2——事件与估值更新只写结论行（无更新明写、改带写旧值→新值），财务明细与推导改写入该股 README 与 fundamentals；§9.2 第二节与 §9.3 由要点行改为 11 列表格并给出逐列取数口径（弱级仍只汇总只数）；不动任何判定标准 |
| **v2.13** | 2026-08-04 | 新增 §12.7 量价信号回放口径：信号取舍一律按流程口径（信号成立且当日为 buy_candidate），固定同报 20/60/120 日超额并声明前视偏差；§12.4 开唯一豁免：量价回放可用当前带与分层还原历史矩阵资格；§8.7.11 维持正式买入信号、门槛 1% 定案；结 OI-028；新增 OI-029；回放脚本增 --since（默认 2022-07-01）并报实际首日 |
| **v2.12** | 2026-08-03 | 结 OI-027：§8.7.11 突破日涨幅门槛 pct_chg 单位错误修复，门槛取 1%，改具名常量 BREAKOUT_DAY_MIN_PCT；正文「77 次/+1.4%/51%」回放断言降为不可复现，新增 OI-028；校准脚本入库 scripts/backtest_8711_threshold.py（--as-of、--gate pipeline、--forwards 20,60,120）；§8.7.11 新增「回放必须声明闸门口径」 |
| **v2.11** | 2026-08-03 | §9.6 深度低估门槛改为 L1 空间 ≥30% 且不再要求档位=低估、L2 ≥50% 且须低估、L3 不适用；名单改强制全量输出（每只列层级×现档、现价、合理价区间、空间、当日、5 日、止跌证据、区间走势、trend_strength）；daily_buy_candidates.csv 增列 fair_price_low/fair_price_high；§9.2 持仓表「带位」改「空间」，band_position 列更名 upside；新增 OI-027 |
| **v2.10** | 2026-08-03 | 新增 §6.5.5.2：「无法估值」只表示建档未完成，已建档公司一律给出可双向使用的带，通用模型失效转入逐票推导；海外新增 mid_cycle 路径（中枢营收×10 年净利率中位、戈登稳态 PE），存储三家脱离无法估值，微软改前瞻 12 个月锚、Meta 改三阶段 DDM；build_overseas_dossiers.py 审定档改随带重算；§6.8 复核触发新增：财报后首个价格较披露前收盘变动 ≥7% 且推出带时 T+1 内复带 |
| **v2.09** | 2026-08-03 | 撤销账户级防护层删除提案、全部保留为常设规则：§13.4 改「账户级防护层（常设）」——仓位包络 100-160%N、杠杆风险提醒三层、融资棘轮、结构上限、账户回撤梯 −8/−12/−20、担保比例预警与 `portfolio_account_snapshot.csv` 六类保留，常态不进每日报告、触及阈值当日提示一行；§14.7 改写为保留依据；§6.5.0 补入标签集与字母跳过 I/L/Q/R 的说明；登记 OI-025（结构上限成文有效却无执行点）；删 6 份 `agents/openai.yaml` 与二跳路由文件，七份 SKILL.md 删复述段与失效路径 |
| **v2.08** | 2026-08-03 | 外部审核复核后一致性修订：§6.7 要求 10 补回 `apply_valuation_band_cards.py`（原链缺唯一搬带步骤）；折现率三处收敛到 8.5%；质量分层定唯一真值源——`a_share_quality_scores_v2.csv` 退役归档、12 列并入 `a_share_watchlist_quality_tiers.csv`，宁德时代裁 L1、分布更正为 22/230/9；§14.5 动作增设「数据缺失」第四取值；扫描脚本 ≥50% 失败判系统性故障、非 0 退出；删 9 个已退役成长期权用例、新增 `test_failure_semantics.py` 九个故障用例；溯源列改写实际 CLI 入参；§7.5 第 5 条升格为 §7.5.5；§2.1 定义业务键与改判链（`workflow_decision_log.py` 可跑自检）；ADR 0002/0003 删除、0005/0006 收缩；登记 OI-024，OI-021 结案移出 |
| **v2.07** | 2026-08-03 | `sources/` 原件目录退役并全部删除，证据统一落 `valuation_evidence/<代码>.json`：结构化层（40 期财报、逐份研报一致预期、预告快报、估值分位，带 `retrieved_at_utc`/`source_urls`）＋人工取证层 `manual_evidence` 数组（外部取证项检索后逐条写入，档案 `band_derivation` 须指名所引条目）；`fetch_a_share_valuation_evidence.py` 新增 `load_manual_evidence()` 显式结转并每轮打印条数；`manual_evidence` 是取证记录处而非替代品，缺输入仍判「无法估值」；§6.5.7 与 §2 产物表同步、README 页脚改指证据 JSON；登记 OI-023（PEG 随 ROE 上移在全池无鉴别力） |
| **v2.06** | 2026-08-03 | 正文精简 2417 行改 2288 行、不改任何在用标准：§6.5.6 成长期权条文压为 7 行退役声明并删七列输出字段与四处引用；§15 第 5/7 条等四处仍写成生效的已退役条文按 v2.05 口径改写；§6.6 参数表、§14.5 输出块、§3 矩阵复述等六处重复段删除或改指针；一次性程序与迁移期口径压缩；失效交叉引用批量修正（`§9.1 步骤 0` 改 `1a` 共 9 处、`S/A/B/C/D` 改 `S/A/C`、`watch_only` 残留 4 处改 `excluded`、§4 总流程末两行改 v2.05 口径）；§5.7.1 并列编号改 7-10 |
| **v2.05** | 2026-08-03 | 持仓侧整体降级为跟踪、§10 买入前闸门整章退役（用户指令）：§14 重写——持仓清单缩至五列（代码/名称/股数/成本价/割肉价），每日只做逐票公告新闻检索＋估值跟踪，`动作` 只有持有/割肉提醒/重大事项三取值；退役建仓期状态与 `position_status`、估值卖出减仓梯、趋势卖出全套判定、硬证伪清仓状态、持仓动作七状态、浮盈与已实现盈亏、留底原则、健康度分组、卖出前人工核对；保留割肉价提醒与除权除息机械调整；§10 的 15 项闸门与四取值合规判定停止执行、`pretrade_decisions.csv` 停写，只保留成交回写；§13 收缩为口径定义（加仓四项与 1.5%N 校验退役）；账户防护层删除提案进 R7 冷静期（组合验算实测依据写入 §14.7、待删项列于 §13.4）；OI-020／OI-022 随载体退役消解；§16 两处硬写版本号删除 |
| **v2.04** | 2026-08-03 | 海外关注清单 21 家逐票建档：新增 scripts/build_overseas_dossiers.py 与 scripts/overseas_dossier_inputs.py，按 §6.5.7 四条路径重算（18 家出带、3 家无法估值）；21 家按 §5.7.4 打参考分，池 MD 附表增参考分列；建 data/companies/ 21 个目录；§6.8 新增海外口径：r=8.5% 跨市场统一、美股分红率用股息+净回购、未派息公司用 1−g/ROE、L4 系数 [0.75,0.95]，加带宽自检 |
| **v2.03** | 2026-08-03 | 删除池 MD 合理价区间列的「（档）」行内标记：build_a_share_core_valuation_pool.py 的 display_cells 去掉后缀拼接，表头改为统一说明「带非空即档案带」；§6.5.5.1 第 3 条与 §6.5.7 全池建档后果第 1 条同步；机器侧 band_derivation 列不变 |
| **v2.02** | 2026-08-02 | 同步 a_share_holdings.csv 的 quality_tier/valuation_tier/strategy_tag 三个派生列至池口径（38 处）；scan_holdings_sell_signals.py 末尾新增清单与池的漂移自检（比对原始行，逐条打印、不阻断）；§14「持仓清单字段」成文：三列为派生量、判定以池为准、改分层或估值档后须同步清单 |
| **v2.01** | 2026-08-02 | §9.1 改为五步定序：事件与估值更新→全池定档→池内量价与区间走势→持仓监控→输出，有带改动即先重跑建带卡、校验、池物化；新增 §9.1 1b 逐票事件核查；新增 §9.6 深度低估重点关注（档位=低估且空间 L1 ≥40%／L2 ≥60%／L3 不适用、已见止跌企稳、trend_strength≠weakening，不作左侧买入依据）；扫描脚本增列 margin_of_safety/deep_value_watch；§9.2 第二节增深度低估与当日估值更新两行 |
| **v2.00** | 2026-08-02 | workflow_decision_log.py 的 WORKFLOW_VERSION 改为解析本文件首行标题；通用十一类建带公式降为分类与新入池脚手架，§0 路由改指 §6.5.7，未建档一律无法估值；§6.5.6 成长期权退役；撤销 §6.5.5.1 第 4 条升级义务；Ashare_quality_rubric.md 的参考分公式、旗标表、评分硬约束与输出字段上收为 §5.7.4；归档 9 个脚本至 scripts/archive/、3 份文档至 docs/archive/；删 suppress_reason 死分支 |
| v1.62 | 2026-08-02 | 全池 261 家建档完成，band_derivation 全部为 dossier，通用十类退居分类标签；列覆盖自检在通用行为 0 时改挂档案必填列；§6.5.2.2 预告入锚改由逐票推导写入、§7.4.1 更新触发承担；跨三档改判 11 家取消买入候选 |
| v1.61 | 2026-08-02 | 建档第十一批 16 家（176/261）；分红率 ≥60% 者按 §6.5.7 走 DDM 而非 PEG；归母跳升而扣非下滑者改用 TTM 扣非作锚；共识滞后于预告者改用 TTM；扣非连续为负者用 2028E 一致预期×同业倍数÷1.1² 贴现；below_runrate 6 行均有覆盖理由 |
| v1.60 | 2026-08-02 | 建档第十批 16 家（160/261）；§6.5.7 打折判据改为「最近一期扣非归母同比为负即打折」，营收方向降为诊断项；新增已披露实际高于共识时不打折、改用 TTM 作锚；规则冲突时优先在倍数一侧保守；增速低于 r=10% 的资源股改以商品价格为可证伪量 |
| v1.59 | 2026-08-02 | 新增 build_company_dossier_readmes.py 渲染档案 README；§6.5.7 新增前瞻锚打折规则：最近一期已披露业绩与共识方向相反时锚改 (TTM+2026E)/2；§15.2 新增第 14 条，PEG 增速窗口统一为 2026E→2028E；建档至 144/261；§16 缩为指针 |
| v1.58 | 2026-08-02 | build_valuation_band_cards.py 新增列覆盖自检（全空列、无 TTM 归母行；通用行 below_runrate 须同时置 cycle_assumption；分母取通用行）；ttm() 新增兜底：年报+本年累计−上年同期累计；建档第八批 16 家（128/261）；§15.2 第 3 条改写 |
| v1.57 | 2026-08-02 | fetch_a_share_valuation_evidence.py 新增 em_market_suffix() 支持北交所 .BJ 后缀，三只北交所票 finance_periods 由 0 期修复；建档第七批 17 家（112/261）；崩塌型标的改用中位锚；below_runrate 2 行均写明覆盖理由 |
| v1.56 | 2026-08-02 | §6.5.4 新增硬规则：多阶段模型高增长年数 n1 默认上限 3 年（=一致预期窗口），更长须写明具体证据；回改海天味业 24.13-29.49、伊利股份 33.46-40.90、伟星新材 6.752-8.252；建档第六批 17 家（95/261） |
| v1.55 | 2026-08-02 | 建档第五批 16 家，档案至 78/261，首次按 §6.5.4 三条路径分派倍数（分红率 ≥60% 走 DDM／ROE 修正 PEG／TTM×自身中位）；三花智控人形机器人期权按 §6.5.6 无证据等级与里程碑不计入；比亚迪月度产销快报列为第一跟踪指标；中航高科、紫光国微由高估改判较高估 |
| v1.54 | 2026-08-02 | 海天味业改三阶段 DDM 得 26.81~32.77；§6.5.4 新增「倍数必须被推导出来」：合理 PE 只能出自多阶段 DDM／派息折现隐含 PE 分红率/(r−g)／ROE 修正 PEG，现值口径系数 [0.90,1.10]，须给 r/n1/g∞ 敏感度并反解隐含折现率；CARD_FIELDS 补入 runrate_check |
| v1.53 | 2026-08-02 | §6.5.7 成文运行率不变量对档案带同样生效，runrate_check 改在 build_card 唯一出口兜底；§6.5.7 新增「档案的目的」与「格式统一」；§7.4.1 触发表新增高频经营数据／产业政策／技术兑现三类，档案更新四步：更新档案→重算带→reviewed_at 改当日→README 重渲染；建档第四批 16 家（62/261） |
| v1.52 | 2026-08-02 | 结 OI-018：改为 §6.5.4 运行率不变量，盈利锚÷已披露 TTM 归母（含预告）<0.85 置 runrate_check=below_runrate 并写 runrate_override_reason，非盈利锚记 na_not_earnings；新增 §15.2 第 13 条；档案新增 next_earnings_check 与 hf_indicators 列；持仓 17 只全部建档（46/261） |
| v1.51 | 2026-08-02 | 新增 §15.2「建带与改规则前必读」12 条（建档时第 1/5/6/7/12 条、改规则时第 2/3/4 条强制自检，只增不删），§0 路由表增入口；结 OI-019：建档队列清零（31/261），无法估值 0、band_is_floor 0，覆盖 ≤2 家者改用非共识锚 |
| v1.50 | 2026-08-02 | 新增逐票研究目录 data/companies/<代码>_<名称>/（README.md＋research_ledger.md＋sources/），analyst_reports/ 退役，CSV 新增 dossier_dir 列；新增「只保守一次」硬规则：锚、倍数、分层系数最多一个偏保守（厦门钨业修正为 34.72-42.89）；档案新增 implied_growth_years（r=10%） |
| v1.49 | 2026-08-02 | 建档第二批 8 家（逐案例分析），档案至 17/261、无法估值 14、下限带 10：百济神州与中国船舶用远期一致预期贴现，中控技术/深信服倍数压至 20x，广联达按修复口径 PEG 1.0，石基信息用每股净资产锚，华大九天用市销率 P/S 15，厦门钨业拒绝前瞻锚改用归一化净利率 6.86% |
| v1.48 | 2026-08-02 | §6.5.7 首批建档 8 家（皆 bespoke，9/261，无法估值 22、下限带 17）；建档以一致预期覆盖 ≥3 家为界，不足者不得用单家研报凑前瞻带；带宽一律由分层系数承担（L1 1.28x／L2 1.24x），废弃 PEG [1.0,1.5] 直接定带 |
| v1.47 | 2026-08-02 | 废止「不发卖出」状态：带不能双向使用且无档案者一律判无法估值并清空带（存 valuation_unvaluable_reason），删「·不发卖出」「（下限）」标记；撤销 v1.45 的 method_divergence 卖出抑制；新增 §6.5.7 逐票估值档案 a_share_valuation_dossiers.csv 与 bespoke 标记（六项必填），首例紫金矿业 37.78~46.67 |
| v1.46 | 2026-08-02 | §6.5.2.2 分界改为未被定期报告取代＋NOTICE_DATE ≤ as_of＋报告期已过 ≥50%，预告取值改区间中值；结 OI-017：D primary 不属下限带（下限带 88→23），每轮打印下限带与缺成长期权数；apply_valuation_band_cards.py 改整列覆盖含清空；新增 OI-018 |
| v1.45 | 2026-08-02 | 结 OI-015：新增 §6.5.2.2 预告/快报入 TTM 锚与运行率校验（快报优先、按累计额合成、跨字段缺失退回已披露口径、置 anchor_vintage）；结 OI-016：新增 §6.5.3 双口径背离处置字段 method_divergence，只抑制卖出侧；池 MD 下限带标「（下限）」「·不发卖出」；新增 OI-017 |
| v1.44 | 2026-08-02 | 结 OI-014：新增 §5.7.3 市场结构性缩量判据（总量而非份额／连续 ≥3 年下降且距峰值 ≥30%／仍在进行／作用于壁垒，逐条写入 tier_reason，命中只降一档），§5.7.1 第 3 条与 §5.8 第 5 步同步；新增 OI-015、OI-016 |
| v1.43 | 2026-08-02 | 新增 §7.4.1 估值每日逐票更新义务（披露、新研报、重大事项任一发生即当日重算带，研报以带变动 >2% 判定）；evidence_cutoff 改按（公告日, 报告期, 标签）取最大；全池 261 家证据重取，18 家带变动 >2%；新增 OI-014 |
| v1.42 | 2026-08-01 | F/H 中枢盈利改以一致预期为主锚，安全阀为预期修正方向（研报下调 >5% 退回历史加权口径）；16 家人工重估落成 manual_revaluation_2026-08-01.csv；evidence_available_at/valuation_evidence_event 改每轮从证据文件派生，59 只 express 复核闭环 |
| v1.41 | 2026-08-01 | A-1 折现率定为 r=8.5%（10Y 1.8%+ERP 6.7%）；A-1 改两阶段 DCF 但不定带，输出 implied_return/implied_return_tier；中枢净利率改加权 0.5×TTM+0.3×近3年中位+0.2×窗口中位，上限为一致预期隐含净利率；新增业务趋势检验（极端 PE 分位者须人工重估） |
| v1.40 | 2026-08-01 | 结 OI-013：新增 §5.7.2 侵蚀路径否决判据（须同时满足点名路径／非普适性／概率有可指事实／与对照锚一致），宁德时代据此改判 L1；相对法构造性盲区成文，新增 multiple_regime_flag 只标记不改带；A 类新增两阶段 DCF 交叉校验并反解隐含折现率 |
| v1.39 | 2026-08-01 | 池阅读版改按质量档 L1→L3、档内参考分降序排列并新增「参考分」列（§5.7 参考分只作档内排序展示）；分层阅读版去 000_ 前缀降为明细文件；持仓卖出扫描与池的三态口径同源；§14 减仓梯落地为 {M,H,1.2H,1.44H} 台阶按剩余股数递减，脚本弃用已废止的留底原则 |
| v1.38 | 2026-08-01 | 结 OI-010/011/012：新增营收周期性判据（近 10 年营收下降年占比 ≤20% 用当期营收／≥30% 用跨周期中位／其间取均值）；新增超额利润持续年数反解 N（价值=中枢×倍数+Σ(运行率−中枢)/(1+r)^t）；§8.7.3 降为提醒信号写入 observation_tags |
| v1.37 | 2026-08-01 | 新增 §9.5 扫描缺口回溯（缺口 >1 交易日即逐日重跑 classify_signal，新增 --since）；新增 §8.7.10-8.7.12 回撤承接型信号，§12 回放仅 §8.7.11 突破后回踩 MA10 通过，§8.7.10/§8.7.12 降为 observation_tags 观察标记；新增 OI-012 |
| v1.36 | 2026-08-01 | §6.5.4 运行率硬校验落地，基准改 TTM、情景基准取 max(TTM, 最近季年化)；锚 <TTM×0.85 即标 cycle_assumption、并列 scenario_band、置 fragile、不触发提醒卖出；新增 OI-011；韩股行情接通（secid 177.000660/177.005930） |
| v1.35 | 2026-08-01 | 复权口径核实无错配；结 OI-009：A-1 r=7.0%、K r=6.5%、g=min(2.5%, ROE×留存率)，A-1 新增前置「可持续内生增长 ≤2.5%」；结 OI-003：池物化不得静默跳过无法归类的行；新增 docs/archive/ 与 data/archive/completed-queues/ |
| v1.34 | 2026-08-01 | 结 OI-008：新增 fetch_a_share_dividends.py，回购注销率由股本净减少测得；A-1 恢复执行、与 A-2 取孰低（分红率 ≥60% 门槛，背离 >25% 置 fragile）；K 恢复 Gordon DDM 与 K-2 取孰低，护栏 g ≤ r_low−1.5pp；校验二改双向只报不改带；新增 OI-009 |
| v1.33 | 2026-08-01 | 锚与倍数口径一致约束：PE扣非中位 = PE归母中位 ÷ 历史(扣非/归母)中位（美的集团改判较高估）；D/M/P 远期现值锚带系数改 [0.90, 1.10]（宁德时代回到中性）；新增 OI-008（股东回报不在任何建带口径内） |
| v1.32 | 2026-08-01 | 结 OI-007：中枢改为 TTM 营收×窗口净利率中位，加结构断点检验（近 5 年 vs 前 5 年净利率中位偏离 >50% 则窗口收缩到 5 年），F/H 倍数改自身 5 年 PE 中位；结 OI-006：远期锚定为 PV=E(T+n)×1/(r−g终值)÷(1+r)^n；D/M/P/N-2 base 带一律 band_is_floor |
| v1.31 | 2026-08-01 | 新增 §6.5.5.1 兜底口径阶梯（F-2/H-2 取孰低、K-2 同 A-2、N-2 用峰值净利率×85%、M-2/P-2 为下限带、C/D 按覆盖分级、净资产兜底），无法估值 90→0；新增 anchor_quality/band_is_floor，下限带不触发提醒卖出；59 只完成 §7.5.5 复核；新增 OI-007 |
| v1.30 | 2026-08-01 | A-2 带系数按分层分档 L1 [0.90,1.15]/L2 [0.85,1.05]/L3 [0.80,1.00]；新增 C→A 迁出判据（近三年最低 ROE ≥20% 用 15%、12-20% 用 12%，回迁 18%/15%）；远期利润锚禁用自身历史 PE 中位；全池 261 家重建；新增 build_valuation_band_cards.py 等三脚本；复核日按实际证据日回填 |
| v1.29 | 2026-08-01 | 新增 §6.5.2.1 锚定量取数口径（建带输入不得含当日现价/市值；累计口径差分成单季再求 TTM；一致预期取 ycmx 逐份中位且覆盖 <3 家不可用）；H 类禁用全窗口 PE 中位；J 类取孰低、银行 COE 8.0%-11.0%；新增 §6.5.6 成长期权（仅 L1/L2、≤base 带中值 50%）；新增校验检查 7 与回归测试 |
| v1.28 | 2026-08-01 | 估值分类改十一类主标签 A/C/D/E/F/H/J/K/M/N/P，B/G 降为通用校验方法；§6.6 步骤 8 关闭「档位反推带」；新增建带卡五槽十二字段、§6.5.4 参数取值成文表（吸收 OI-001）、§6.5.5 敏感度强制与 band_fragile 降一档；新增 validate_valuation_bands.py 作池物化前置门禁 |
| v1.27 | 2026-08-01 | 质量分层五档改三档（L1 强/L2 中/L3 弱护城河，由 Q1×Q2 判定），分层不改名单归属，金字塔要求退役；准入矩阵改三态（可买/可持有/提醒卖出）；删 §8.10 B、D 档；watch_only 层退役；估值卖出改按档分级+减仓梯，废止「留底≥建仓金额」；个人体系 §12 整节废止 |
| v1.26 | 2026-07-30 | §7.2 质量复核触发基准改为 max(last_quality_review_date, evidence_available_at)，分层表新增 evidence_available_at 字段；§6.6.1.2 新增 2a 股本口径校验：研报归母÷研报 EPS 反算隐含股本与现股本不一致时改用归母净利口径建带、禁用研报 EPS 及其 PE/PEG |
| v1.25 | 2026-07-30 | §10.1 要素项第 7 项去掉「已给定 stop_loss_price」，建仓时割肉价缺失不作 off_system 判据；新增 §14 割肉规则第 4 条：缺失时无 Tier-0 保护、扫描置顶提示、补定前禁止加仓；存量 3 笔改判 compliant（compliant 10／off_system 12／hold_off 1） |
| v1.24 | 2026-07-30 | §10 闸门分两类：要素项（硬前置，第 1/5/7/11/12/13/14 项，任一不达标不得建仓）与研究档案项（限期义务，其余 8 项，未补齐期间 pending_adjustment＋禁止加仓）；§10.4 重写：decision_result 新增 compliant，off_system 重定义为要素缺失；第 5 项增弱级×等确认视为未达标；不可核验条件不产生合规结论 |
| v1.23 | 2026-07-30 | 撤销买入时间窗：§15 第 7 条改为「执行时点不设限」，买入与卖出均无时点约束；扫描报告不再输出「14:40 后执行」标注、不再把时间窗列为 off_system 缺项；§12.6 新增第 5 组对照样本（逐笔记录买入成交时段与其后 5/20 日前向收益） |
| v1.22 | 2026-07-29 | 新增 §6.8 海外关注清单（观察口径，一律不可买、不入池 CSV、不进 §8 扫描）：新建 overseas_watchlist_valuation.csv（渲染为池 MD 文末附表）、overseas_effective_tiers.csv 与 overseas_quotes.py；首批 19 家，附表按 L1→L4 排列 |
| v1.21 | 2026-07-29 | 趋势态判态/毕业改为任一交易日 close > MA20 > MA60 > MA120（§8.6 新增中期多头排列）；§14 风险预警 4 单笔风险持仓监控版退役；§13 第 8 条结构上限改主动/被动口径，被动漂移越限只禁加仓并每越 10 个百分点复核；§13 第 6 条 1.5%N 为建仓时一次性校验；§12.6 新增第 4 组对照样本 |
| v1.20 | 2026-07-27 | 撤销止损网格 6%N/8%N 预算，§13 第 5 条改为杠杆风险提醒三层（>160%N 警告；市场转弱、政策性风险事件、持仓性价比下降任一提醒收敛至 130%N；两项及以上或回撤 -8% 收敛至 100%N）；新增 §8.11.1 持续候选置顶提醒（近 10 日达标 ≥8 日且新触发 ≥4 日，最多 12 只），脚本增列 persistent_candidate |
| v1.19 | 2026-07-23 | 持仓趋势走坏判定分态：按首次建仓日收盘 vs 当日 MA120 分趋势态/反转态，趋势态维持破 MA60/MA120，反转态改按启动结构锚 launch_platform_price＋MA20 判走坏；§13 加仓条件 1 趋势项分态化；卖出扫描脚本增 trend_ref_state/ma20 列 |
| v1.18 | 2026-07-22 | 新增 fetch_a_share_report_disclosures.py 每日物化正式定期报告与业绩快报（§6.7.9），入 §9.1 步骤 0；§7.2/§7.3 复核触发改公告日口径：公告日 > max(估值时间, evidence_available_at) 即入队冻结；§6.7.8 待复核名单改三类披露并集；queue 增 --report-disclosures，pool 增 --disclosures |
| v1.17 | 2026-07-22 | §12.6 台账增设第三组对照样本「规则驱动的退出」：规则促成的清仓/减仓以 execution_record 逐笔入列，跟踪卖出价后 5/20 日前向收益，月度与既有两组及 §10.4 一并复盘；首批入列紫光股份 2026-07-15、锦波生物 2026-07-21/22 |
| v1.16 | 2026-07-22 | §9.1 新增步骤 0 披露同步：每个扫描日先跑 fetch_a_share_earnings_forecasts.py 再 build_report_update_queue.py；§7.3 新增确定性预告触发（公告日晚于 valuation_reviewed_at 即入队冻结）；§7.5.5 增超期补做；§6.7.8 预告物化改每日刷新、汇总改输出待复核名单；queue 增 --forecasts |
| v1.15 | 2026-07-21 | §9.2 持仓健康度表增列层级×现档（现档与审定档不同时写「审定档→现档」）与带位（带内X%／低于带底-X%／越带顶+X%），与第二节候选行同口径；scan_holdings_sell_signals.py 新增 band_position 列，§9.3 机器明细同步 |
| v1.14 | 2026-07-21 | 撤销 v1.13 单向限档与 §7.4.7 下行越档复核：§6.2.1.6 恢复双向不限幅、价格类档位变化不触发复核；三脚本还原同源无限幅定档函数，撤 TIER_LOOSE_ORDER/‡ 标记/待复核名单；40 只钉档还原为纯价格档；池 MD 表头补注箭头左端为审定档 |
| v1.13 | 2026-07-20 | §6.2.1.6 新增单向限档：价格自动定档相对审定档向宽松方向至多推进一档、收紧方向不限；新增 §7.4.7 下行越档复核（钉档标的入 express 队列）；池 MD 增 ‡ 钉档标记，刷新日志增待复核名单（v1.14 撤销） |
| v1.12 | 2026-07-20 | 卖出规则重设：退役 3 个月锁定期、Tier-2 软走弱通道、盈利后离场档位表、三档趋势保护线（MA150 出均线组）；卖出许可限定为 Tier-0 割肉／硬证伪清仓／估值卖出（现档较高估/高估，留底≥build_amount_cny）／趋势卖出（收盘破 MA60 或 MA120）／账户级降风险；止损网格 6/8%N 撤销进入 R7 冷静期至 2026-07-27 |
| v1.11 | 2026-07-20 | 割肉价简化为唯一硬规则：建仓时用户设定，当日价触及即无条件清仓（Tier-0）；撤销 MA120 上调上限、raise_stop_suggested/suggested_stop_price/ma120 输出与 stop_loss_last_raised_date 字段；「建议上调割肉价」改纯风险预警；同批设定/上调 13 只割肉价 |
| v1.10 | 2026-07-17 | 池 MD 删「带位」列、保留空间（区间中值/现价−1）；估值档「可接受较高估」全系统简化为「较高估」，同步 §5/§6.2.1/§6.2.1.6/§6.6/§8.13、池与扫描脚本、估值源表/池 CSV/pool_effective_tiers.csv 与 WORKFLOW_VERSION |
| v1.09 | 2026-07-17 | 池 MD 删「中报预告」列：预告物化文件降为 §7.5.5 express 复核队列后台输入（预告公告日晚于 valuation_reviewed_at 即待复核），forecast_cell 前瞻 PE 展示退役，刷新汇总改记「业绩预告覆盖 N 只」；§2/§6.7.6/§6.7.8 同步 |
| v1.08 | 2026-07-17 | 池 MD 删「估值时点价」列，PB 后新增「估值时间」与「估值事件」两列；§6.6 输出新增 valuation_evidence_event 字段并回填 2026-07 批次；§6.6 补「任何复核完成后必须更新 reviewed_at 与 evidence_event」，§7.5.4 同步 |
| v1.07 | 2026-07-17 | 池 MD「估值价」列更名「估值时点价」并移至现价之后，表头注明其为审计基准而非模型输出，合理价区间为估值唯一输出锚、公允中枢≈区间中值；§6.7.6 同步 |
| v1.06 | 2026-07-17 | 新增 §8.7.9 底部企稳与V反计段（平台企稳大阳、平台温和转强、V反启动阳线各=一段，平台上沿新高=二段，不要求 §8.5 放量）；§8.13 新增执行规则 5 阶段延续与承接（有效段位=近 5 交易日最高段，承接升段、趋势里程碑、失效归零），扫描增列 entry_stage_today；L2×中性所需阶段 4→3 |
| v1.05 | 2026-07-17 | §6.2.1.6 重写为档位由现价对照合理价区间每日自动重定（>1.2×带顶高估、带顶~1.2×带顶可接受较高估、带内中性、带底以下按空间 ≥40% 分低估/较低估），删 §7.4.7/§7.4.8 价格类复核与 */† 标记；池 MD 合并为单一列表，新增快照 pool_effective_tiers.csv；北交所行情接入腾讯 newfqkline；band_top_flag 改 valuation_tier_changed |
| v1.04 | 2026-07-17 | 高估/无法估值名单物化为 pool_layer=excluded 仅观察行进入池 CSV 与每日扫描（decision_type=scan_excluded）；新增 §7.4.8 高估回落复核（回落至带顶及以下打 †）；池 MD 两表统一列并新增「中报预告」列（§6.7.8），新增 fetch_a_share_earnings_forecasts.py |
| v1.03 | 2026-07-17 | 池阅读版去「层」列，新增现价/带位/空间/PE(TTM)/PB 快照列（新模块 a_share_quotes.py），估值列 *=越带顶待复核；物化脚本增 --md-only --quotes fetch 每日刷新并入 §9.1 步骤 2；扫描脚本落地 §8.5.6/§8.7.8/§8.13 入场阶段（entry_stage/stage_required/stage_met）与 §6.2.1.6 定档列 |
| v1.02 | 2026-07-17 | §10.1 删除赔率项与左侧通道（16→15 项），估值约束移至池层 §6.2.1；新增 §8.13 分段右侧入场与 §8.7.8 底部/平台放量反转；事件通道关闭，watch_only 可见不可买，新增 §7.4.6/§7.4.7 复核；仓位改二档制重仓 50 万/轻仓 25 万；§9.1 扫描纯量价化；新增 §8.5.6 巨盘温和放量、§6.6.1 单票台账、§12.6 对照台账 |
| v1.01 | 2026-07-14 | §13 执行规则 4 明确向下兼容：袖序核心>准核心>动量>观察，标的按层级×估值定可入最高袖、可自愿降席至更低袖持有，反向不允许；观察仓不限于 L4×低估，任何池内合格组合可 ≤3%N 观察席持有；信号要求随席位；个人体系 v1.7 同步 |
| v1.00 | 2026-07-14 | 四袖仓位架构定版：核心仓 3 只×≤15%N／准核心 3 只×≤10%N／动量袖 6 只×≤6%N／观察仓 6 只×≤3%N（满配 129%N）；包络中枢 130%N、浮动带 100-160%N；止损网格 6/8%N 预算；仓位数字唯一存于 §13；版本记录外迁本文件；个人体系 v1.6 同步 |
| v0.31 | 2026-07-14 | 版本号格式切换：自本版起采用 v0.x 编号，v0.31≡v31，历史行保留原编号；后续按 v0.32、v0.33 递增 |
| v30 | 2026-07-14 | 仓位架构全面改净资产（N）口径并采纳分袖上限，§13 重写；默认总仓位 100-115%N、融资硬顶 130%N；结构占比改按持仓市值计 |
| v25 | 2026-07-12 | 每日输出简并为单一文件 `000_daily_scan_log.md`，tracker MD 删除；扫描脚本去 --output-md，只写 CSV |
| v22 | 2026-07-11 | §9 重构：§9.1 组合流程（先持仓后池内、可盘中发起）、§9.2 组合摘要格式、§9.4 新增固定产物 `000_daily_scan_log.md` |
| v18 | 2026-07-10 | §5.4.7 状态迁移表；§7.5 复核期冻结；§5.3 名单不可变快照；决策日志增 decision_id；回放限 available_at<=回放日 |
| v17 | 2026-07-09 | 新增账户级快照 portfolio_account_snapshot.csv，列入 §14 输入；真实持仓录入 a_share_holdings.csv |
| v16 | 2026-07-08 | 吸收外部框架对比结论：§6.6 估值步骤增适用性前置检验与反向预期拆解；§5.8 增 2-3 年护城河侵蚀检验；§0 新增池外单票 express 研判 |
| v15 | 2026-07-08 | 文件结构整理：深度复核协议分层校准并入 §5.7.1（金字塔、锚点对照）；新增 §2.2 跨轮次公司分析索引脚本；复核结果归档，旧脚本与重复文档删除 |
| v12 | 2026-07-06 | 全流程均线日线化，趋势保护线废弃周/月判定；年线 MA250；§13 退出规则并入 §14；新增除权除息字段调整规则、amount_ma20 流动性门槛 |
| v11 | 2026-07-05 | 新增 §0 任务路由表、§8.7 信号分级、§8.12 市场状态、趋势保护线；估值排雷仅 L1/L2；阶段五 §14、执行方式 §15、版本记录 §16 |
| v9 | 2026-06-28 | 三类初筛重写：worth_attention 须过资本复制测试；garbage 仅限造假治理与结构性绝望行业；boundary 含资料不足与平庸可逆 |
| v4 | 2026-06-27 | 新增结论日志、三类初筛、垃圾公司永久排除、季度证券名单更新要求，并补全估值执行规则 |
| v1 | 2026-06-27 | 固定A股质量分层、核心估值合格池、每日量价扫描、连续放量定义和历史回放校准流程 |
| v1~v31（其余） | 2026-06-27 ~ 2026-07-14 | 早期版本：v2 具名历史样本移出正文改外部回放案例；v3 新增放量突破关键均线信号；v6 新增阶段五每日持仓监控与卖出扫描；v7-v8 建仓金额、割肉价、3 个月锁定与硬离场例外；v10 有效放量上涨即列买入候选；v13 均线组定为七条、市场状态改沪深300 MA250；v19 估值扩至 L1-L4 全量并设分层×估值准入矩阵；v20 watch_only 观察层与 L3×中性事件通道，v23-v24 通道改 24 小时催化剂验证并扩至 L2×可接受较高估；v21 §15.1 重蹈覆辙拦截；v26-v27 建仓期 position_status、v28-v29 买入时间窗 14:40、v31 高分位放量单独认定封顶「中」 |
