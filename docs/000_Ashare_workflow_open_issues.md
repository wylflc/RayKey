# A 股工作流待处理事项

登记已确认的流程缺陷与用户要求的待办；每项标明类别、证据、影响、所需动作及完成条件，按发现日倒序排列，每项使用三级标题。编号连续。

完成后，规则变化写入 `Ashare_workflow_changelog.md`，实验依据写入 `Ashare_backtest_log.md` 或报告，编号与处置移入 `Ashare_workflow_open_issues_closed.md`；此处只保留未完成项。

## 待处理（1 项）

### OI-198｜缺陷：存量状态未同步少数股东负账面扣减的零下界（2026-09-21）

- **证据**：OI-197 发布前重建发现19只股票156个报告键仍用 `7bd97bef` 之前的桥公式；苏宁易购2019年报行存量 `net_debt_ps=0.1678`，现行实现为0.2513，差额来自少数股东账面−0.0835。候选／B2都有，详见 `exp_oi197_20260921/legacy_bridge_keys.json`；该清单只覆盖本次151只重建目标，并非全市场普查。
- **影响**：直接用现行源码重建会把非本题的估值变化混入公司行动修复，旧BASE不能据此视为可复现。OI-197对明确键在两组同时冻结旧桥公式，仍要求OLD逐字段复现；生产桥实现未改。
- **所需动作／完成条件**：核定零下界与成文口径的一致性，普查受影响存量、单独按§12.1复核并同步历史状态和下游；消除实验兼容层依赖，保留当前BASE旧输入作为对照。
