# A 股工作流待处理事项

登记已确认的流程缺陷与用户要求的待办；每项标明类别、证据、影响、所需动作及完成条件，按发现日倒序排列，每项使用三级标题。编号连续。

完成后，规则变化写入 `Ashare_workflow_changelog.md`，实验依据写入 `Ashare_backtest_log.md` 或报告，编号与处置移入 `Ashare_workflow_open_issues_closed.md`；此处只保留未完成项。

## 待处理（1 项）

### OI-179 缺陷：历史均线除权映射遗漏停牌期间事件（2026-09-14）

- **证据**：`backtest_valuation_strategy.exright_affine` 仅取 `events.get(days[i])`，事件日无报价时不会折算历史价。电投能源002128于2017-03-16后停牌，2017-06-15每股派0.30，2017-08-14复牌；原MA60作差（前值折至复牌基准）0.312833，按完整除权事件逐窗直接重算为0.017833。见 `data/experiments/exp_ma_slope_20260914/ma_gap_evidence.json`。
- **影响**：既有回测排列、均线止损及以其为基础的历史信号去重可能受影响，范围及收益影响尚未量化。不能把本批新增斜率的独立修正说成整个BASE已修复。
- **当前处置**：本批MA斜率研究只以进入/离开窗口价格的精确差计算新增闸门，逐项纳入停牌期事件；BASE其他逻辑保持当前实现以隔离变量。首轮不满足定义的候选烟测/信号统计作废。
- **所需动作/完成条件**：审计所有按有报价日匹配公司行动的均线/回报路径；用完整事件日历修复仿射映射，覆盖现金/送配及多次停牌事件、未来事件不影响历史、无事件路径不变；按轨道A完成当前BASE全/A同输入护栏并重登，复核受影响前向信号及候选资格。
