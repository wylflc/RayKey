# 工作流待办逐项修复与核验（2026-09-14）

本批处理审核后剩余的 9 项待办，规则落在工作流程 v4.187。用户已明确的 OI-186 保持不变：实际净成交后才启动比例冷却，随后按合格机会递减。旧审核观察保留在 `data/interim/workflow_audit_20260914/`，不覆盖原始反例。

## 逐项结果

| 编号 | 修复后的行为 | 验证 |
| --- | --- | --- |
| OI-180 | 回测买卖档位取信号日净资产，成交日盯市继续服务资金和风险约束 | 合成例由 4,200 股修正为 4,100 股；完成 14 起点配对并重登基准 |
| OI-181 | 收盘取当日未复权报价；均线由原始历史按完整公司行动折到信号日，取消腾讯异基准补尾和未复权均线兜底；零成交占位不增加样本 | 历史截断、未来事件排除、停牌期间多事件、扫描/跟踪同基测试；已启动卖出冷却在 T+1 缺价时仍消费 T 日机会 |
| OI-182 | 执行必需文件缺失、必要列缺失、过期队列或证据摘要不符时失败；空持仓与空队列须可验证，持仓带不退候选带 | 池、队列、分层、持仓、三类表、两侧模型、证据凭据逐项缺失反例；空文件凭据日期及摘要测试 |
| OI-183 | 行情、计划、跟踪、日志先暂存，事件复核通过才安装；最后写成功凭据，失败回滚，安装中断在下次读冷却前恢复 | 主/池外持仓报价失败、事件失败、写冷却后失败、安装中断恢复及并发文件改动反例 |
| OI-184 | 买入与换仓触发候选都与 worth_attention 求交；同日已减档来源不重复换出；零股换仓不登记卖出源 | 空名单不买、出名单不买回、不重复作换仓来源、零股换仓不记卖出测试 |
| OI-185 | 标记市值与可交易性分开；停牌持仓保留经事件折算的标价；总市值不能验证时暂停新增买入，不把缺价计零 | 缺标价不买、停牌市值仍约束总仓位、不能以旧账户现金/负债快照下计划 |
| OI-187 | 跟踪与扫描共用模型加载和拒绝处理；最新拒绝不能复活旧模型，缺模型不使用池展示带；退出只看三类表 | 缺模型、拒绝模型、较新拒绝覆盖旧 ok 行、仍在名单但核心池缺行的反例 |
| OI-188 | 公司行动与证据校验前置；最后一次背离复核读取本次行情形成的合格集；扫描内部同批生成持仓跟踪，取消外层重复扫描后检查 | 跨阶段输入变化、未处理除权、新增取证覆盖、当日新增合格股进入最终复核测试 |
| OI-189 | 核心池 CSV 物化后自动渲染公司 README；公司页明确价格为“估值物化价格快照”及日期，日扫执行价另读已发布行情 | 307 份档案检查全部通过，本批更新 194 份；结构化研究档案及人工证据日期未改 |

执行边界集中在 `scripts/daily_execution_guard.py`；正式计划须由 `daily_execution_guard.py verify --as-of 日期` 核验日期、输入和全部产物摘要。只有行情预览不写正式执行文件。同日重跑仍按 OI-186 的 `remaining_before` 重算，不累加冷却消费。

## 回测验证

实验目录：[`exp_oi180_189_20260914`](../../data/experiments/exp_oi180_189_20260914/)。对照为 OI-186 已确认成交冷却版；两臂只通过 `--execution-consistency legacy/signal` 区分本批引擎修复。`signal` 是生产缺省，`legacy` 只用于复现。此处测的是档位、机会时点和零股/重复换仓修复的合并影响，不将全部收益变化归因于单项。

按预登记使用 `sweep_backtest_configs.BASE`、相同生产输入及已验证等价的面板子集。旧锚点 48 个字段/窗口完全复现；14 起点 × 2 臂 × 全样本/A × 4 档成本，共 224 比较路径，加 1 条复现路径。两臂前五赢家相同，U=A，因此复用同一剔除组。全部起点和窗口严格配对；文件摘要在跑前、跑后相同。

下表为修复臂减对照臂的同起点配对差中位，单位 pp；P 是同窗口滚动 5 年年化先相减、再取两层中位，CAGR 是全期年化。

| 每边滑点 | 全样本 ΔP | 全样本 ΔCAGR | 去赢家 A ΔP | 去赢家 A ΔCAGR | 全样本滚5回撤 Δ |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0bp | +0.606 | +0.694 | +0.002 | +0.208 | −0.362 |
| 10bp | −1.155 | −0.728 | +0.297 | +0.622 | −0.174 |
| 20bp | −2.391 | −1.951 | +0.009 | +0.482 | +0.576 |
| 30bp | +1.896 | +1.822 | −0.317 | +0.126 | +0.103 |

无滑点生产基准通过轨道 A 护栏，无负窗由零转正或新增强平；所有路径负现金日数为零。成本 10/20bp 下若套用同一收益损失护栏会不通过，必须保留这个敏感性结果。工作流程 §12 第 7 款规定成本档位不作否决线，因此以机制正确性和无滑点护栏处理本次既定标准修复；不据此宣称提高未来收益。完整成本表、BASE 各成本相对 0bp 的配对差、起点尾部及标准附表均在实验目录。

28 条新 BASE 摘要以 `sig1_lrc3` 标记重登，原台账行保留。作业 `26683497` 完成，32 核、6 分 14 秒，峰值 31.4 GiB。

## 验证范围和日扫衔接

156 项测试通过：本批 31 项、冷却 25 项、相关执行/资金/除权/模型/队列/参数/纪元测试 91 项、原失败语义 9 项；另有 307 份 README 重渲染检查、文档审计、Python 编译及调度脚本语法检查。命令与证据摘要见实验目录 `validation.json`。

历史原始价格没有刷新：296 股中 155 股末端为 2026-08-07、140 股为 2026-08-28、1 股为 2018-12-20；实验不能代表此后行情。新增每日链使用离线故障注入验证，未在本任务中重跑实际账户日扫或以新接口响应核验全池。实际持仓、成交凭据、冷却及研究原始输入均未改动。

下一次正式日扫先执行 `daily_postclose.sbatch` 的 evidence 阶段，处理必要的模型更新和持仓公司行动，再执行 scan 阶段。此前生成、没有本版成功凭据的旧计划须重扫后才可按本版流程使用。
