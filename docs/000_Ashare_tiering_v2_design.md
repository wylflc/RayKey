# 质量分层 v2（L1-L5 评分制）方案设计与需求分析

- **状态**：设计中，**未实施**。本文件不是标准；标准只存在于 `docs/000_Ashare_workflow.md`（§15）。
- **发起**：2026-07-30，用户提出把 L1-L4 分层改为 L1-L5 评分制分层，并同步重制买入准入尺度。
- **本轮范围**：需求分析 + 现状盘点 + 冲突识别 + 方案选项 + 影响评估 + 待决清单。不改任何标准文件、不改数据。

---

## 1. 用户需求原述

分层由 L1-L4 改为 L1-L5，以护城河强度评分定档：

| 档 | 描述 | 分数 |
| --- | --- | --- |
| L1 | 极强，几乎不可颠覆的护城河 | 95~100 |
| L2 | 很强，很难颠覆的护城河 | 90~95 |
| L3 | 优秀，比较牢固的护城河 | 80~90 |
| L4 | 良好，比较稳固的护城河 | 70~80 |
| L5 | 及格，有一定的护城河 | 60~70 |

`worth_attention` 列表统一视为 60 分及格分以上。同时要求重新细化各档的可买标准（原「L1 较高估可买」这类尺度需按 5 档重新分配）。

---

## 2. 现状盘点

### 2.1 现行分层（§5.7）

| 档 | 现行语义 | 现行可买尺度（§6.2.1） |
| --- | --- | --- |
| L1 | 顶级核心候选，极强护城河 | 低估/较低估/中性/较高估可买 |
| L2 | 准核心候选，护城河明确但有折扣 | 低估/较低估/中性可买（较高估 watch_only） |
| L3 | 战术观察，护城河**不足以**进核心层 | 低估/较低估可买（中性 watch_only） |
| L4 | 零仓观察，护城河强度/证据质量**不足**进前三层 | 仅低估可买、不加仓 |
| **L5** | **不是保留档**——判为「不再值得长期关注」，`attention_class` 由 `worth_attention` 降为 `boundary_pending` | 不建仓 |

### 2.2 现行不存在质量评分

A 股分层目前**没有任何数值分**，`quality_tier` 由逐票判断直接给出。
`docs/moat-scoring-rubric.md` 有一套 0-100 七维加权评分，但其 scope 说明（2026-07-08，依 ADR-0006）已把 A 股撤出，现仅服务港股/美股全覆盖打分脚本。

### 2.3 现行分布（`a_share_watchlist_quality_tiers.csv`，261 家）

| 档 | 家数 | 占比 |
| ---: | ---: | ---: |
| L1 | 24 | 9.2% |
| L2 | 158 | **60.5%** |
| L3 | 67 | 25.7% |
| L4 | 12 | 4.6% |

`attention_class` 全域：`worth_attention` 261 / `boundary_pending` 5337 / `garbage` 55。

§5.7.1 要求「整体应呈金字塔形」，当前实际是**头重脚轻的菱形**——L2 独占六成。这是本次改版必须解决的存量问题，也是分数带落地后冲击最大的地方。

---

## 3. 冲突识别（必须先解决，否则无法动工）

### C-1 L5 语义碰撞（阻断级）

现行 L5 = **剔除标记**，用户提案 L5 = **及格保留档**。同一个符号两个相反含义，不能并存。

| 方案 | 做法 | 评估 |
| --- | --- | --- |
| **A1（建议）** | **退役「L5 = 剔除」这个用法**，剔除完全由 `attention_class = boundary_pending` 表达 | ADR-0005 已确立 `attention_class` 与 `quality_tier` 分离；用一个质量档去表达「不再关注」本就越界。§5.8 步骤 9 现行写法实际也是「降级为 boundary_pending」，L5 只是个多余的中间说法。退役后语义反而更干净 |
| A2 | 剔除档改名 L6 / NR | 可行但多一个无信息量的符号 |
| A3 | 保留 L5=剔除，及格档叫 L4b | 命名混乱，否决 |

### C-2 分数带边界重叠与噪声敏感（技术级）

- 95 / 90 / 80 / 70 同时属于相邻两档。需改为半开区间：L1 ≥95；L2 [90,95)；L3 [80,90)；L4 [70,80)；L5 [60,70)；<60 出 `worth_attention`。
- L1/L2 带宽仅 5 分，而七维加权下**单一维度差 20 分 × 28% 权重 = 5.6 分**，足以跨整档。需要配套：分数留 2 位小数、**档界 ±1 分内强制锚点双重对照并留痕**（§5.7.1 规则 6），防止 94.8 / 95.1 这种打分噪声直接决定买入资格。

### C-3 「护城河强度分」还是「公司综合质量分」（需求澄清级，影响最大）

用户五档的文字定义**全部是护城河强度陈述**（极强/很强/优秀/良好/及格的**护城河**）。但现成的七维 rubric 是**综合质量分**，其中行业前景 14% + 治理 6% 共 20% 权重与护城河强弱无关，却能把一条不可颠覆的护城河拖下档。

用现行 v0.4 权重（护城河 28% / 技术壁垒 24% / 市场地位 14% / 行业前景 14% / 商业模式 8% / 经营质量 6% / 治理 6%）试算：

| 公司 | 各维示意分 | 加权总分 | 落档 |
| --- | --- | ---: | --- |
| 贵州茅台 | 护城河 98、技术壁垒 90、市场地位 98、商业模式 95、经营质量 98、行业前景 **70**、治理 85 | **91.14** | 新 L2 |
| 宁德时代 | 护城河 88、技术壁垒 92、市场地位 95、商业模式 78、经营质量 85、行业前景 85、治理 80 | **88.06** | 新 L3 |

**结论：按现行权重，A 股几乎无人能到 95 分，新 L1 会是空档**，而全池最强锚点茅台只能进 L2。这与用户「L1 = 极强、几乎不可颠覆」的语义不符——茅台正是该定义的典型。

| 方案 | 做法 | 评估 |
| --- | --- | --- |
| **甲（建议）** | **定档分 = 护城河强度分**：由护城河、技术/工艺壁垒、市场地位与不可替代性重构并归一到 100；行业前景、商业模式、经营质量、治理**改为扣分项与一票否决项**，只能压档不能抬档 | 最贴合用户五档文字定义。行业下行不降质量档，本就是 §5.7.1 规则 1 的既有立场（茅台判例：「行业增长走弱只记入估值与仓位备注，不降质量档」）。该方案让规则与分数首次自洽 |
| 乙 | 保留七维加权，分数带整体下移（L1≥90 / L2 85-90 / L3 78-85 / L4 70-78 / L5 60-70） | 改动最小，但违背用户给出的分数直觉，且把「行业景气」偷偷混进护城河档位，重蹈 §5.7.1 规则 8 警告的「短期利润爆发≠更强护城河」 |
| 丙 | 双轨：护城河分定档 + 综合质量分作为降档触发 | 信息最全但要维护两套分，执行成本高 |

### C-4 与 ADR-0006 的关系（需新 ADR）

ADR-0006 把评分从 A 股撤下，撤的是**用脚本自动分做 round-1 三类初筛**；其决策 6 明确写着「Deep annual-report and research reading is **deferred to L1–L5 tiering**」。因此**在分层层引入评分不与 ADR-0006 冲突**，冲突的是拿关键词自动分当证据。

但仍需一条新 ADR 记录「重新在分层层引入评分」，并写死两条边界：
1. 分数**必须由证据驱动的逐票人工评分**产生，不得由聚合站简介/关键词脚本生成（v0.3 校准注记已承认关键词打分是 A 股高分人口虚高的机械成因）。
2. 分数带**不是配额**。ADR-0006 决策 3「There is no numeric target; the count follows from the standard」继续有效；金字塔形状只作校准自检，不作强制分配。

### C-5 60 分线与资本复制测试的一致性

ADR-0006 规定 `worth_attention` 的门槛是**资本复制测试通过**，不是分数。用户提出 `worth_attention` ⟺ ≥60 分，等于新增第二道门槛。两者必须机械自洽，否则会出现「资本复制测试没过但算了 62 分」的裂缝。

**建议规则**：资本复制测试不通过的，护城河维度**上限 55 分**，加权后自然低于 60 线。这样两个门槛是同一件事的两种表述，而非并列的两个标准。

---

## 4. 准入矩阵重制（用户 2026-07-30 指定：三态矩阵）

### 4.1 用户指定的思路

矩阵由**单态（可买 / 不可买）升级为三态**：

| 状态 | 含义 |
| --- | --- |
| **可买** | 过矩阵，具备买入资格（仍须过 §8.13 入场阶段与 §10 闸门） |
| **可持有** | 不可新建仓、不可加仓，但已持仓不因此卖出 |
| **提醒卖出** | 提醒**部分退出锁定收益并留底**，非清仓、非强制指令（口径见 §4.4） |

**这是本次改版的架构性变化**：现行 §6.2.1 是纯买入准入，执行说明第 1 条明写「矩阵是买入准入，不是卖出指令」。三态矩阵让它第一次同时管买入端与持有端，该条必须重写。原 E1/E2/E3 三个平移方案全部作废。

### 4.2 三态矩阵（2026-07-30 用户定稿）

| 层级 | 分数 | 可买 | 可持有 | 提醒卖出（该档及以上） | 池内层 |
| --- | --- | --- | --- | --- | --- |
| L1 | ≥95 | 低估 / 较低估 / 中性 | 较高估 | 高估 | 核心 |
| L2 | [90,95) | 低估 / 较低估 / 中性 | — | 较高估 | 核心 |
| L3 | [80,90) | 低估 / 较低估 | 中性 | 较高估 | 核心 |
| L4 | [70,80) | 低估 / 较低估 | — | 中性 | 战术 |
| L5 | [60,70) | 低估 | 较低估 | 中性 | 战术 |

**设计机理：用「可持有」缓冲档做半步。** 五级估值阶梯（低估→较低估→中性→较高估→高估）本来只够排四档，逐档整步平移到 L4 就会退化成「对低估持仓提醒卖出」。此方案让奇数档（L1/L3/L5）享有一个缓冲档、偶数档（L2/L4）直接由可买跳到提醒卖出，于是每档恰好比上一档严半步，五档在五级阶梯上排得下且互不重复，**不需要设任何地板**。

配套要点：

1. **相邻档的可买集合成对相同**（L1=L2 买到中性、L3=L4 买到较低估），区分落在缓冲档与入场阶段上。这同时消解了 C-2 的打分噪声问题——94.9 分与 95.1 分之间不存在「能不能买」的悬崖，只差一个缓冲档。
2. **核心层/战术层切分线由 L1/L2 | L3/L4 改为 L1/L2/L3 | L4/L5**。
3. **§8.13 入场阶段映射需要重新锚定，不能直接沿用。** 现行锚定行（2026-07-17 用户决定与判例：L1×低估=一段、L3×较低估=三段、L2×中性=三段/比亚迪判例）是按**旧档语义**校准的；新体系下同一家公司的档位标签会变（例如比亚迪很可能由旧 L2 落到新 L3），照抄会把校准搬错位。正确顺序是：锚点公司打分完成 → 用新档位重述这些判例 → 再内插其余映射。
4. 保留 §6.2.1 其余执行说明：战术层须先过 §10 人工闸门、事件通道关闭、价格自动定档双向不限幅、分层或档位变化后重跑池物化即自动重算。

### 4.3 相对现行标准的净变化

| 变化 | 方向 |
| --- | --- |
| L1 较高估：可买（需上修/催化）→ 仅可持有 | 收紧。§8.10 优先级表 **B 档整行删除**（现行 B = L1×较高估×盈利预测持续上修×强右侧信号），这是现行体系里唯一允许在偏贵位置买最强公司的口子 |
| L2 中性：可买 → 仍可买 | 不变（新 L2 语义严于旧 L2，实质是收紧） |
| L3 较低估：可买 → 仍可买；L3 中性：watch_only → 可持有 | 语义澄清 |
| L4 较低估：不可买 → 可买 | **放宽**（但新 L4 语义严于旧 L4，实质仍是收紧） |
| **L4/L5 在「中性」即提醒卖出** | **新增**。现行 §14 估值卖出统一在较高估触发，本方案把弱护城河公司的触发点前移到中性 |
| 全档「提醒卖出」由统一较高估改为按档分级 | 新增分级 |

### 4.4 卖出决策由「盈亏锚定」整体切换为「估值锚定」（2026-07-30 用户决定）

用户原述：**「不再保留之前的盈利后卖出的所有规定。统一改为按照现在的估值判定的思路，之前的盈利后退出的思路本质上是一种对于十倍股的贪婪，而一定程度上忽略了风险。一个股票的卖出应当基于其估值水平来确定，而不应当与当前盈利或者亏损多少有关。提醒卖出的仓位，可以参考其估值水平的偏离程度。」**

并明确**不设收益前提**——到档即提醒，与浮盈浮亏无关。

#### 4.4.1 现状核对：大部分已在 v1.12 生效

| 项 | 现状 | 本次要做的 |
| --- | --- | --- |
| 浮盈档位累计卖出上限表（+30%→10% 等）、3 个月锁定期 | **已于 v1.12（2026-07-20）退役**，§14 原文即「盈利本身既不是卖出理由，也不再限制卖出」 | 无需再动 |
| 个人体系 §12.5 周/月线三档保护线 | **已在工作流层退役**（v1.9 注记） | 正式删除条文 |
| 个人体系 §12.3 慢减仓表（含 >+500% 停止机械卖出、10 倍保留永久底仓）、§12.4 底仓比例表 | 已被降为「策略层的思想参考」，但条文仍在 | **正式废止**，由 §4.4.3 减仓梯替代 |
| §14 估值卖出的**留底原则**（剩余市值 ≥ 建仓金额 `build_amount_cny`） | **仍生效，且是最后一个成本锚定约束** | **废止**——与「卖出不应与盈亏有关」直接抵触 |
| §12.2 开始减仓的 7 项条件 | 仍在 | 由估值偏离度单一口径替代 |

#### 4.4.2 与 v1.21 的关系：不构成反转

v1.21（2026-07-29）去除上涨型机械警告时，用户原话是**「如果没有触发估值水平变化，或者趋势走坏，不应当提出类似超限这种的警告」**——该条**明确把估值触发的提醒排除在去除范围之外**。本次的减仓梯正是估值触发，**形式上与 v1.21 的条件完全一致，不是反转**。

反转的是**十倍股保护的实质立场**：v1.21 的顾虑是「错过大牛股」，本次用户改判为「对十倍股的贪婪、忽略了风险」。这是一次明确的立场变更，须在修订时如实留痕，不得表述为「一直如此」。

#### 4.4.3 减仓梯：由两行锚点反推的通则

用户给的两行锚点：

- **L1**：较高估继续持有 → 高估提醒卖出 20% → 在高估线基础上又高估 20% 时再减 40%
- **L5**：较低估继续持有 → 中性超过中轴提醒卖出 20% → 进入较高估再卖 20% → 进入高估再卖 20%

设合理价区间下沿 `L`、上沿 `H`、中轴 `M=(L+H)/2`。现行档位边界即：中性=[L,H]、较高估=(H, 1.2H]、高估=>1.2H。两行锚点可由同一条通则精确复现：

> **触发台阶 = {M, H, 1.2H, 1.44H}；每档从各自的提醒起点开始，价格每上跨一个台阶提醒减仓 20%；跨到 1.44H 及以上时步进加倍至 40%。**

- L1 起点 1.2H：1.2H→20%、1.44H→40% ✓ 与锚点一致
- L5 起点 M：M→20%、H→20%、1.2H→20% ✓ 与锚点一致

据此推出的完整五行：

| 层级 | 提醒起点 | M（中轴） | H（带顶/较高估） | 1.2H（高估） | 1.44H |
| --- | --- | --- | --- | --- | --- |
| L1 | 1.2H | 持有 | 持有 | 减 20% | 减 40% |
| L2 | H | 持有 | 减 20% | 减 20% | 减 40% |
| L3 | H | 持有 | 减 20% | 减 20% | 减 40% |
| L4 | M | 减 20% | 减 20% | 减 20% | 减 40% |
| L5 | M | 减 20% | 减 20% | 减 20% | 减 40% |

**对称性自洽**：卖出侧成对（L2,L3）与（L4,L5）、L1 单列，恰是买入侧配对（L1,L2）与（L3,L4）、L5 单列的镜像——这是 §4.2 半步设计的必然产物，不是巧合，也说明两侧口径互不矛盾。

#### 4.4.4 减仓比例的口径与例外

1. **按剩余股数计**（沿用旧 §12.3 约束 2 的口径）。副产品很重要：每次减剩余的 20%，仓位渐近趋零但**永不清仓，天然形成底仓**——这正好替代掉被废止的「留底 ≥ 建仓金额」，且不引入任何成本锚。
2. **景气阶段可减半为 10%**——用户原述「如果这家公司明显处于景气阶段，后续发展空间很大，则可以每次只减仓 10%」。**建议不用「景气」这种不可核验的软判断**，改挂现行体系已有且可核验的条件：**盈利预测持续上修**（§6.6 已用它作 L1×较高估 的附加条件）。满足则本次步进减半。
3. **提醒 ≠ 指令**：与止损（Tier-0 无条件清仓）性质不同，本列只产生提醒与建议比例，执行由用户决定。
4. **防抖（用户已确认需要）**：须**连续 N 个交易日收盘定型维持在该台阶**才发出提醒，避免单日冲高即提醒、次日回落即撤销。N=3 为初始校准，按 §12 回放与台账修订。
5. **恢复路径**：现行 §6.2.1.6「价格改档、证据改带」已覆盖用户所说的「等待下一次研报把估值区间上移」——带上移后台阶自动上移，无需新增机制，只需在提醒文案中写明该路径。

#### 4.4.5 实施前置：需写 §17.9 评估表

本改动废止 §12.3/§12.4/§12.5 与留底原则，属对既有规则的结构性修改。方向上是**允许更多卖出 = 降风险方向**，按 §17.3 第 5 条「方向豁免：降风险方向的指令永不拦截」**不构成 R7 拆除、不适用 5 个交易日冷静期**。但按 §17.5-§17.8 的既有惯例，仍须在个人体系新增 **§17.9 评估表**逐项留痕（用户原述、红线逐条核对、保留不变项清单）。

保留不变（本次不触及）：割肉价 Tier-0 逐票无条件清仓、硬证伪清仓、趋势卖出分态口径、单笔风险 1.5%N、账户回撤梯 -8%/-12%/-20%、仓位包络 100%-160%N、融资棘轮、结构上限。

### 4.5 「可见不可买」的处置（用户 2026-07-30 指示）

用户原述：**「我其实不希望有任何的可见不可买的情况存在，如果一个股票在 worth attention list 中，且评级、估值水平符合要求，而且出现了量价启动的信号，均应当一视同仁。」**

据此确立两条原则：

1. **买入端只有二态**：过矩阵 = 可买，不过矩阵 = 不可买。不存在「过了矩阵但因为别的类别标签而不可买」的情况。
2. **「可持有」是持仓端语义，不是一个观察类别**——它只对已持仓的股票有意义（不可加仓、不卖出），不用来给未持仓股票贴「可见但永远不能买」的标签。

**连带需要复核现行 `watch_only` 仅观察层（v20）**：它把未过矩阵但估值为低估/较低估/中性/较高估的组合物化进池文件、进入每日扫描可见范围但不具备买入资格——这正是一种「可见不可买」。需要区分它的两个功能后再决定存废（见 D-3f）：

- **可见性功能**：让用户看到接近可买的名字，价格一旦回落到带内，§6.2.1.6 次日自动恢复买入资格。删掉会失去预判视野。
- **禁买标签功能**：这部分与用户指示直接冲突。

倾向方案：**保留可见性、去掉「仅观察层」这个身份标签**——池文件继续展示全部 worth_attention 公司及其当日三态，但状态直接由矩阵算出（可买/可持有/提醒卖出/不可买），不再有独立的 `watch_only` 层级名。这样既满足「一视同仁」，又不损失扫描视野。

### 4.4 与 §14 卖出规则的咬合（必须先厘清，否则会与已退役机制打架）

现行卖出通道共四条（v1.12，v1.19 细化趋势卖出）：止损（割肉价 Tier-0）、硬证伪、**估值卖出（较高估 + 留底≥建仓金额）**、趋势卖出（分态）。

三态矩阵的「提醒卖出」列**不是新增第五条通道**，而是把既有的估值卖出通道改成**按质量档分级触发**：

| | 现行 | 三态矩阵后 |
| --- | --- | --- |
| L1 | 较高估即触发估值卖出 | **放宽到高估才提醒**（好公司容忍更多估值拉伸） |
| L2-L5 | 较高估即触发 | 不变 |

必须显式保留、不得被新矩阵覆盖的三项：

1. **留底 ≥ 建仓金额**——估值卖出不清仓，保留底仓。
2. **个人体系 §12 盈利后减仓与十倍股保护**——§12.3 在 >+500% 明确「停止机械卖出，改用趋势和 thesis 保护」，10 倍时 thesis 未破保留永久底仓。
3. **提醒 ≠ 指令**——v1.20/v1.21 已连续撤销两类「只可能因股价上涨触发」的机械警告（止损网格 6%/8%N、单笔风险持仓监控版），理由是大赢家被逐日警告会制造恐慌并促成过早卖出。估值卖出通道本身在那两次撤销中被保留，因此本列**在既有边界内**；但落地时必须写死它是提醒而非机械减仓指令，否则等于把刚撤销的失败模式换个名字装回来。

### 4.5 连带需要改的下游

| 位置 | 现状 | 需改为 |
| --- | --- | --- |
| §6.2.1 执行说明 1 | 「矩阵是买入准入，不是卖出指令；持仓档位滑出准入范围按 §14 风险预警 5 复核，不自动清仓」 | 重写——矩阵现含卖出提醒列；须保留「不自动清仓」 |
| §6.2 输出分类表 | 「是否进入核心估值合格池」单列 | 改为三态列 |
| §8.10 优先级表 | S=L1 / A=L1,L2 / B=**L1 较高估**+上修+强右侧 / C=L3 / D=L4 | **B 档整行删除**（L1 较高估不再可买）；C/D 改指 L4/L5 |
| §6.7 池物化 | `pool_layer`：core=L1/L2，tactical=L3/L4；watch_only 为未过矩阵者 | core=L1/L2/L3，tactical=L4/L5；`watch_only` 语义与新「可持有」态合并或区分，待定 |
| §14 估值卖出 | 统一较高估触发 | 改为按层级分级（L1 高估、其余较高估） |
| §7.3 复核队列 / §6.8 海外清单行序 / §6.1 与 §6 标题 / §5.4 流程图 | 「L1-L4」 | 「L1-L5」 |
| §6.6 估值审查执行范围 | 「只对 L1/L2 核心质量公司执行」 | 与新核心层（L1/L2/L3）对齐 |
| §5.7.1 校准细则 | 规则 2 / 4 / 5 按旧语义写 | 按新语义重写；旧 L3「战术理由」要求重新安放（见 §5） |

**§13 仓位金额不受影响**——重仓 50 万/轻仓 25 万由用户逐票决定，不与质量档挂钩。

### 4.6 脚本改动清单

| 文件 | 位置 | 内容 |
| --- | --- | --- |
| `build_a_share_core_valuation_pool.py` | L51 `TIER_ELIGIBILITY`、L56 `CORE_LAYER_TIERS`、L108/L329 档序元组 | 由单态资格集合改为三态映射；新增 L5；改核心层集合与档序 |
| `screen_daily_volume_price_signals.py` | L233-236 段位映射、L728/L735/L737 优先级判定 | 按新矩阵重写；新增卖出提醒输出 |
| `build_report_update_queue.py` | L67 `startswith(("L1","L2","L3","L4"))` | 加 L5 |
| `build_quarterly_quality_review_queue.py` | L68 `tier.startswith("L1")` | 复核频次是否随新档调整，待定 |
| `build_a_share_company_analysis_index.py` | 档位枚举 | 加 L5 |

### 4.3 连带需要改的下游

| 位置 | 现状 | 需改为 |
| --- | --- | --- |
| §6.2 输出分类表「是否进入核心估值合格池」列 | 「较低估→L1-L3」「中性→L1/L2」「较高估→仅 L1 常备；L2 watch_only」 | 按新五档重写 |
| §8.10 优先级表 | S=L1 / A=L1,L2 / B=L1较高估 / C=L3战术层 / D=L4战术层 | 五档重排，C/D 改指 L4/L5 |
| §6.7 池物化 | `pool_layer`：core=L1/L2，tactical=L3/L4 | core=L1/L2/L3，tactical=L4/L5 |
| §6.8 海外清单行序 | 按 L1→L4 排 | L1→L5 |
| §7.3 估值复核队列 | `quality_tier in ["L1","L2","L3","L4"]` | 加 L5 |
| §6.1 / §6 标题 / §5.4 流程图 | 「L1-L4」字样 | 「L1-L5」 |
| §6.6 估值审查执行范围 | 「只对 L1/L2 核心质量公司执行」 | 与新核心层定义对齐 |
| §5.7.1 校准细则 | 规则 2「L1 三种子类型」、规则 4「L3 不是中间桶」、规则 5「L4 是保守默认档」 | 按新语义重写；旧 L3/L4 的「战术理由」要求需要重新安放（见 §5 遗留问题） |

**§13 仓位金额不受影响**——重仓 50 万/轻仓 25 万由用户逐票决定，不与质量档挂钩。分层只决定买入准入与入场阶段，这一点改版前后不变。

### 4.4 脚本改动清单

| 文件 | 位置 | 内容 |
| --- | --- | --- |
| `build_a_share_core_valuation_pool.py` | L51 `TIER_ELIGIBILITY`、L56 `CORE_LAYER_TIERS`、L108/L329 档序元组 | 新增 L5 档、改核心层集合、改档序 |
| `screen_daily_volume_price_signals.py` | L233-236 段位映射表、L728/L735/L737 优先级判定 | 按新矩阵重写映射与优先级 |
| `build_report_update_queue.py` | L67 `tier.startswith(("L1","L2","L3","L4"))` | 加 L5 |
| `build_quarterly_quality_review_queue.py` | L68 `tier.startswith("L1")` | 复核频次是否随新档调整，待定 |
| `build_a_share_company_analysis_index.py` | 档位枚举 | 加 L5 |

---

## 5. 遗留问题：旧 L3/L4 携带的「战术理由」要求往哪放

旧 L3 的定义里绑了一条硬要求（§5.7.1 规则 4）：**必须至少有一个明确的战术关注理由**（行业催化、右侧触发候选、周期拐点、局部瓶颈、差异化技术、股东回报催化、有据可查的困境反转路径），理由只是「公司不错」则降 L4。

新体系是**纯护城河强度的一维尺子**，「有没有战术理由」与护城河强弱正交，无处安放。三个选项：

1. 作为**独立布尔字段**（`tactical_thesis` + 理由）保留，只对战术层（新 L4/L5）作买入前置——最贴近现状，建议。
2. 并入买入矩阵的「常备可买档位」条件文字（草案 4.2 的 L4 行已这样写）。
3. 取消——不建议，这会让战术层退化成「低分也能买」的口子。

---

## 6. 影响评估

### 6.1 分布迁移（本次改版最大的实际后果）

新 L2 = 90-95 分「很难颠覆」，按任何严肃口径都属 elite 区间。**现有 158 家 L2 不可能都留在 L2**。预期迁移方向：

- 现 L1 的 24 家：少数留新 L1/L2，多数落新 L3
- 现 L2 的 158 家：**绝大多数落新 L3/L4/L5**，其中一部分打分 <60 直接出 `worth_attention` → `boundary_pending`
- 现 L3/L4 的 79 家：多数落新 L5，部分出池

参考金字塔形状（**校准自检用，非配额**）：L1 ≤3%、L2 ~8%、L3 ~25%、L4 ~35%、L5 ~29%。偏离参考形状不算错误，但必须能逐票解释。

**这意味着改版后可买名单会明显收缩**，尤其是「中性档可买」的公司数量。是否接受这个收缩，是本方案最需要用户先确认的一件事——它比矩阵怎么排更影响实际操作。

### 6.2 执行成本

261 家 × 逐票证据驱动评分。ADR-0006 要求年报/公告/研报级证据，不得用聚合站简介。这是本轮最大的工作量，需要分批与锚点先行（见 §7）。

### 6.3 新增风险：分数漂移

引入分数后会出现新的失效模式——**为了让某只股票可买而微调分数**。质量分与 §6.2.1.6 的价格自动定档性质不同，不应每日变动。建议写死：

- 质量分**只在 §7.2 质量复核时变更，且变更必须有新证据**；
- 「无新证据的分数调整」纳入 §15.1 拦截清单，与 R1-R7 红线同构处理。

---

## 7. 实施路径草案（本轮不执行）

1. **定标准**：按待决清单收敛后修订 `docs/000_Ashare_workflow.md` §5.7/§5.7.1/§5.8/§6.2.1 及连带章节，新增打分细则章节；写新 ADR 记录「分层层重新引入评分」。
2. **定锚点**：先给 10~15 家跨行业锚点公司打分并由用户确认（茅台、宁德、比亚迪、中免、迈瑞、海康、紫金等），锚定分数尺度，再据此校准其余。锚点不确认不批量。
3. **批量重估**：按 `docs/peer-group-calibration/` 的行业 peer group 分批，每批出分数 + 七维理由 + 锚点对照。
4. **过渡期规则**：重估未完成的公司**冻结在旧档并标 `tier_pending_rescore`**，买入资格沿用旧矩阵，避免重估期间池子失效。全部完成后一次性切换矩阵。
5. **回归验证**：切换前后各跑一次池物化与每日扫描，对比可买名单差异并逐条解释。

新增 CSV 字段（`a_share_watchlist_quality_tiers.csv`）：`moat_score`、`score_version`、`scored_at`、`tier_basis`（`score` / `anchor_override`）、七个维度的 `*_score` 与 `*_reason`、`tactical_thesis`。

---

## 8. 决策记录与待决清单

### 8.1 已决（2026-07-30 用户确认）

| 编号 | 决策点 | 结论 |
| --- | --- | --- |
| D-1 | 旧 L5（剔除档）如何处置 | **退役该用法**，剔除完全由 `attention_class = boundary_pending` 表达 |
| D-2 | 定档分口径 | **护城河强度分**：护城河/技术壁垒/市场地位/不可替代性重构归一到 100 定档；行业前景、商业模式、经营质量、治理改为扣分项与一票否决项，只压不抬 |
| D-3 | 准入矩阵形态 | **三态矩阵**（可买 / 可持有 / 提醒卖出），交替缓冲档设计，定稿见 §4.2。原 E1/E2/E3 平移方案作废 |
| D-3a | L5 是否保留买入资格 | **保留**，仅低估可买 |
| D-3b | 卖出侧是否设地板 | **不需要**——交替缓冲档已排得下五档，L4/L5 在中性即提醒 |
| D-3c | 提醒卖出是否防抖 | **需要**（初始校准 N=3 个交易日收盘定型） |
| D-4 | 是否接受可买名单收缩 | **接受** |
| D-8 | 是否允许「可见不可买」状态存在 | **不允许**。买入端只有二态；「可持有」仅是持仓端语义 |
| D-3d | 提醒卖出是否设收益前提 | **不设**。到档即提醒，与浮盈浮亏无关 |
| D-3f | 现行 `watch_only` 仅观察层存废 | **去掉层级身份标签、保留可见性**——池文件继续展示全部 worth_attention 及其当日三态，状态由矩阵算出 |
| D-3g | §12.3「>+500% 停止机械卖出」与新提醒的冲突 | **废止 §12.3**，不再保留十倍股保护。卖出统一由估值偏离度决定 |
| D-9 | 卖出决策的锚 | **由盈亏锚定整体切换为估值锚定**；一并废止 §12.3/§12.4/§12.5 与 §14 留底原则（最后一个成本锚） |
| D-9a | §4.4.3 五行减仓梯 | **成立**，按 §4.4.3 表执行 |
| D-9b | 减仓比例口径 | **按剩余股数**——渐近趋零但永不清仓，天然留底 |
| D-9c | 「景气阶段减半为 10%」的判定依据 | **挂钩「盈利预测持续上修」**（体系已有且可核验），不用「景气」这类软判断 |
| D-5 | 旧 L3 的「战术理由」硬要求 | **独立字段 `tactical_thesis`**，只对战术层（L4/L5）作买入前置 |
| D-6 | 60 分线与资本复制测试如何咬合 | 资本复制测试不通过 → 复制门槛维度封顶；机械实现见打分细则「总分 ≤ 复制门槛维度分」 |
| D-7 | 是否一并处理 OI-001（D 档参数成文化） | **一并** |

### 8.2 待决

| 编号 | 决策点 | 选项 | 建议 |
| --- | --- | --- | --- |
| **D-10** | 打分细则的三维权重与封顶规则是否定稿 | 见 `docs/000_Ashare_moat_strength_rubric.md` | 已按 D-2 起草并做锚点试算，待用户校准 |

### 8.3 打分细则

护城河强度分的维度、权重、分档锚点、封顶项与定性闸门另见 **`docs/000_Ashare_moat_strength_rubric.md`**（设计中，未生效）。
